# MergeReport.md — 백엔드 2벌 상태의 **병합 판단**과 채점 구조 · `merge/final` 실행 기록

#report #decision-queue

> **작성일**: 2026-08-04 · **1차 개정**: 2026-08-05 (§2 전면 정정 + §4~§7 신설)
> **2차 개정**: 2026-08-05 — **제안 문서에서 실행 기록으로 전환.** `merge/final` 브랜치에서
> §7 조치를 실제로 수행했다. 새 절: **§8 야간산포도 상세와 대안**(사용자 요청) ·
> **§9 실행 결과**(커밋·점수 전후·실호출 검증). 종전 §8(한계)은 **§10**으로 밀렸다.
> **작업 브랜치**: `merge/final`(`origin/dev` = `57bdff4`에서 분기, alembic head `0035` → `0037`)
> **대조 대상**: `origin/feature/main-logic` = `d54b0de`
>
> 🔴 **이 문서는 [[FinalReport]]와 성격이 다르다.**
> [[FinalReport]]는 *"문헌은 확보됐는데 코드에 없는 것"*의 대기열이고,
> 이 문서는 *"코드가 두 벌로 갈렸을 때 무엇을 고를 것인가"*의 대기열이다.
>
> ⚠️ **2026-08-05 정정 고지**: 초판 §2는 *"밤 기온 평년값이 전 지역 NULL이고 일조는 밴드 제작
> 불가"*라고 적었다. **틀렸다.** CSV ETL(`load_weather_climatology.py`) 한 경로만 보고
> API 적재 경로(`load_aws_climatology.py`)와 read-time 환산(`sunlight_calculation.py`)을
> 확인하지 않은 오판이다. §2-1에 실제 배선을 다시 적고, 그로 인해 추천이 **ⓑ → ⓐ′로
> 바뀐 것**까지 §2-4에 남긴다. 초판 결론을 지우지 않고 무엇이 왜 뒤집혔는지 병기한다.
>
> ⚠️ **2026-08-05 2차 정정 고지 (2건)** — 1차 개정이 새로 만든 오류다.
> 1. **«`risk_width` 차단 사유가 소멸했다 → 상추 0점 붕괴가 완화된다»는 틀렸다.**
>    `_indicator_score`는 `allowed_min`이 없으면 `_risk_score`에 **닿기 전에** `0.0`을
>    반환한다. `risk_width`는 허용경계 **밖**의 척도라 `allowed_min`이 있어야 발화한다.
>    상추 야간을 살리는 것은 산포도가 아니라 `allowed_min` **값 하나**다. §2-2·§2-3·§6-2에
>    각각 정정을 달았다. 상세와 대안은 **§8**.
> 2. **«감자 야간 allowed −3~28»은 틀렸다.** `−3`은 감자 **`temp_day` 초기 단계**의
>    하한(`0012`, Stegner 2019)이고, 야간 행의 `allowed_min`은 **NULL**이다. 즉 감자 야간은
>    10℃ 아래로 내려가면 상추와 똑같이 **즉시 0점**이다(§5-4의 «감자가 이미 그 형태다»도
>    이 오독에 기댔다 — 정정했다).

## 읽는 법

| 표기 | 뜻 |
|---|---|
| 🔴🔴🔴 | 전제가 틀렸다 — 선택하기 전에 사실을 고쳐야 한다 |
| 🔴🔴 | 결정하면 점수·등급이 실제로 바뀐다 |
| 🔴 | 결정이 필요하나 점수는 안 바뀐다(표기·구조) |
| 🟠 | 미뤄도 되지만 미룬 사실을 남겨야 한다 |
| ⭐ | 이 절의 핵심 근거 |
| 🔵 | 오해하기 쉬운 세부 |

선택지는 ⓐⓑⓒ로 적고 **추천을 반드시 하나 고른다**.

---

# 0. 30초 요약

|       | 질문                | 답                                                                                                                                                                      |
| ----- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **①** | 밤 기온·일조량을 총점에 넣을까 | 🔴🔴 **밤 기온은 넣는다(ⓐ′).** 평년값은 AWS 일통계 API로 **이미 채워져 있고**(255구역) 고도 보정·KNN까지 돈다. 막는 것은 데이터가 아니라 **밴드 커버리지 3/5 작물**이며, 그건 표기로 처리한다. **일조는 보류** — 값은 있으나 문헌이 `partial` 1건뿐 |
| **②** | 결측을 50점으로 칠까      | **아니다.** min 구조에서 50은 점수가 아니라 **C등급 확정**이다. 대안은 상수가 아니라 **계층적 대체**(실측 → KNN/국가통계 → 제외 → 판정 보류). 측정된 신뢰도가 있는 건 KNN뿐(`temp_avg` MAE **0.467℃**) → §3                     |
| **③** | 두 MLCM은 어떻게 다른가   | 🔴 **내 것은 1층 평탄 min, FarmML은 2층**(토양 내부는 평균, 층 간만 min). 국가 원문(심교문 2016)이 토양을 **합산**으로 처리하는데 내 구현이 거기에도 min을 겹쳤다 — **내 쪽이 방법론을 잘못 옮긴 것**이다 → §4                         |
| **④** | 최적구간 vs 서리 임계     | **속도 대 사건**이다. 최적구간은 평균·누적으로 재는 생장 속도, 임계는 순간 극값으로 일어나는 비가역 사건. **평균이 극값을 지우기 때문에** 한 칸에 넣으면 둘 다 못 말한다 → §5                                                            |
| **⑤** | 3밴드 + 연속/계단 함수    | ⭐ **이미 구현돼 있다.** 연속형(로그 3구간)·계단형(배점표) 둘 다 코드에 존재한다. 빈 것은 함수가 아니라 **파라미터 4개 중 3개**이고, 그 3개는 휴리스틱 허용 대상이다. 단 `optimal`은 허용하면 안 된다 → §6                                   |
| **⑥** | (부수) 두 백엔드 병합     | 🔴 `feature/main-logic`은 알렘빅 리비전 `0032`·`0033`이 `dev`와 충돌해 **머지 불가**. 팀 미결 4건 중 3건이 그 브랜치에 구현돼 있어 리넘버링 후 부분 회수 → §7                                                    |
| **⑦** | 야간 산포도는 믿을 만한가    | 🔴 **부분적으로만.** «전국 지역들이 서로 얼마나 다른가»(공간 분산)를 재는 값을 «작물이 얼마나 견디나»(생물학적 내성)의 자리에 놓은 것이다. 이진 채점을 피하는 **스케일 조정자**로는 쓸 만하고 실측 개선도 있으나 **감수 곡선으로 믿을 값은 아니다.** 야간에서는 원천마저 없다 → **§8** |
| **⑧** | (실행) 무엇을 했나        | ✅ `merge/final`에 **4커밋**. 🔴 부수 성과: `soil_profile_client`가 **전 호출 실패**하고 있던 것을 실호출로 잡았다(심토토성·자갈·경사 전 필지 결측). `drainage`는 우리가 부를 수 있는 어느 토양 API에도 **없다** → **§9**       |

---

# 1. 상황 — 백엔드가 왜 2벌이 됐나 (사실관계)

| 시각 | 일 | 결과 |
|---|---|---|
| 08-04 10:49 | FarmML **PR #11**(내 것, ML만) | **닫힘 — 머지 안 됨** |
| 08-04 11:01 | FarmML **PR #12** (`dotoe1234-pixel`) *"PR #11 대체"* | 닫힘 |
| 08-04 12:21 | FarmML **PR #13** *"FinalReport 33항목 팀 답변 반영"* | **머지됨** → FarmML `main` |
| 08-04 13:13 | main repo **PR #104** *"산출물 미러"* + 백엔드 **`0035`** | **`dev`에 머지됨** |
| 08-04 | 내 `origin/feature/main-logic` push | **PR 없음 — 아무도 안 봄** |

⭐ 팀은 내 백엔드 작업이 존재하는지 모르는 상태에서 같은 문제를 `0035`로 다시 풀었다. PR #104 본문이 그대로 드러낸다:

> *"`crop_growth_guide`에 재배형 컬럼이 없다 — 유니크 키가 `(crop_id, growth_stage, indicator)`라 한 지표에 두 행을 못 둔다. 노지 값 하나만 실었고, **FarmML에 이 사실을 알려야 한다**"*

**그 컬럼과 유니크 키 재정의는 내 `0032`가 이미 한 일이다.** 기술 문제가 아니라 전달 실패다.

## 1-1. `0035`가 이미 반영한 것 (내 `0033`과 겹침)

| 대상 | `0035` | 내 `0033` | 판정 |
|---|---|---|---|
| 사과 `k` | `0.3~0.6 / 0.15~0.75` | 같음 | **중복** |
| 감자 `ph` | `5.5~7.0 / 4.75~7.75` | 같음(단 노지·시설 **2행**) | 값 중복 · 구조 미반영 |
| 감자 `organic` | `20~30 / 15~35` | 같음 | **중복** |
| 사과 `ca` | `5.0~6.0 / 4.5~6.5` **양측 복귀** | 단측(`0030`) 유지 전제 | 🔴 **어긋남** |

🔵 사과 `ca` 양측 복귀는 팀의 실측 결정이고 근거가 붙어 있다(`allowed_max 6.5`는 문헌값이 아니라 역산치라 `derived` 표기). 점수 영향은 크다 — `ca` 평균 96.9→46.8, 60점 미만 6→**100/150 지역**, 사과 총점 74.9→68.6. **내 계약 테스트는 단측 전제라 그대로는 실패한다.**

---

# 2. 🔴🔴 질문 ① — 밤 기온·일조량을 총점에 넣을까

## 2-1. ⚠️ 초판 정정 — 두 지표 모두 **값은 확보돼 있다**

내가 본 것은 `scripts/load_weather_climatology.py` 하나였고, 그건 **1순위 경로**(농업기상 월값 CSV)다. 그 파일이 야간최저를 주지 않는 건 사실이지만, **그걸 메우려고 만든 2순위 스크립트가 따로 있다.** `scripts/load_aws_climatology.py` docstring이 내 오판을 그대로 반박한다:

> *"지금 평년치는 농업기상 116개 구역만 있고 나머지 140개는 KNN으로 남의 동네 값을 빌려 쓴다. 게다가 `temp_night_min_normal`은 **전 행 NULL**이라 장기 탭이 야간 저온을 아예 채점하지 못한다 — **문제정의서가 지목한 A씨 실패 원인이 그 지표인데도** 그렇다. **AWS 관측망(534지점)이 둘 다 고친다.**"*

그리고 그 배선은 **완료됐다.** `backend/app/services/climatology_service.py`의 `NORMAL_FIELDS` 주석(2026-08-03 확인):

> *"`temp_night_min_normal`은 **AWS 배선(PR #68·#69)으로 채워졌고**, `sunlight_normal`은 지금도 전 행 NULL이지만 **그건 결손이 아니라 설계다** — 일조는 `solar_radiation_normal`에서 읽을 때 환산한다(저장하면 보정계수가 바뀔 때 값이 낡는다). 실측 일조가 생기면 우선 필드로 쓰인다."*

### 실제 배선 (전량 API)

| 지표 | 평년(장기 탭) | 단기 탭 | 계산 로직 |
|---|---|---|---|
| `temp_night_min` | ✅ 기상청 apihub `typ01` 지상/AWS 일통계 **`ta_min`** (`aws_daily_client.OBS_TEMP_MIN`) → `load_aws_climatology.py` | ✅ 단기예보 **`TMN`** (`forecast_client.CAT_TEMP_MIN`) → `weather_snapshot.temp_night_min` | 534지점 → 구역 **k=5 거리역수 가중평균**, 10km 상한, `aws_temp_calibration.json` 고도 보정 |
| `sunlight` | ✅ 농업기상 V3 `sun_Time`(분→시간) **또는** `solar_radiation_normal`(MJ/m²/day, `srqty`)에서 환산 | 🔴 단기예보 미제공(`DAILY_UNAVAILABLE_INDICATORS`에 명시 제외) | `services/sunlight_calculation.py` + `calibrate_sunlight.py` + `validate_solar_donors.py` |
| 결측 구역 | ✅ read-time **KNN 대체** k=3 · 고도 100m 필터 · 감률 0.65℃/100m | - | `climatology_service._rank_donors`/`_weighted_normals` |

🔵 **`LAPSE_FIELDS = ("temp_avg_normal", "temp_night_min_normal")`** — 야간최저까지 고도 감률 보정 대상이다. 즉 이 지표는 "겨우 채워진" 게 아니라 **보정·대체·검증 3단이 다 붙어 있다.**

측정된 대체 정확도(leave-one-out, 255구역): 정규화 MAE **k=3 0.0846** / k=5 0.0862 / k=10 0.0901, `temp_avg` 절대 MAE **0.4667℃**(고도 필터 적용). 근거 원본 `docs/ml/climatology_knn_validation.json`.

## 2-2. 그래서 무엇이 달라지는가

| 초판 §2-1의 주장                                        | 실제                                                                       | 결과                         |
| -------------------------------------------------- | ------------------------------------------------------------------------ | -------------------------- |
| 밤 기온 평년값이 없어 **넣을 수 없다**                           | AWS API로 채워짐 + 고도 보정 + KNN 대체                                            | 🔴 **ⓐ가 실행 가능해진다**         |
| 일조 밴드는 **단위 불일치로 제작 불가**                           | `solar_radiation_normal`이 **MJ/m²/day 그대로** 있다 → 문헌 단위로 직접 채점 가능(환산 불필요) | 🔴 **불가 판정 철회** — 후보로 복귀   |
| 밤 기온은 **단기 탭에만** 실값                                | 평년·단기 **양쪽**                                                             | 장기 탭에서도 채점된다               |
| `risk_width`는 *"산포도 낼 실측이 없어"* NULL 불가피(`0020` 주석) | ~~255구역 야간 평년 확보 → 산포도 계산 가능~~ → **2차 정정: 산출 경로가 없다.** `data/03_weather_monthly_modified.csv`는 `avg_temp`·`precipitation` 2컬럼뿐이고 야간 평년은 **DB에만** 있는데 `.env`에 `DATABASE_URL`이 없다 | 🔴 **차단 사유는 남아 있다.** 게다가 채워도 상추에는 **발화하지 않는다**(아래) → §8 |

### 🔴 그런데 **안 달라지는 것 3건** — 이쪽이 실제 제약이다

1. **밴드 커버리지가 3/5 작물이다.** 이건 데이터가 아니라 **문헌** 문제라 API가 해결하지 않는다.

| 작물 | 야간 밴드 | 상태 | 출처 |
|---|---|---|---|
| 감자 | `10~14℃`(비대기), allowed `**NULL**~28` | ✅ **시드됨** — 🔴 **하한 없음 → 10℃ 아래는 즉시 0점**(상추와 같은 절벽) | [[climate-soil-integrated-suitability-2021]] 표26 / 상한 28은 `0012`(Zhang 2024 2차 인용) |
| 상추 | `10~15℃`(잎 분화 최적) | `filled` — 시드 가능 | [[rda-lettuce-handbook-023]] |
| 오이 | `15~18℃`(생육적온 야간) | `filled` — 시드 가능 | [[rda-cucumber-handbook-107]] |
| 배 | 🔴 절대 임계 없이 **상대값만** — 대조 대비 야간 +1.5~2.8℃면 밀병 2배(20.0→39.5%) | 밴드 불가 | [[pear-watercore-hayama-2014]] |
| 사과 | 🔴 **0건** — `0021`이 삭제(`optimal 8~8`, 폭 0이라 8.00℃ 아니면 상시 0점) | 0건 | - |

2. **min 구조에서 커버리지 불균등은 작물 간 불공정이다.** min은 지표를 하나 더 넣으면 총점 상한이 낮아질 기회만 늘고 되돌아오지 않는다. 야간 행이 3작물에만 있으면 **사과·배는 감점 항목이 하나 적어 구조적으로 유리**해진다. 우리 화면은 5작물 총점을 나란히 놓고 *"이 밭에 무엇이 맞나"*를 말하므로 이 비대칭이 곧 잘못된 추천이 된다.

3. **세 밴드는 전부 「생육 최적 구간」이고 서리 임계가 아니다.** 문제정의서의 실패(개화기 봄 서리)는 이 밴드로 잡히지 않는다 — 이유는 §5. 사과 개화기 서리 임계는 저장소 **0건**, 감자 국내 정량 임계도 **0건 확정**(교본 `서리` 15쪽 전수 확인).

## 2-3. 선택지

|        | 내용                                                                                            | 장점                                                              | 단점                                                          |
| ------ | --------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ----------------------------------------------------------- |
| **ⓐ**  | 야간 최저기온을 총점(min 집합)에 넣는다 — 오이·상추 시드 + 감자 기존 행                                                 | 평년·단기 값이 다 있다 / [[PRD]] §7 방침 / 문제정의서 지표가 총점에 등장                | 3/5 작물이라 사과·배가 구조적으로 유리 / 상추 하한 10℃ + allowed 부재 → 0점 붕괴 위험 |
| **ⓐ′** | **ⓐ + 3조건**: ① `risk_width`를 야간 실측 산포도로 채운다 ② 커버리지(채점 항목 수)를 응답·UI에 병기 ③ **서리는 별도 경보 축으로 유지** | ⓐ의 두 단점을 각각의 원인 지점에서 막는다 / 「모르는 건 비워 두고 표기」 원칙과 §3 답이 같은 논리로 일관 | 표기 UI 작업이 붙는다 / 사과·배 야간은 여전히 0건(문헌 대기)                      |
| **ⓑ**  | 총점에서 빼고 경보로만                                                                                  | 커버리지 불균등 무해 / FarmML 총점과 안 갈림                                   | **채점할 수 있는 것을 안 하는 것** — 값·보정·검증이 다 있는데 쓰지 않는다              |
| **ⓒ**  | 기온 축을 `기후 = 낮·밤 합산`으로                                                                         | 야간이 단독으로 상한을 못 정한다                                              | 합산 비율에 근거 0([[FinalReport]] §1-11 재발) / FarmML 3단과 축 정의가 갈림 |

### ⭐ 추천 — **ⓐ′** (초판 ⓑ를 철회한다)

1. **초판 추천의 3번째 근거가 사라졌다.** *"평년값이 NULL이라 넣어도 채점이 안 된다"*는 사실이 아니었다. 그 근거가 빠지면 ⓑ는 *"쓸 수 있는 실측을 쓰지 않는다"*가 되고, 이건 [[CLAUDE-coding]] §18-4(우리 장애를 데이터 한계로 위장하지 않기)에 정면으로 걸린다.
2. **커버리지 3/5는 표기로 다룰 문제다.** 사과 야간이 비어 있는 건 문헌 부재이고, 우리 원칙은 «모르는 것을 지어내지 않고 모른다고 적는다»다. 여기서 야간 전체를 빼면 «모르는 하나 때문에 아는 셋도 버린다»가 되어, §3에서 「50점 대입 거부」에 쓴 논리와 어긋난다. **두 답이 같은 원칙 위에 서야 한다.**
3. ~~**`risk_width` 차단 사유가 소멸했다** — 상추 0점 붕괴 위험은 `0020`이 야간 산포도를 낼 수 없어 생긴 것인데, 이제 255구역 평년으로 낼 수 있다. **단점 하나가 데이터로 해소된다.**~~

   🔴 **2차 정정 — 이 근거는 두 겹으로 틀렸다.** ⓐ′는 여전히 추천이지만 근거는 1·2번뿐이다.

   - **(a) 발화하지 않는다.** `risk_width`는 허용경계 **밖**의 감쇠 척도다. 상추 야간은 문헌이 하한 10℃만 주고 `allowed_min`이 없어 `_indicator_score`가 `_risk_score`에 **닿기 전에** `0.0`을 반환한다. 값을 채워도 그 칸은 안 바뀐다.
     ```python
     if lo is not None and value < lo:
         if guide.allowed_min is None:
             return 0.0, "risk"      # ← 여기서 끝. risk_width를 보지도 않는다
     ```
     0점 붕괴를 막는 것은 **`allowed_min` 값 하나**(±50% 휴리스틱 = 7.5℃)다. 순서가 반대였다.
   - **(b) 채우면 오히려 가팔라질 수 있다.** 완충폭 폴백이 이미 `risk_width` 후보보다 **넓은** 행이 있다. 감자 야간(`optimal 10~14`)에 `temp_day` 수준의 산포도(≈2.2~2.9℃)를 넣으면 종전 폴백보다 좁아져 **감점이 세진다.** «완화»라고 적은 것은 방향까지 틀렸다.

   → 실제 실행에서는 신설 야간 행의 `risk_width`를 **전부 NULL로 두었다**(§9). 상세는 **§8**.

**일조는 보류**: 값은 있으나 밴드가 `crop_growth_guide` **0행**이고, 유일한 문헌 후보 [[potato-hightemp-daylength-radiation-kim-2016]]이 `partial`에 *"차광 시험이 아니라 계절·시설 부산물이라 기온·일장과 교락"*이라는 한계가 붙어 있다. **한 작물·교락 있는 근거로 5작물 총점에 축을 세우지 않는다.** 대신 `SEASONAL_UNAVAILABLE_INDICATORS`처럼 **명시 제외 목록에 올려** 지금의 "라벨은 있는데 조용히 채점 안 됨" 상태를 끝낸다.

---

# 3. 🔴🔴 질문 ② · ⑤ — 결측 처리, 현행과 대안의 신뢰도

## 3-1. 현행은 사용자가 아는 그대로다 (제외)

```python
if raw is None:
    breakdown[indicator] = {"status": "missing"}
    risk_flags.append(f"{indicator}:missing")
    continue                     # 가중합·가중치합(그리고 내 min 집합) 전부에서 제외
```

`backend/app/`에 `UNFILLED`·`= 50.0` **0건**. 즉 백엔드는 **한 번도 상수를 대입한 적이 없다.**

🔵 다만 **이건 마지막 층이다.** 그 앞에 이미 대체가 두 단 있다 — 기상은 `climatology_service`의 read-time KNN, 토양은 `district_soil_service`/`repair_empty_soil_state.py`. 현행을 *"결측이면 그냥 빼는 단순 정책"*으로 읽으면 실제보다 나쁘게 보는 것이다. **정책은 이미 계층적이고, 제외는 계층의 끝이다.**

## 3-2. 물통(min) 구조에서 50점이 특히 위험한 이유

가중평균에서 50은 다른 지표가 희석한다. **min에는 희석 장치가 없다** — 지표 하나가 결측이면 총점이 **50 상한에 고정**되고 `_grade` 컷(S≥90/A≥75/B≥60/C)에서 곧바로 **C 확정**이다. *"모른다"*가 *"절반쯤 좋다"*가 아니라 *"부적합하다"*로 바뀐다.

🔴 그리고 FarmML 자신이 부작용을 기록해 뒀다:

> *"`subsoil_texture_score` 50은 '가능지'와 '판정불가'가 같은 숫자로 나온다. **점수만 읽고 등급을 역추론하면 안 된다**"*

🔵 또 *"FarmML과 맞추려면 50"*이라는 전제가 성립하지 않는다. FarmML의 `UNFILLED_SCORE = 50.0`은 **3순위**(KNN → 컬럼 평균 → 50)이고 토양은 150/150 조인이라 사실상 발동하지 않는다. 우리는 KNN을 쓸 수 없다(밭 단위 실시간, 이웃 밭 표본 없음) → **같은 50을 써도 FarmML은 잔여만·우리는 결측 전부에 적용되어 더 크게 갈린다.**

## 3-3. 대안 전수와 신뢰도

|       | 방식                                                           | 신뢰도 근거                                                                                                                                                      | 판정                                                |
| ----- | ------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| **ⓐ** | **제외**(현행) — min·평균 대상에서 뺀다                                  | **가정 0개**(측정 불필요). 🔴 대신 min에서 **총점 상향 편향** — 데이터 적은 밭이 높게 나온다                                                                                              | ✅ 유지                                              |
| **ⓑ** | 상수 50                                                        | 근거 **0건**. 토성에서 실제 등급값과 충돌 기록 있음                                                                                                                            | ❌                                                 |
| **ⓒ** | **기상: 지역간 KNN**(이미 구현) k=3 · 고도 100m 필터 · 감률 0.65℃/100m      | ⭐ **유일하게 측정된 값** — leave-one-out 정규화 MAE **0.0846**, `temp_avg` 절대 **0.4667℃**. 한계도 명시돼 있다: 결측이 무작위가 아니라(관측지점 없는 곳 = 산간·도서) **실제 오차는 이보다 클 수 있다** `[확인 필요]` | ✅ **이미 적용 중** · `substituted_from`·`donors` 표기 필수 |
| **ⓓ** | **토양: 읍면동 통계 대체** — `soil_chem_stat_client`(농경지화학성 V2, 면적가중) | 국가 공식 집계라 지어낸 값이 아니다. 🔴 단 **3지표만**(pH·유기물·유효인산), **조사연도 필드가 응답에 없어** 몇 년도 값인지 모르고, 필지 실측이 아니라 읍면동 평균                                                       | 🟠 **1순위 후보** — 클라이언트·테스트는 있고 **서비스 배선이 없다**      |
| **ⓔ** | 작물별 전국 중앙값                                                   | 분포 중심이라 방향 오류는 작지만 **지역 변별력 0** — 채점의 목적을 지운다                                                                                                               | 🟠 최후                                             |
| **ⓕ** | 보수적 대입(허용경계 값)                                               | 근거 0. *"모르면 나쁘게"*는 정직해 보이나 과다 감점이고, ⓑ와 같은 종류의 조작                                                                                                            | ❌                                                 |
| **ⓖ** | **판정 보류** — 필수 지표 결측 시 점수·등급을 내지 않는다                         | **가정 0개.** ⓐ의 상향 편향을 막는 **유일한 무가정 수단**                                                                                                                      | ✅ ⓐ와 짝                                            |

**값을 만들어내는 방식(ⓑ~ⓕ) 중 측정된 신뢰도가 있는 것은 ⓒ 하나뿐이다.** ⓓ는 "국가 통계"라는 출처 신뢰는 있으나 우리 쪽 정확도가 측정되지 않았고, ⓔ·ⓑ·ⓕ는 근거가 없다.

### ⭐ 추천 — **ⓐ + ⓒ(현행) + ⓖ + 커버리지 병기**, ⓓ는 배선 후 2순위

계층을 명시적으로 고정한다: **필지 실측 → (기상)KNN·(토양)읍면동 통계 → 제외 → 필수 결측이면 판정 보류.** 각 층의 출처를 응답에 싣는다 — `substituted_from`·`donors`·`is_substituted`가 이미 있으므로 **새 계산은 커버리지 카운트 하나뿐이고, 그것도 `risk_flags`에서 세면 된다.**

### ✅ 2026-08-05 실행분 — 사용자 확정으로 **`outcomes/` 미러도 제외 방식으로 통일**했다

사용자 정정: **KNN 대체는 기상 데이터에만 적용한다.** 토양은 '리' 단위 API 조회라 CSV로 전수 적재할 데이터가 아니고, 이웃 리에서 빌려온 값이 그 필지의 값이라는 근거가 없다.

- `outcomes/scripts/ml/`의 토양 KNN 대체 + 컬럼평균 + `UNFILLED_SCORE = 50.0` 폴백을 **전부 폐기**했다(`FILL_MISSING = False`). 결측은 NaN으로 남아 `mean`·`min`이 건너뛴다.
- **탐지는 계속 돈다** — 이상치 플래그·물리범위 위반은 채우는 것과 별개 기능이므로 하나를 끄려고 다른 하나를 버리지 않았다(`impute(fill=False)` 신설).
- 부수 효과: 종전 §3-2에 인용한 *"`subsoil_texture_score` 50은 '가능지'와 '판정불가'가 같은 숫자"* 문제가 **소멸**했다. 이제 50점은 '가능지' 하나만 뜻한다.
- 실측 영향과 등급 분포 전후는 **§9-5**. **판정불가(NaN)가 된 지역은 0개**다 — ⓖ(판정 보류)가 실제로 발동한 밭이 없다는 뜻이므로, ⓖ는 «구현했지만 아직 쓰이지 않는 안전장치»가 아니라 **여전히 미구현 상태로 남았다**(발동 조건이 없어 급하지 않다).

---

# 4. 🔴 질문 ③ — MLCM 두 구현의 정확한 차이

## 4-1. 내 구현 (`feature/main-logic` `608bf3f`)

```python
scored: list[float] = []                       # 토양 7~8 + temp_day + temp_night_min + … 전부 한 통
for guide in guides:
    if raw is None:      continue              # 결측 → min 집합에서 빠진다
    if not _is_valid(...): continue
    score, status = _indicator_score(value, guide)
    if not scored or score < min(scored):
        limiting_indicator = indicator          # 지표 단위 제한요인
    scored.append(score)
    weighted_sum += score * weight              # 병기용으로만 누적
score = round(min(scored), 1)                   # ← 주 총점
grade = _grade(score)
score_weighted_mean = round(weighted_sum / weight_sum, 1)   # 회귀 진단용 병기
```

- **1층 평탄 min.** 토양·기온·야간이 같은 통에서 한 번에 min된다.
- **`weight`가 총점에 영향을 주지 않는다.** min은 가중치를 쓰지 않으므로, [[FinalReport]] §1-11ⓒ가 유지한 weight는 병기 평균에만 쓰인다.
- 제한요인은 **지표 1개**.

## 4-2. FarmML (`build_regional_score.py`, `dev` 미러 v5)

```python
# 1단: 토양 — 지표별 밴드 점수를 낸 뒤 균등평균(가중 아님)
df[f"soil_score_total_{crop}"] = df[indicator_scores].mean(axis=1).round(1)

# 2단: 기후 — 앵커월 블렌드 평균기온 1축
df[f"temp_{crop}_{name}_score"] = score_column(df[col], temp_rule(...))

# 3단: 두 층을 min
df[f"total_score_{crop}_{name}"] = pd.concat([soil, temp], axis=1).min(axis=1).round(1)

# 진단 보존
df[f"..._mlcm_flat"]     = df[factor_cols].min(axis=1)                    # 평탄 min
df[f"..._weighted_mean"] = soil * SOIL_FRAC + temp * TEMP_FRAC            # 종전 정의
limiting_factor          = 토양이 결속하면 그 안 최저 지표, 아니면 "기온"
```

근거로 인용한 것은 **심교문 2016 국가 적지평가**의 3층 구조다:

```
토양 항목 → 요인별 점수제 **합산** → 토양 등급
기후 항목 → **최대저해인자법**     → 기후 등급
두 등급을 다시 **최대저해인자법**으로 통합
```

## 4-3. 대조표

|              | 내 구현                                                                                                | FarmML                                                     |
| ------------ | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| MLCM을 적용하는 층 | **전 지표 1층**                                                                                         | **토양↔기후 통합 1곳** (+ 기후 내부, 지금은 1축이라 자기 자신)                  |
| 토양 내부 합성     | `min`                                                                                               | **균등평균** `mean`                                            |
| `weight` 사용  | 총점 미사용(병기 평균만)                                                                                      | 총점 미사용(균등평균) — 종전 `weighted_mean`만 `SOIL_FRAC`/`TEMP_FRAC` |
| 기후 축 구성      | `temp_day`·`temp_night_min`이 **각각** min 대상                                                          | **기온 1축뿐**(강수 weight 0, 야간 데이터 없음)                         |
| 결측           | 제외                                                                                                  | KNN → 컬럼평균 → **50**                                        |
| 제한요인         | 지표 1개                                                                                               | **층 + 층 안 최저 지표** 2단                                       |
| 실측 결과        | 사과 58.6→**24.9**, 배 77.7→18.9, 오이 80.4→16.2, 감자 81.6→15.0, 상추 75.7→**6.6** ([[MainLogicReport]] §6) | 사과 주 총점 **68.6** (평탄 min은 23.9)                            |
| 등급 분포        | 거의 전량 C                                                                                             | S13 A36 B57 C44                                            |

## 4-4. ⭐ 무엇이 진짜 차이인가

**같은 「MLCM」이라는 말을 서로 다른 층에 쓰고 있다.** 국가 원문은 MLCM을 **기후 내부와 층 통합에만** 쓰고 토양 내부는 **합산**으로 처리한다. 내 구현은 그 구분을 없애고 토양 내부에도 min을 걸었다.

결과는 산술적으로 예정된 것이다 — 토양 지표가 7~8개면 min은 그중 최악 하나가 되고, 지표가 많아질수록 총점은 단조 감소한다. FarmML 실측이 그대로 보여준다: 평탄 min에서 150지역 중 **사과 138·상추 148·감자 143이 C**, 그 최소값을 만든 1위는 **유효인산**(57/150 — 상한 350인데 전국 중앙값 391.8로 **98/150이 밴드 밖**)이다. 즉 내 총점은 *"이 밭이 부적합하다"*가 아니라 *"우리 유효인산 밴드가 전국 분포와 어긋난다"*를 5작물 전체에 걸쳐 반복해 말하고 있었다.

🔵 **등급 공간 min은 대안이 아니다.** [[MainLogicReport]] §6이 완화안 ⓐ로 올린 것인데, 등급 함수가 점수에 대해 단조이므로 `min∘등급 = 등급∘min`이고 팀 실측에서 **150지역×5작물 전수의 등급 분포가 평탄 min과 완전히 일치**했다(한 지역도 바뀌지 않는다). **내가 올린 완화안이 수학적으로 무효다.**

→ **총점 구조는 FarmML의 2층 국가 구조로 통일한다.** 내 평탄 min은 진단 지표(`_mlcm_flat`)로만 의미가 있고, 그 자리는 FarmML에 이미 있다.

---

# 5. 질문 ④ — 「생육 최적 구간」과 「서리·냉해 임계」의 차이

## 5-1. 서로 다른 물리 현상이다

|           | 생육 최적 구간                                          | 서리·냉해 임계                                                 |
| --------- | ------------------------------------------------- | -------------------------------------------------------- |
| 무엇을 재는가   | **속도** — 생장률·분화율·수량 잠재력                           | **사건** — 조직 결빙·세포막 파괴                                    |
| 시간 척도     | 누적·평균(월·생육기 전체)                                   | **순간**(1회, 수십 분~수 시간)                                    |
| 피해 성질     | 가역·연속 — 느려질 뿐                                     | **비가역·불연속** — 그 해 수확 소멸 가능                               |
| 값의 형태     | **구간** (10~15℃)                                   | **점 + 노출시간** (−2.2℃, 30분)                                |
| 필요한 입력 통계 | 월평균 야간최저                                          | **일최저의 극값**(평균이 아니다)                                     |
| 벗어나면      | 점수가 서서히 내려간다                                      | 밴드로 표현할 수 없다 — 사건 확률·경보                                  |
| 우리가 가진 예  | 상추 야간 `10~15℃`(분화 최대) · 오이 `15~18℃` · 감자 `10~14℃` | 감자 잎 냉해 `−3℃`(Stegner 2019, **2차 인용**) · 사과 개화기 = **0건** |

## 5-2. ⭐ 왜 한 칸에 넣으면 안 되는가 — **평균이 극값을 지운다**

4월 월평균 야간최저 8℃는 최적구간(10~15)에서 조금 벗어난 값이라 점수로는 «약간 감점»이다. 그런데 그 달에 −2℃가 **하루** 있었으면 사과는 그 해 수확이 없다. **점수는 8℃를 보고 "조금 나쁨"이라 말하고, 농민은 전손을 입는다.** 척도가 다른 두 현상을 한 지표에 담으면 총점은 둘 중 어느 것도 말하지 못한다.

## 5-3. 두 번째 이유 — 피해 성격이 **작물마다 반대**다

- 감자는 늦서리로 지상부가 죽어도 **지하부에서 재출현**한다(교본 p150).
- 사과·배 개화기 서리는 **그 해 결실이 끝난다.**

같은 −3℃가 감자에선 회복 가능한 손실, 사과에선 전손이다. **하나의 서리 곡선을 5작물에 공통 적용하면 감자를 과다 감점한다.** [[registry]]가 이를 *"설계상 가장 중요한 발견"*으로 기록해 뒀다.

## 5-4. 🔵 다만 «임계를 밴드로 쓸 수 없다»는 뜻은 아니다

`allowed_min`에 임계를 넣고 그 방향의 성격을 `literature_limit`(경계 점수 0)으로 표기하면, **임계는 밴드 구조 안에서 연속 곡선으로 표현된다** — 절벽이 아니라 «최적경계 100점 → 임계 0점»으로 이어지는 감쇠다.

🔴 **2차 정정**: 초판은 *"감자 야간 행이 이미 그 형태다 — `optimal 10~14` + `allowed_min −3`"*이라고 적었다. **틀렸다.** `−3`은 감자 **`temp_day` 초기 단계**의 하한이고(`0012`), 야간 행의 `allowed_min`은 **NULL**이다. 즉 감자 야간에는 두 성격이 공존하지 않고 **하한이 그냥 비어 있다.**

실제로 이 형태를 갖춘 첫 행은 `0037`이 넣은 **사과 서리 행**이다 — `optimal_min −1.7`(만개 동해 한계, 교본 표8-1) + `allowed_min −3.9`(만개기 90% 동사, 표1-4) + `allowed_min_kind = 'literature_limit'`. 한 생육단계에 문헌이 **두 점**을 준 유일한 사례라 밴드가 세워졌다(배는 두 수치가 서로 다른 단계라 불가, §9-2). 두 성격을 구분해 표기하는 컬럼이 `0036`의 `allowed_min_kind`/`allowed_max_kind`다.

그래서 §2-3 ⓐ′의 3번 조건(«서리는 별도 경보 축»)은 *"밴드로 못 쓴다"*가 아니라 **«월평균 입력으로는 못 쓴다»**는 뜻이다. 같은 임계값을 단기 탭의 **일최저 극값**에 걸면 그게 경보가 된다.

---

# 6. 질문 ⑤ — 3밴드 + 연속/계단 함수의 구현 가능성

## 6-1. ⭐ 이미 구현돼 있다

희망한 구조가 현행 `_indicator_score`(백엔드)와 `band_score`(outcomes)의 정의 그대로다. 2026-07-27에 선형·이차·로그 중 로그를 고른 결과다.

```
최적구간 안                     100
최적경계 이탈 순간               95        ← 이탈 자체의 고정 감점
허용구간              95 → 60,  log 곡률(최적 근처 완만 · 허용경계 근처 급락)
허용경계                        60        ← B등급 하한 (내 boundary_score(kind)로 0까지 가변)
위험구간              60 →  0,  같은 곡률을 반대로 (급락 후 완만한 꼬리)
위험폭 밖                        0
```

`_log_falloff(x) = ln(1+9x)/ln(10)` — 연속·단조이고 구간 경계에서 값이 이어진다(95·60). 감쇠 거리는 `risk_width`(전국 실측 산포도 × 2.0, `0020`), 없으면 완충폭 1배 폴백. **단측 밴드**(한쪽 경계만 문헌에 있는 경우)도 계약으로 지원된다.

**계단형도 이미 있다** — `category_score(code_scores)`가 범주형(토성)을 국가 배점표 `20/15/10/5 → 100/75/50/25`로 처리한다. `drainage`의 U자형(양호 20 > 매우양호 15)은 이 경로에 그대로 들어간다. **필요한 건 함수가 아니라 배수등급 입력 컬럼이다.**

## 6-2. 그래서 빈 것은 함수가 아니라 **파라미터**다

| 파라미터 | 지금 채우는 근거 | 빈 칸 | 휴리스틱 허용 |
|---|---|---|---|
| `optimal_min/max` | 대부분 문헌 | 사과 야간 등 | 🔴 **허용 불가** (아래 6-4) |
| `allowed_min/max` | 일부 문헌, 다수 `optimal ±50%` | 상추 야간 등 | ✅ **허용** — `allowed_*_kind='heuristic'` 표기 |
| `risk_width` | `0020` 실측 산포도 × 2.0 | `temp_night_min`·`rainfall_daily` | 🔴 **2차 정정 — 여전히 막혀 있다.** 산출 원천이 DB에만 있고, 채워도 `allowed_min`이 없는 칸에는 발화하지 않는다 → §8 |
| 허용경계 점수 | 일괄 60 | 절대한계는 0이어야 | ✅ 내 `boundary_score(kind)`가 그 자리 |

## 6-3. 실행 순서

1. **`0036`** — 성격 컬럼 4종(`cultivation_type`·`allowed_min_kind`·`allowed_max_kind`·`method`) + 유니크 키 재정의. 점수 불변이라 리뷰가 가볍다. → ✅ **실행됨**
2. **`boundary_score(kind)` 주입** — `_allowed_score`/`_risk_score`에 `boundary` 인자. `literature_limit`만 0, 나머지 60 유지([[FinalReport]] §1-9 결정). → ✅ **실행됨**
3. ~~**`risk_width` 야간 산포도 적재**~~ → 🔴 **하지 않았다.** 원천 부재 + 발화하지 않음(§8). 신설 야간 행은 `risk_width` NULL이다.
4. **오이·상추 야간 밴드 시드** — `optimal`은 문헌값, `allowed`는 `heuristic` 표기. → ✅ **실행됨**(`0037`)
5. **배 야간은 비워 둔다** — 문헌 0건. 커버리지 표기로 드러낸다(§2-3 ⓐ′ 조건 ②). **사과는 야간 행이 생겼다** — 다만 생육 밴드가 아니라 **서리 임계** 행이다(§5-4).

## 6-4. 🔴 한 가지 경계선 — `optimal`에는 휴리스틱을 허용하지 말 것

`allowed`·`risk_width`·경계점수는 *"얼마나 빨리 깎을까"*의 **척도**라 근거가 약해도 방향이 틀리지 않는다. `optimal`은 *"무엇이 좋은가"* **자체**라, 틀리면 전 지역이 반대로 채점된다.

실제 사례가 있다 — 사과 기온 `18~28`은 어느 책의 「생육적온」 행에서 왔는데, 1차 출처 Kim 2016 Table 3의 같은 「생육적온」 행은 **`10.0~20.0`**이다(PR #13 지적). 게다가 원표 영문 행명이 `Temperature during the age of maturity`(성숙기 기온)인데 한글 재인용본이 「생육적온」으로 옮긴 것이라, **행 자체가 다른 것을 가리킬 가능성**이 남아 있다.

→ **휴리스틱 허용 범위를 `allowed`·`risk_width`·`경계점수` 3개로 한정**하고, `optimal`이 빈 지표는 «채점하지 않음 + 표기»로 두는 것을 권한다.

---

# 7. 🔴 병합 실무 — 지금 할 수 있는 조치

## 7-1. `feature/main-logic`은 **머지 불가**다

| 리비전 | `origin/dev` | `feature/main-logic` |
|---|---|---|
| `0032` | `0032_weather_snapshot_daily_max_and_hourly` | `0032_guide_provenance_columns` |
| `0033` | `0033_long_term_recommendation` | `0033_open_field_and_prescription_bands` |
| `0034` | `0034_daily_recommendation_input_hash` | - |
| `0035` | `0035_farmml_20260804_band_sync` | - |

둘 다 `revision = "0032"` / `down_revision = "0031"`이다. 합치면 알렘빅이 리비전 중복으로 뜨지 않는다. PR #104도 같은 함정을 적었다 — *"로컬 워킹트리에는 `0030`까지만 있어서 로컬에서 번호를 세면 충돌한다."*

## 7-2. 살릴 것 / 버릴 것

| 구성물 | 판정 | 이유 |
|---|---|---|
| `0032` 성격·재배형 컬럼 4종 + 유니크 키 재정의 | ✅ **`0036`으로 회수** | PR #104 미결 **#3·#4**를 그대로 해소 + §6-2의 표기 인프라 |
| `0033` 감자 pH 노지·시설 **2행 공존** | ✅ **`0037`로 회수** | `0035`는 노지 1행만 실었다 |
| `0033` 밴드 3건(사과 `k`·감자 `ph` 값·감자 `organic`) | ❌ 버린다 | `0035`가 같은 값으로 반영 |
| `boundary_score(kind)` 주입 | ✅ **회수** | PR #104 §3이 *"`boundary` 주입 자리가 필요하다"*고 지목한 그것 |
| `_pick_cultivation_type`(재배형 우선) | ✅ **회수** | 감자 pH 2행의 전제 |
| **MLCM 평탄 min 총점** | ❌ **버린다** | §4-4 — 국가 방법론 오적용, 등급공간 완화안도 무효 |
| `outcomes/` 미러 + 산출물 5건 | ❌ 버린다 | `dev`가 v5, 내 것은 v3/v4 |
| 계약 테스트 사과 `ca` 단측 전제 | 🔧 수정 | `0035`가 양측(`derived`)으로 복귀 |

## 7-3. 순서

1. `dev`에서 새 브랜치(`feature/guide-provenance`). 7커밋 중 5개를 버리므로 리베이스보다 새로 담는 쪽이 짧다.
2. `0036` 성격 컬럼 — **단독 PR**(점수 불변).
3. `0037` 감자 pH 2행 + 재배형 우선 선택.
4. `boundary_score(kind)` — 점수가 바뀌므로 변경 전/후 표 첨부.
5. **총점은 FarmML 2층 구조로 통일**(§4-4). 이 시점에 §2-3 ⓐ′·§3 추천이 반영된다.
6. `risk_width` 야간 산포도 + 오이·상추 야간 밴드(§6-3).

## 7-4. 🟠 이미 push된 브랜치

`origin/feature/main-logic`(`d54b0de`)을 그대로 두면 다음 사람이 `0032` 충돌을 다시 만난다.
**추천**: 브랜치는 남기고 [[MainLogicReport]] 최상단에 *"리비전 충돌로 머지 불가 — §7-2 표대로 부분 회수. 평탄 min 총점은 폐기."*를 커밋한다. 조사 근거와 점수 측정치가 그 안에 있어 지우면 손실이다.

---

# 8. 🔴🔴 야간 산포도 — 무엇이고, 믿을 만한가, 대안은 무엇인가

> 사용자 요청 절이다. §2-3 ⓐ′ 3번 근거를 철회한 이유가 여기 다 들어 있다.

## 8-1. 정의 — 무엇을 재는 값인가

`risk_width`는 채점 곡선에서 **허용경계(60점) → 0점까지의 거리**다. `0020`이 그 거리를 이렇게 정했다.

```
risk_width = 2.0 × robust_sd,      robust_sd = 1.4826 × MAD
```

🔴 **MAD를 내는 대상이 핵심이다 — 작물이 아니라 「전국 지역 간 분포」다.** 그 지표가 국토 안에서 지역마다 얼마나 흩어져 있는지를 재고, 그 폭의 2배를 «위험구간의 길이»로 쓴다. 실적재값:

| 지표 | n | 중앙값 | robust_sd | risk_width |
|---|---|---|---|---|
| `ph` | 126 | 5.908 | 0.232 | 0.4641 |
| `ca` | 103 | 7.231 | 1.889 | 3.7777 |
| `p2o5` | 126 | 419.7 | 137.67 | 275.33 |
| `temp_day`(사과 앵커월) | 136 | 20.79 | 1.112 | 2.2239 → `0023`에서 2.219 |
| **`temp_night_min`** | — | — | — | **NULL** ← 비어 있는 칸 |

**「야간 산포도」란 = 같은 계산을 `temp_night_min`에 적용하는 것**이다. `0020`이 NULL로 둔 이유는 주석에 그대로 적혀 있다 — *"지역간 산포도를 낼 실측이 없어 NULL로 둔다."*

## 8-2. 왜 도입됐나 — 이 값은 «참»이라서 쓰는 게 아니다

`0020` 이전의 감쇠 거리는 **완충폭**(허용경계 ~ 최적경계 간격)이었다. 그런데 완충폭은 `allowed = optimal 폭 ±50%` 휴리스틱에서 나오므로, **optimal이 좁은 지표는 허용구간도 위험구간도 같이 좁아진다.** 3중 압축이라 채점이 사실상 이진이 됐다.

| 실측(outcomes 150지역) | 개정 전 0점 | 개정 후 |
|---|---|---|
| 상추 `pH` | **107건** | 44건 |
| 사과 기온 | **92건** | 35건 |
| 상추 `ca` | 56건 | 6건 |

즉 `risk_width`는 **적어도 그 지표의 실제 측정 스케일에 비례한다**는 점에서 완충폭보다 나은 근사이고, 개선이 실측됐다. **«덜 임의적이라서» 쓰는 값이다.** 표준편차 대신 MAD를 쓴 것도 옳다 — 유효인산 실측 최대 1288mg/kg 같은 극단값이 SD를 부풀려 스스로를 정상으로 만드는 것을 막는다.

## 8-3. ⭐ 신뢰도 — 세 가지로 나눠 답한다

### (1) 재는 것이 다르다 (근본 문제)

`risk_width`는 **«전국 지역들이 서로 얼마나 다른가»(공간 분산)** 를 재는데, 채점에 필요한 것은 **«작물이 얼마나 견디나»(생물학적 내성)** 다. 두 양은 물리적으로 무관하다.

극단으로 밀면 드러난다 — 한국 전역의 야간최저가 균일하면 `robust_sd = 0` → `risk_width = 0` → 허용경계를 0.1℃ 넘어도 즉시 0점. 국토가 넓어 편차가 크면 관대해진다. **작물 생리는 국토의 지리적 다양성과 아무 상관이 없다.**

`0020` 자신이 인정한다: *"배수 2.0은 문헌 근거가 아니라 명시적 휴리스틱이다(정규 가정에서 전국 지역 약 95% 포함 폭). 실제 감수 곡선 문헌 확보 시 교체 대상."*

### (2) 야간에 확장하면 `temp_day`보다 한 단 낮다

| | `temp_day` | `temp_night_min` |
|---|---|---|
| 원천 | 관측지점 **월값 CSV** 직접 | `weather_climatology.temp_night_min_normal` |
| 성질 | 관측값 | **합성값** — AWS 지점 k=5 거리역수 가중평균 + 고도 보정 + (구역 없으면) KNN k=3 대체 |
| 결과 | 분산이 원 관측 그대로 | 🔴 **가중평균이 잡음을 깎아 분산이 축소된다** → 실제보다 **좁게** 나온다 → 채점이 **가팔라진다** |

방향이 안전한 쪽이 아니다. 여기에 계절 문제가 겹친다 — 야간최저는 3월과 6월의 지역 간 편차 차이가 일평균보다 크다(복사냉각은 분지·내륙에서만 강하다). 앵커월을 하나로 묶어 MAD를 내면 **어느 달을 대표로 쓰느냐에 값이 크게 흔들린다.**

🔴 **그리고 지금은 계산 자체가 불가능하다.** `data/03_weather_monthly_modified.csv`는 `avg_temp`·`precipitation` 2컬럼뿐이라 야간 컬럼이 없고, 합성 평년값은 DB에만 있는데 `.env`에 `DATABASE_URL`이 없다.

### (3) 🔴 그리고 상추 문제에는 애초에 듣지 않는다

`risk_width`는 허용경계 **밖**의 척도라 `allowed_min`이 있어야 도달한다. 상추 야간은 문헌이 하한 10℃만 주고 allowed를 안 줘서 `allowed_min is None`에서 **즉시 0점**으로 빠진다. `risk_width`가 무슨 값이든 무관하다. (§2-3 2차 정정의 코드 인용 참고.)

거꾸로도 위험하다 — 감자 야간(`optimal 10~14`, 폭 4)의 완충폭 폴백은 `optimal` 폭의 절반인 **2℃**이고, `temp_day` 수준의 산포도(**2.2~2.9℃**)를 넣으면 조금 넓어진다. 그러나 상추 야간(`optimal 10~15`, 폭 5 → 완충폭 2.5℃)에 야간 산포도가 그보다 좁게 나오면 **채점이 지금보다 세진다.** 어느 방향으로 움직일지 계산 전에는 알 수 없다 — «완화 장치»로 부르면 안 된다.

## 8-4. ⭐ 대안 — 셋뿐이고, 순서가 있다

| | 대안 | 무엇을 하는가 | 신뢰도 | 판정 |
|---|---|---|---|---|
| **ⓐ** | **`allowed_min`을 휴리스틱으로 먼저 채운다** (`optimal` 폭 ±50%) | 상추 야간 `allowed_min = 7.5℃`, 감자 야간 `allowed_min = 8℃`. 그러면 «10℃ 아래 즉시 0점»이 «10 → 7.5℃ 구간 95 → 60점 감쇠»가 된다 | 근거는 **휴리스틱**이나 [[FinalReport]] §1-9로 **이미 승인된 규칙**이고 `allowed_*_kind='heuristic'`으로 표기된다. 방향이 틀릴 수 없다(경계를 **넓히는** 쪽) | ✅ **1순위 — 이것이 실제 0점 붕괴의 해법이다.** `risk_width`는 이게 있어야 의미를 갖는다 |
| **ⓑ** | **AWS 지점 「일값 원자료」에서 월별로 산포도를 낸다** | `data/aws_daily_cache/`의 지점 일최저를 월별로 묶어 MAD → 월별 `risk_width`. 합성 평년값의 분산 축소를 피한다 | ⓒ가 없는 동안의 **차선**. 여전히 공간 분산이지 감수 곡선은 아니다(§8-3(1) 그대로 적용) | 🟠 **2순위 — 야간에 산포도를 쓸 거면 이 경로가 맞다.** 합성 평년값에서 내면 안 된다 |
| **ⓒ** | **농작물재해보험 서리 피해율 통계** | 「야간 최저 X℃에서 피해율 Y%」를 직접 준다 → 감쇠 거리가 **실제 피해 곡선**이 된다 | ⭐ **유일하게 성질이 맞는 근거.** 온도와 실제 피해를 잇는 국내 데이터다 | ✅ **본해(本解)** — 저장소에 0건이나 확보 가능성 있음. 확보되면 ⓐ·ⓑ를 **교체**한다 |

**폐기 후보**: 문헌 감수 곡선 직접 인용 — 5작물 전수 조사에서 **0건**이다(있으면 애초에 `0020`이 산포도를 쓰지 않았다).

### ⭐ 권고

1. **지금**: `allowed_min` 휴리스틱(ⓐ)을 상추·감자 야간에 넣는다. 0점 붕괴가 실제로 해소되는 유일한 조치다. `risk_width`는 계속 NULL로 둔다 — **없는 것을 NULL로 두는 것은 정직이고, 성질이 다른 값으로 채우는 것은 아니다.**
2. **다음**: 야간 `risk_width`가 필요해지면 ⓑ로 산출하고, **월별로 따로 내서 어느 달을 썼는지 `risk_width_source`에 적는다.**
3. **본해**: ⓒ를 확보 대상으로 [[registry]] 필요문헌에 올린다.

🔵 어느 경우에도 `risk_width_source` 컬럼과 `[확인 필요]` 표기는 유지한다. 이 값은 «측정된 산포도»이지 «측정된 내성»이 아니라는 사실이 UI까지 전달되어야 한다.

---

# 9. ✅ 실행 결과 — `merge/final`

> §7 제안을 실제로 수행한 기록. 브랜치는 `origin/dev`(`57bdff4`)에서 분기했다.
> **push·PR은 하지 않았다** — 사용자 확인 대기.

## 9-1. 커밋

| 커밋 | 내용 |
|---|---|
| `9700b56` `fix(soil)` | **토양특성 단면정보 클라이언트 결함 2건** — 아래 §9-3 |
| `47f3b83` `feat(db)` | **`0036`** 성격 컬럼 4종 + 유니크 키에 `cultivation_type` 포함 |
| `0040bef` `feat(scoring)` | **`0037`** 야간 밴드·서리 임계·감자 pH 2행 + `pick_cultivation_type` + `boundary_score(kind)` + **국가 3단 총점** |
| `917d416` `chore(outcomes)` | 미러의 **토양 KNN 대체·50점 폴백 폐기** + 산출물 재생성 |

테스트: 백엔드 **539 passed**(신규 13건 — 성격별 경계 0/60점, 국가 3단 총점, 결측 제외, 재배형 우선). `outcomes/` 자체 테스트 4종 전부 통과.

## 9-2. 밴드 변경분 (`0037`)

| 대상 | 값 | 성격 | 근거 |
|---|---|---|---|
| 오이 `temp_night_min`(`growing`) **신설** | `15~18`, allowed `13.5~19.5`, weight 1.5 | `heuristic` | [[rda-cucumber-handbook-107]] 표5+본문 |
| 상추 `temp_night_min`(`spring`·`fall`) **신설** | `10~15`, allowed `7.5~17.5`, weight 2.0 | `heuristic` | [[rda-lettuce-handbook-023]] — 문헌이 «야간이 주간보다 영향 크다»고 명시해 `temp_day`와 같은 weight |
| 사과 `temp_night_min`(`fruit_growth`) **신설** | `optimal_min −1.7` / `optimal_max NULL` / `allowed_min −3.9`, weight 1.0 | 🔴 **`literature_limit`**(경계 0점) | [[rda-apple-handbook-2025]] 표8-1 만개 동해 한계 + 표1-4 만개기 90% 동사 |
| 감자 `ph` **시설 행 신설** | `facility 5.5~6.2`, allowed `5.15~6.55` | `heuristic` | 처방 5차 p83. 기존 `0035` 행(5.5~7.0)은 2010판 **노지** 값이라 `open_field`로 정정 → **2행 공존** |

**만들지 않은 것과 이유**

| 대상 | 사유 |
|---|---|
| 배 야간 밴드 | 문헌 수치가 「겨울 최저 −20℃ 전후 언 피해」뿐 — **휴면기 동해 경고**이지 생육 밴드가 아니다 |
| 배 서리 행 | 교본이 화총 **−3.5** / 만개·유과 **−1.7**을 주는데 **서로 다른 생육단계**다. 한 단계의 최적경계·허용경계 2점이 되지 않아 밴드를 세울 수 없다 |
| 감자 서리 행 | 2026-08-05 사용자 지시. (근거상으로도 괴경 −1.4~−1.9는 지상부와 별개 지표이고 Boydston 2006이 **온도×시간 조합**으로 주어 단일 임계로 환원되지 않는다) |
| 야간 `risk_width` | §8 — 원천 부재 + 발화하지 않음 |
| `sunlight` 밴드 | 지침 행 0건. 소스는 배선돼 있으나 문헌이 `partial` 1건뿐(§2-3 말미) |

🔴 **생육단계를 새로 만들지 않았다.** `crop_growth_stage`에 개화기가 없고(사과 `fruit_growth` 111–263 / `coloring` 233–293 / `maturity` 294–314), `pick_stage`는 단계를 **하나만** 고른다. `flowering`을 끼우면 그 창에서 사과 `temp_day` 행이 사라져 **통째로 미채점**이 된다. `fruit_growth`(4/21~9/20)가 만개 직후 서리 위험기를 포함하므로 그 단계에 얹었다.

⚠️ **서리 행의 실효 범위**: 장기 탭이 보는 것은 **월 평년 최저기온**이라 −1.7℃가 거의 걸리지 않는다. 이 행이 실제로 작동하는 곳은 **단기 탭**(일 최저 실측·예보 — `aws_daily_client.ta_min` / `forecast_client.TMN`)이다. 장기에서 안 걸리는 것은 결함이 아니라 평년값의 해상도 문제다.

## 9-3. 🔴 부수 성과 — `soil_profile_client`가 전 호출 실패하고 있었다

실호출로 잡았다. **심토토성·심토자갈·경사가 전 필지 결측으로 들어오고 있었다.**

| 결함 | 현행(수정 전) | 실측 정답 |
|---|---|---|
| URL | `SoilEnviron/SoilCharacSctnn/getSoilCharacterSctnn` → `NO_OPENAPI_SERVICE_ERROR`(HTTP 400) | `SoilEnviron/SoilCharacSctnn/`**`V2`**`/getSoilCharacterSctnn` → **200** |
| 응답 필드 | `PNU_Code` · `Deepsoil_Qlt_Code` · `Deepsoil_Ston_Code` · `Soilslope_Code` | `PNU_Cd` · `Deepsoil_Qlt_Cd` · `Deepsoil_Ston_Cd` · `Soilslope_Cd` |
| 명세서 표기 | `soilV3_API-Guide.md`가 준 `/V3` 경로 | **`/V3`도 응답하지 않는다.** 200을 주는 것은 `/V2`뿐 |

🔴 **테스트가 못 잡은 이유**: 픽스처 XML이 코드의 오해를 그대로 베껴 `*_Code`로 적혀 있었다. **운영에서 전 호출이 결측으로 새는 동안 테스트는 계속 초록이었다.** 실호출 응답으로 교체하고 `/V2` 경로를 테스트로 못박았다.

🔵 `scripts/collect_region_soil_profile.py`는 처음부터 옳은 URL·필드명을 쓰고 있었고 그 docstring이 *"soil_profile_client.py도 이 스펙에 맞게 같이 수정함"*이라 적었는데 **실제로는 반영되지 않았다.** 인증키는 `soil_API`·`soilV3_API` 둘 다 200이라 `settings.soil_api` 그대로 쓴다.

## 9-4. 🔴 `drainage` — 우리가 부를 수 있는 어느 토양 API에도 없다

실호출 확인 결과다.

| API | 응답 | 배수등급 |
|---|---|---|
| 토양검정 화학성 `SoilExam/V2` | 200, 13필드(`ACID`·`OM`·`VLDPHA`·`POSIFERT_*`·`ELCD` 등) | ❌ 전부 화학성 |
| 토양특성 **단면**정보 `SoilCharacSctnn/V2` | 200, **정확히 3필드**(심토토성·심토자갈·경사) | ❌ |
| 토양특성 **상세**정보 27종 (data.go.kr **15144225**) | ⚠️ **서비스 경로 미확인** — 후보 30여 개 전부 `NO_OPENAPI_SERVICE_ERROR` | ⭕ **여기 있다**(배수등급·유효토심·과수적성등급) |

🔵 `SOIL_DETAIL_API` 키는 **살아 있다** — 단면정보에 던지면 `403`이 온다(= 다른 서비스에 승인된 유효 키). 즉 **데이터셋 승인은 돼 있고 엔드포인트 경로만 모른다.**

🔵 `soilV3_API-Guide.md`는 **단면정보 명세서**다(문서 제목 「토양특성 **단면**정보V3」, API명 `SoilCharacSctnn`, 응답 명세 3필드). 그 명세서의 샘플 PNU로 호출해 200 + 실데이터를 받았으므로 PNU가 문제였던 것도 아니다.

→ **이번 브랜치에서 배수는 채점하지 않는다.** 문헌 배점표는 이미 확보돼 있다 — 심교문 2016 / [[climate-soil-integrated-suitability-2021]] 표11·14가 독립 일치로 **양호 20 > 매우양호 15 > 약간양호 10 > 불량 5**의 **U자형**(과배수도 감점)을 준다. 계단형 채점 경로(`category_score`)도 이미 있다(§6-1). **15144225 명세서만 들어오면 클라이언트 + 밴드를 바로 얹을 수 있다.** 🔴 단조 가정(«배수가 좋을수록 좋다»)으로 구현하면 틀린다.

## 9-5. 채점 결과 — 변경 전/후 (150지역 재산출)

`outcomes/` 미러의 결측 처리 개정(KNN 대체·50점 폴백 → 제외)만의 효과다. 밴드값은 바뀌지 않았다.

| 작물 | 평균(전) | 평균(후) | 차 | 등급 S/A/B/C (전) | (후) |
|---|---|---|---|---|---|
| 사과 | 68.6 | **69.3** | +0.7 | 13 · 36 · 58 · 43 | 9 · 46 · 58 · 37 |
| 배 | 66.6 | **67.3** | +0.8 | 9 · 36 · 60 · 45 | 18 · 31 · 53 · 48 |
| 상추 | 65.6 | **63.7** | −1.8 | 0 · 31 · 77 · 42 | 2 · 36 · 62 · 50 |
| 감자 | 71.8 | **72.9** | +1.1 | 11 · 59 · 56 · 24 | 29 · 44 · 52 · 25 |
| 오이 | 72.3 | **70.4** | −1.8 | 0 · 68 · 61 · 21 | 4 · 66 · 52 · 28 |

- **총점이 판정불가(NaN)가 된 지역은 0개**다 — 모든 지역이 토양·기후 두 층 각각에서 최소 한 지표를 갖는다. «50점을 빼면 데이터 없는 밭이 판정불가로 사라진다»는 우려는 실측에서 발생하지 않았다.
- 방향이 양쪽으로 갈리는 이유: 결측 지표를 빼면 나머지 지표가 50점보다 좋은 지역은 올라가고 나쁜 지역은 내려간다. **S·C 양 끝이 동시에 늘어난 것**(사과 S 13→9지만 A 36→46, 배 S 9→18)이 그 신호다 — 50점 대입이 분포를 중앙으로 눌러왔다는 증거다.
- 🔴 **한계**: 지표 수가 지역마다 달라져 총점의 비교 가능성이 균일하지 않다. `*_missing` 플래그를 UI에 반드시 노출해야 한다.
- **기준선 검증**: 커밋본 `RegionalScore.csv`가 같은 입력에서 **93/93 수치 컬럼 일치**로 재현됨을 먼저 확인한 뒤 재산출했다. (입력 CSV가 `outcomes/data/`에 없고 리포 루트 `data/`와 `outcomes/data/ml/`로 갈려 있어, 스크래치패드에 합본을 만들어 돌렸다 — 리포지토리는 건드리지 않았다.)

## 9-6. 🔴 백엔드 ↔ 미러의 남은 비대칭 (의도된 것)

두 구현의 점수가 갈리면 안 되는 계약이지만, **구조적으로 갈리는 두 곳**을 명시한다.

| 지점 | 백엔드 | `outcomes/` 미러 | 사유 |
|---|---|---|---|
| 야간 최저기온 축 | ✅ 4행 채점(사과 서리·오이·상추·감자) | ❌ **축 자체가 없다** | 미러 입력 `data/03_weather_monthly_modified.csv`가 `avg_temp`·`precipitation` 2컬럼뿐. 밴드를 미러에 적어두면 채점되는 것처럼 보이므로 **적지 않았다** |
| 감자 pH 재배형 | 2행(노지·시설) | 1행(노지) | `soil_overrides`가 지표당 한 밴드 구조. 채점에 쓰이는 값은 **같다**(노지) |

→ `_shared.json`의 `knowledge_version`·`scoring_version`을 **`2026-08-05-v6`**으로 올리고 두 비대칭을 사유와 함께 기록했다. v5 이력은 `*_v5_note`로 보존했다.

🔵 부수로 잡은 것: `outcomes/scripts/ml/test_crop_literature_anchor.py`의 `_repo_root()`가 `CLAUDE.md`를 찾아 올라가는 탓에 **ForYourFarm 리포에서는 이 테스트가 항상 실패**하고 있었다(FarmML 리포에서는 `outcomes/`가 곧 루트라 통과). **계약을 지키려고 만든 테스트가 정작 지켜야 할 쪽에서 돌지 않고 있었다** — 찾는 대상을 `memory/crop_rules`로 바꿨다.

---

# 10. 측정하지 못한 것 (한계)

1. 🔴 **DB에 접속하지 않았다** — `.env`에 `DATABASE_URL`이 없다. 따라서:
   - `temp_night_min_normal`이 채워졌다는 것은 `climatology_service`의 2026-08-03 주석과 `load_aws_climatology.py`의 존재로 판단한 것이고, **현재 dev DB의 실제 NULL 비율을 세지 않았다.** 확인 명령: `SELECT count(*) FILTER (WHERE temp_night_min_normal IS NULL), count(*) FROM weather_climatology;`
   - **`0036`·`0037`의 `alembic upgrade`/`downgrade` 왕복을 돌리지 못했다.** 마이그레이션 파일은 문법 검사와 계약 테스트(상수 대조)만 통과한 상태다 — 실 적재 검증은 **DB 있는 환경에서 반드시 해야 한다.** `0035`의 실 적용도 여전히 미검증이다(PR #104의 요청이 이행됐는지 확인하지 못했다).
   - 야간 산포도를 산출하지 못한 것도 같은 이유다(§8-3).
2. **`0037`이 실제로 몇 행을 바꾸는지 세지 않았다.** 신설 4행·정정 1행은 SQL 상수로는 확정이나, 감자 pH `facility` 행 `UPDATE`가 `0035`+`0036`을 거친 실 DB에서 정확히 1행을 잡는지는 DB에서 확인해야 한다.
3. **백엔드 국가 3단 총점의 실 데이터 영향을 재지 않았다.** §9-5의 전후 표는 `outcomes/` 미러(150지역)의 **결측 처리 개정분만**이다. 백엔드 총점이 가중평균 → `min(토양, 기후)`로 바뀐 효과는 단위 테스트로만 검증했다 — 실 밭 데이터에서 «어제까지 A였던 밭이 오늘 C로 보이는» 폭을 세지 못했다. PR #104가 남긴 그 질문은 **그대로 열려 있다.**
4. **상추·감자 야간의 0점 붕괴는 구조에서 나온 추론이고 실측이 아니다.** 하한 부재·`risk_width` NULL 두 조건만 코드로 확인했다. 실제로 몇 지역·몇 달에서 발생하는지는 야간 평년값이 있는 DB에서 세야 한다. **이번 브랜치는 그 붕괴를 고치지 않았다** — §8-4 ⓐ(`allowed_min` 휴리스틱)는 권고까지다.
5. **`drainage`의 존재를 «없다»로 확정하지 못했다.** 부를 수 있는 2개 API에는 확실히 없으나, 27종 상세정보(15144225)는 **경로를 못 찾아 응답을 보지 못했다.** 명세서가 확보되면 «배수 필드가 실제로 오는가»를 다시 확인해야 한다.
6. `soil_profile_client` 수정은 **호출 1건**(PNU `5115034022100750001`, `5115034022100050000`)으로 검증했다. 전국 필지 커버리지(응답 없는 PNU 비율)는 세지 않았다.
7. **`outcomes/AnswerData.csv`는 갱신하지 않았다** — 문헌 밴드만으로 만들어지고 미러 밴드가 안 바뀌었으므로 낡지 않았다. `imputation_outliers.csv`·`imputation_validation.json`도 내용이 동일해 변경이 없다(탐지 로직 무변경).
8. **push·PR을 하지 않았다.** `origin/feature/main-logic`(`d54b0de`)도 그대로 남아 있어 다음 사람이 `0032` 충돌을 다시 만날 수 있다 → §7-4의 조치가 아직 안 됐다.

---

## 관련 문서
- [[FinalReport]] — 상위 대기열(문헌 확보 · 코드 미반영 30건)
- [[MainLogicReport]] — `feature/main-logic` 적용 내역·점수 측정치
- [[registry]] — 야간기온·일조·서리 문헌 커버리지 원본
- [[user-decisions]] · [[git-sync-workflow]]
- [[PRD]] §7 · [[DB]] §3.11 · [[CLAUDE-coding]] §12 §18
