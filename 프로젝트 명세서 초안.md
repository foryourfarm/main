# 🌾 귀농 사전 시뮬레이션 게임 프로젝트 명세서 (Architecture & Task Spec)

## 1. 프로젝트 개요
* **목표:** 초보 귀농인이 희망 지역의 실제 기상/토양 데이터를 바탕으로, 시행착오를 게임처럼 미리 경험하며 학습하는 맞춤형 예방 가이드 시뮬레이션.
* **타겟 작물 (5종 제한):** 사과, 배, 오이, 감자, 상추
* **아키텍처 구조:** Client (시뮬레이션 UI/UX 전담) - Server (공공 API 연동 및 환경 적합도 알고리즘 전담)

---

## 2. 프론트엔드 (Client) 업무 명세
**목표:** 복잡한 농업 데이터(pH, EC 등)를 직관적인 게임 UI로 치환하고, 몰입감 있는 PC/웹 시뮬레이션 환경 구축.

### 2.1. 기술 스택
* **Core:** React, TypeScript (웹앱)

### 2.2. 핵심 구현 과제 (Task List)
* **게임 UI/UX 및 데이터 시각화**
  * **전문 용어의 게임 스탯화:** EC(전기전도도) → '영양분 흡수율', 토양 산도(pH) → '땅의 피로도' 등 지표별로 치환. 유효인산, 유기물함량, 토성 등 나머지 지표도 각각 게임 스탯 은유로 매핑 필요(백엔드 지표 확정 후 매핑표 작성).
  * **생육 적합도 대시보드:** 백엔드에서 넘겨받은 '생육 적합도 점수'를 게임 내 평가 등급(S/A/B/C) 등으로 시각화.
  * 2D 타일 또는 카드 뷰 형태로 희망 지역과 작물을 세팅하는 로비(Lobby) 화면 구현.
* **LLM 멘토링 인터페이스**
  * 분석 결과 및 행동 지침을 NPC 촌장 등이 대화하는 형태의 UI(말풍선, 타이핑 효과)로 출력.
  * 기능 범위: (1) 지역↔작물 추천 — 지역 입력 시 5종 중 작물 추천 / 작물 입력 시 지역 추천 중 **한 방향만 구현**(메인 디벨로퍼 판단, 방향 미정), (2) 선택된 지역의 토양 특성 설명, (3) 1주 단위 게임 진행에 맞춰 해당 주 시작 시 행동 추천.
  * 모델(로컬 LLM 등)은 미정이지만 FE는 "적합도 점수 + 위험 요소 → 안내 텍스트" 인터페이스 계약만 먼저 확정해 구현(모델 확정 대기하지 않음).

---

## 3. 백엔드 (Server) 업무 명세
**목표:** 5대 평가 지표를 반영한 강력한 '환경 적합도 알고리즘' 구축 및 안정적인 공공 데이터 파이프라인 확보.

### 3.1. 기술 스택
* **Core:** Spring Boot, Spring Data JPA, Java
* **Database:** PostgreSQL (GCP Cloud SQL)
* **Infra:** GCP Cloud Run
* **AI 연동:** 미정 (TBD) - DeepSeek 등 로컬 LLM 도입 가능성 검토 중

### 3.2. 핵심 구현 과제 (Task List)
* **데이터 사전 적재 파이프라인** (실시간 호출이 아닌 배치 방식)
  * **지역 입력 단위:** 시/군 단위. 기상청 API는 행정구역명이 아닌 격자좌표(nx, ny) 기준이므로 시/군-격자좌표 매핑 테이블을 사전 구축. 토양 데이터는 시/군 단위 평균이라 필지별 편차는 반영 못 함(한계로 UI에 명시).
  * **토양 데이터 (농진청 흙토람 등):** 대상 시/군의 토성, pH, EC 등을 미리 수집해 DB에 적재. (착수 전 흙토람 공식 API 문서/발급 절차 존재 여부 확인 필요 — 아직 API 키 미발급, 기획 단계)
  * **기상 데이터 (기상청 API):** 최근 1년치 실측 데이터를 CSV로 미리 받아 DB에 적재.
  * **예보/위험신호 처리 — MVP 범위:** 외부 실시간 호출 없이, 작년 1년치 CSV만으로 진행. "해당 주 행동 추천"의 위험 신호는 예보가 아니라 **평년(작년 동일 주차) 대비 이탈**로 근사.
    * **한계 인지:** 이건 예보가 아니라 히스토리 리플레이라, 올해 실제로 발생하는 이상기후(작년엔 없었지만 올해 발생하는 이상저온 등)는 못 잡음 — A씨 사례의 핵심 문제(표준값과 실제 환경의 괴리)를 "작년 값과 올해 실제의 괴리" 형태로 그대로 안고 감. MVP 단계에서는 감수하고 진행, 이후 단계에서 보완.
  * **[v2 이후 과제] 실제 예보 연동:** 위 한계를 보완하려면 최소 단기예보(3일) API를 주 시작 시점에 1회 호출하는 하이브리드가 필요. 검토했던 방안:
    1. 과거 예보 아카이브 확보 → 기상청이 "발표 시점 예보값" 자체는 보통 공개하지 않아 확보 어려울 가능성 높음.
    2. n년 데이터로 자체 예보 모델 학습 → 프로토타입 스코프 초과, 비권장.
    3. 예보 정확도 변수 도입(과거 예보-실측 일치율 분석 후 신뢰도 멘트 출력) → 1번(과거 예보값)이 있어야 성립하는 후속 아이디어.
    4. (제안) 매일 배치로 기상청 예보를 크론 캐싱 → 짧은 주기 배치라 기존 "사전 적재" 철학과 일치.
    * 이 단계에서 추가 확인 필요: 게임 내 1주가 실제 캘린더 1주와 연동되는지, 아니면 한 세션에서 여러 주가 압축 진행되는지 — 후자면 예보 지원 범위(~10일)를 벗어나는 주차는 다시 평년 통계로 대체.
* **[가장 중요] 환경 적합도 산출 룰 엔진(알고리즘) 개발**
  * 아래 5가지 핵심 요구사항을 반드시 반영하여 점수화 로직 구현:
    1. **가중치 설계:** 기온, pH, EC 등에 작물 생육 영향도를 다르게 부여.
    2. **범위 밖 정도 (Deviation):** 표준 범위 이탈 정도에 따른 감점 폭 차등화.
    3. **작물별 민감도:** 5종 작물별 고유 환경 요구 조건 세팅.
    4. **시계열/예측 반영:** 단순 현재 기온이 아닌, 예보 기반의 위험 신호 감지 로직.
    5. **예외 처리 (Fallback):** API 결측치나 이상치 발생 시 대체하는 방어 로직.
* **데이터베이스 및 ERD 설계**
  * **생육 지침 데이터 직접 제작:** 농사로 매뉴얼은 정형 API가 아닌 서술형 PDF라 그대로 못 씀 — 5종 한정이니 수작업으로 구조화 추출.
  * **데이터 구성:** 단순 적정범위(min/max) 하나로는 요구사항(이탈도 차등, 가중치, 예보 위험신호)을 못 채움. 지표별로 다음을 포함해야 함:
    - 적정 / 허용 / 위험 3단계 구간 (이탈도 차등 감점의 기준)
    - 지표·작물별 가중치 (매뉴얼엔 정량값이 없을 가능성 높아 직접 설정 필요)
    - **야간 최저기온을 주간 평균기온과 분리한 별도 지표** (A씨 사례의 핵심 실패 원인)
  * **저장 형태:** 5종·저빈도 변경 데이터라 DB 테이블 + 관리자 CRUD까지는 불필요. 작물별 JSON/YAML 시드 파일로 레포에 관리하고 마이그레이션 시 DB에 적재하는 정도로 충분.
* **LLM 리포트 생성 통합 (TBD)**
  * AI 도입 방식(API 방식 또는 로컬 인퍼런스) 확정 후, 적합도 점수와 위험 요소를 Prompt로 가공하여 텍스트를 응답받는 로직 추가 예정.