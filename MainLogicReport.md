# MainLogicReport.md — FinalReport 결정 30건 적용 결과

#report #main-logic

> **작성일**: 2026-08-04 · **브랜치**: `feature/main-logic` (`origin/dev`에서 분기)
> **입력**: `FinalReport.md`(결정 대기열 30건) + 2026-08-04 사용자 판단
> **검증**: backend 472 passed · outcomes 17 passed · 마이그레이션 왕복(폐기용 DB) 통과

이 문서는 **무엇을 했고, 무엇을 안 했고, 그래서 점수가 어떻게 달라졌는지**만 적는다.
근거 전문은 `knowledge-base/registry.md` §5, 값의 원본은 각 마이그레이션·JSON의 `source_ref`에 있다.

---

## 0. 30초 요약

| | |
|---|---|
| 적용 | **13건** (밴드 5 · 스키마 3 · 채점 3 · 출처 2) |
| 무변경 확인 | **7건** — 이미 적용돼 있었거나 현행 유지가 결정 |
| 보류 | **9건** (사용자 지정) |
| 실행 불가 | **1건**(§1-14) — 입력 데이터·근거가 없다 |

🔴 **가장 큰 변화는 총점 정의다.** 가중평균 → **최대저해인자법(MLCM, 구성 인자의 최솟값)**.
150지역 평균이 **사과 58.6→24.9 / 배 77.7→18.9 / 오이 80.4→16.2 / 감자 81.6→15.0 / 상추 75.7→6.6**
으로 내려갔다. 회귀가 아니라 정의가 바뀐 것이지만, **이대로면 거의 전 지역이 C등급**이다 —
§6에 원인 분해와 완화안 3개를 정리했다. **판단이 필요한 유일한 항목이다.**

🔵 **가장 잘 맞은 변화는 감자 pH다.** 노지 밴드(5.5~7.0)로 바꾸자 전국 평균 점수가
**99.6점** — 우리 실측 토양과 거의 완전히 겹친다. 반대로 **상추 pH는 15.0점**으로,
시설 기준(6.5~7.0)이 노지 실측(중앙값 5.9)과 거의 겹치지 않는다는 §1-1 문제가
처음으로 **숫자로** 드러났다.

---

## 1. 사용자 재확인 3건 — 답변끼리 충돌하거나 전제가 틀렸던 것

| # | 무엇이 문제였나 | 확정 |
|---|---|---|
| 감자 pH | §2-4 ⓐ는 `5.5~6.2`(시설), §1-1은 "감자 pH는 노지"=`5.5~7.0`. **직접 충돌** | **두 행 다 적재하고 노지로 채점** |
| 사과 기온 | 🔴 **FinalReport §1-4의 출처 서술이 코드와 달랐다.** 현행 `14.5~18.5`는 Kim 2016 Table 3이 아니라 **arccas 재배적지 「적지」**이고, `13.5~19.5`는 휴리스틱이 아니라 **「가능지」 문헌값**이었다 | optimal만 `18~28`, `allowed_min 13.5` 유지 |
| 1-9 경계점수 | 성격 무관 일괄 60점 | **`literature_limit`만 0점** |

---

## 2. 적용한 것 (13건)

### 2-1. 밴드 값 5건

| 지표 | 종전 → 신 | 근거 | 결정 |
|---|---|---|---|
| 감자 `ph` | (시설 5.0~6.0 단일) → **노지 `5.5~7.0` + 시설 `5.5~6.2` 2행** | 처방 2010판 p.49 「감자(노지재배)」 / 처방 5차 p83 | §1-1+§2-4ⓐ |
| 감자 `organic` | `30~47` → **`20~30`** | 처방 5차 표 + 2010판 노지 행 **양쪽 일치** | §1-6ⓐ |
| 사과 `k` | `0.6~0.9` → **`0.30~0.60`** (allowed `0.15~0.75`) | 반증 4건 | §1-2ⓐ+§1-3ⓐ |
| 사과 기온 | optimal `14.5~18.5` → **`18~28`** | 농사로 p.141 생육적온 | §1-4ⓐ |
| 감자 기온 | optimal_max `19.3`→**`22.0`**, allowed_max `30`→**`27.0`** | Kim & Lee 2016 노지 2개년 | §1-1 |

⭐ **조사 중 확인된 것 — §1-1의 성격이 바뀐다.** `raw/papers/common/rda-fertilizer-prescription-revised-2010.md`
(A)표의 제목이 **「오이(노지·시설재배)」·「상추(노지·시설재배)」**다. 국가 표준 자신이 두 작물에
**재배형 공통 밴드**를 쓰고, 값도 현행과 전부 일치한다. 종전에 *"노지 값이 없어서 시설 값을 쓰는 중"*
이라 적었던 것은 **"국가 자료가 둘을 구분하지 않는다"**로 정정된다.
감자만 노지 행이 따로 있고, 그중 현행과 갈리는 칸은 **pH·유기물 둘뿐**이다(P·K·Ca·Mg·EC 5칸은 이미 동일).

### 2-2. 스키마 3건 — `crop_growth_guide` 신규 컬럼 (`0032`)

| 컬럼 | 값 | 결정 |
|---|---|---|
| `cultivation_type` | `open_field` \| `facility` | §2-1ⓐ |
| `allowed_min_kind` / `allowed_max_kind` | `heuristic` \| `literature_limit` \| `literature_threshold` \| `cultivable_range` \| `derived` \| `unverified` \| `not_applicable` | §2-2ⓐ |
| `method` | `1to5_h2o` \| `tyurin` \| `lancaster` \| `nh4oac` \| `unknown` | §2-3ⓐ |

🔴 **FinalReport에서 두 가지를 바꿨다.**
1. **`allowed_kind`를 방향별 2컬럼으로 쪼갰다.** 실제 데이터가 방향별로 갈린다 — 사과 착과기 기온은
   하한 `15.0`이 ±50% 휴리스틱이고 상한 `31`은 출처가 특정되지 않는다. 한 컬럼이면 **둘 중 하나를
   반드시 거짓 표기**하게 된다.
2. **성격을 4종에서 7종으로 늘렸다.** 실제 데이터에 4종으로 담기지 않는 것이 있었다 — 상추 EC `2.9`는
   국내 실측이되 **수량 20% 감소점**이지 붕괴점이 아니고(`literature_threshold`), EC·경사·자갈의 하한은
   **경계 개념 자체가 없다**(`not_applicable`). 근거를 특정 못 한 값은 `heuristic`이 아니라
   `unverified`다 — *"±50%로 만들었다"*와 *"어디서 왔는지 모른다"*는 다른 말이다.

**재배형 백필은 일괄 `facility`가 아니다.** 실제로 시설 기준인 것은 **오이·감자·상추의 화학성
7지표뿐**이고, 사과·배 토양 행은 처방서의 **과수원** 표, 기온·강수 행은 전부 노지 기준이다.
일괄로 칠하면 새 컬럼이 첫날부터 거짓말을 한다.

### 2-3. 채점 3건

| 무엇 | 어떻게 |
|---|---|
| 재배형 선택 (§1-1) | `load_guides()`·`_guides_for_stage()`가 같은 (단계, 지표)에 두 행이 있으면 **`open_field` 우선**. 실제로 갈리는 것은 감자 pH 하나 |
| 경계 점수 (§1-9·§2-2) | 경계 점수를 **그 방향의 성격**이 정한다. `literature_limit`만 0점 |
| 총점 (§1-10ⓐ) | **MLCM**(구성 인자의 최솟값)이 주 총점. 종전 가중평균은 `score_weighted_mean`, 무엇이 끌어내렸는지는 `limiting_indicator`로 병기 |

**`literature_limit`이 실제로 붙은 곳은 3행**이다 — 상추 기온 `2.5·36℃`(spring·fall 2행,
발아 한계·복합장해), 오이 기온 `5·35℃`(*"35℃ 이상/5℃ 이하 시 생육 중지"*). 감자 `27℃`(수량 0)는
outcomes 앵커 쪽에 붙는다. **생장이 완전히 멈추는 온도가 지금까지 B등급 하한(60점)을 받고 있었다.**

### 2-4. 출처 정정 2건 (§1-15) — 값 무변경

- 감자 야간 `28℃`: *"Zhang 2024"* → 그 논문 **Discussion의 2차 인용[24]**임을 밝히고 국내 1차 자료
  (농사로 p.34 「덩이줄기 비대 정지 27~30℃」)로 교체. 🔴 **단, 그 자료는 주야 구분이 없다** —
  시드는 「야간」이므로 성격이 같다고 단정하지 않고 `unverified`로 둔다.
- 감자 냉해 `-3℃`: *"Stegner 2019"*의 **2차 인용[25]**임을 명기. 교체할 1차 자료가 없다.
  🔴 원문상 잎은 결빙 후 **10~78분**만 견디고 *"2차 결빙은 확률적 사건"*이라 **단일 임계로는 판정 불가**다.

---

## 3. 무변경 확인 7건 — 이미 그 상태였거나 현행 유지가 결정

| # | 결정 | 확인 결과 |
|---|---|---|
| §1-5ⓐ·§3-4ⓐ | 오이 pH 재배치 | ✅ **`0031`에서 이미 적용 완료**(optimal 6.0~6.5 / allowed 5.5~6.8). 휴리스틱 7.45는 이미 사라졌다 |
| §1-16ⓐ | 상추 pH 현행 유지 | 값 유지. 🔴 다만 §6에서 보듯 **전국 평균 15.0점**으로 실측과 거의 안 겹친다 |
| §1-11ⓒ | `weight` 현행 유지 | 값 유지. 단 **MLCM은 가중치를 쓰지 않는다** — `score_weighted_mean` 산출에만 남는다 |
| §3-3ⓐ | 사과 `ph.allowed` 값 유지 | 값 유지 + `allowed_*_kind='heuristic'`으로 성격 표기 |
| §3-2ⓑ | ±50% 휴리스틱 유지 | 값 유지 + `heuristic` 표기. 문헌으로 대체된 것은 사과 K뿐이라 그것만 함께 옮겼다 |
| §3-5ⓐ | 사과 `temp_max 31℃` 강등 유지 | 값 유지 + `allowed_max_kind='unverified'`(출처가 루프 라벨뿐) |
| §1-12 | 강수 무조치 | ✅ 사용자 판단대로 문제 아님. `rainfall_daily`는 장기 탭에서 이미 제외돼 있고 `weights.precipitation=0`이다 |

---

## 4. 보류 9건 (사용자 지정)

`§1-8`(상추 채점월) · `§1-13`(감자 강수 작형) · `§1-18`(방법론·표기 임시값) ·
`§2-5`(soil_change_rule) · `§2-6`(tree_age) · `§2-7`(지표 신설 4종) · `§2-8`(제주 화산회토) ·
`§2-9`(UI 표기 3건) · `§3-1`(「문헌 기반 점수」 표기 — 삭제하지 않음)

---

## 5. 실행 불가 1건 — §1-14 (사과·배 토성 순위 분리)

**결론: 할 수 있는 부분은 이미 돼 있고, 남은 부분은 입력이 없다.**

| 항목 | 상태 |
|---|---|
| 경사 | ✅ **이미 작물별 분리됨** — 사과 `0~15%`, 나머지 `0~7%` |
| 자갈 | ✅ **이미 감자만** 채점 — 2010판도 자갈 열이 감자에만 있다 |
| 지형 (사과 하상지~산록경사지 / 배 곡간지~대지) | 🔴 **입력 컬럼이 없다** |
| 배수성 (사과 양호 / 배 양호~매우양호) | 🔴 **입력 컬럼이 없다** → §7 참고 |
| 토성 순위 | 🔴 **근거가 충돌한다** |

🔴 **토성 — 두 국가 자료가 반대다.** 심교문 2016 적지 배점표는 사과 「사양질·미사사양질」,
배 「식양질·미사식양질」로 순위를 반대로 두는데, 처방 2010판 (D)표는 **사과·배 둘 다
「사양토~식양토」**로 같다. 2026-08-03의 `texture_not_scored` 결정(코드 순서의 어디부터
어디까지인지 확정 불가)을 유지한다.

🔴 **판본 충돌 1건 추가 발견**: 배 경사를 **처방 5차는 `0~7%`, 2010판은 `<15%`**로 적는다.
같은 기관 두 판이 어긋나므로 무변경(현행 `0~7%`)으로 두고 기록만 한다.

---

## 6. 🔴 점수가 어떻게 달라졌나 — 판단이 필요한 유일한 항목

### 6-1. 총점 (150지역, `outcomes/RegionalScore.csv`)

| 작물 | 종전 가중평균 | **신 MLCM** | 등급 분포 (종전 → 신) | MLCM 0점 |
|---|---|---|---|---|
| 사과 | 58.6 | **24.9** | S1/A20/B34/C95 → S3/A9/B3/**C135** | 20/150 |
| 배 | 77.7 | **18.9** | S26/A71/B42/C11 → S2/A6/B1/**C141** | 37/150 |
| 오이 | 80.4 | **16.2** | S5/A112/B33/C0 → S0/A2/B0/**C148** | 35/150 |
| 감자 | 81.6 | **15.0** | S27/A97/B19/C7 → S1/A5/B1/**C143** | 55/150 |
| 상추 | 75.7 | **6.6** | S2/A81/B64/C3 → S0/A1/B1/**C148** | 69/150 |

**이대로면 서비스가 전 지역을 C로 판정한다.** MLCM 자체는 사용자 결정이고 근거도 4건으로
단단하지만, **우리 구현의 근사 2가지가 결과를 과하게 만들고 있다**:

1. **원문 MLCM은 등급(S1~N1) 공간의 min인데 우리는 연속 점수의 min이다.** 등급이면 "가장 나쁜
   등급"이 S2/S3 수준에서 멈추는데, 연속 점수는 로그 감쇠 꼬리를 그대로 물려받아 0에 붙는다.
2. **지표가 8개다**(화학 6~7 + 물리 1~2 + 기온). 국가 적지평가는 3~5인자다. 인자가 많을수록
   min은 구조적으로 낮아진다.

### 6-2. 무엇이 총점을 끌어내리는가 (제한요인 상위 3)

| 작물 | 제한요인 |
|---|---|
| 사과 | 유효인산 57 · K 29 · pH 22 |
| 배 | 유효인산 43 · Ca 35 · **경사 25** |
| 오이 | Ca 47 · 유효인산 38 · K 27 |
| 감자 | Ca 48 · **경사 25** · 유효인산 23 |
| 상추 | **pH 105** · 경사 14 · K 13 |

🔴 **물리성(경사·자갈)이 제한요인인 지역이 감자 42/150, 배 25/150**이다. 그런데 이 지표는
등급코드 3~6단계를 %로 환산한 것이라 **해상도가 화학 지표보다 훨씬 거칠고**, 값도 "등급 내 최악값"
이라는 보수적 대표값이다. 거친 지표가 min을 지배하는 것은 근거의 강도와 맞지 않는다.

### 6-3. 완화안 3개 (권고 순)

| 안 | 효과 | 비용 |
|---|---|---|
| **ⓐ MLCM을 등급 공간에서 적용** — 지표 점수를 S/A/B/C로 먼저 이산화한 뒤 최악 등급 채택 | 원문 정의에 정확히 맞고 근사 ①이 사라진다. 총점이 "가장 나쁜 등급"이 되어 0점 급락이 없어진다 | 총점이 연속값이 아니게 되어 UI·percentile 재설계 |
| **ⓑ 물리성을 min 대상에서 제외**(가중평균에는 유지) | 평균 사과 24.9→27.7 / 배 18.9→24.2 / 감자 15.0→22.6. 0점 지역 감자 55→21 | 근거 약한 지표를 뺀 이유를 설명해야 함 |
| ⓒ percentile 병행 노출 | 절대값이 낮아도 지역 간 순위는 살아난다 | 절대 판정이 여전히 전부 C |

🔵 **우리 권고는 ⓐ+ⓒ**다. ⓐ가 원문 정의이고, 근사를 줄이는 방향이라 새 휴리스틱을 만들지 않는다.

### 6-4. 지표별 전국 평균 점수 — §1-1이 숫자로 드러난 곳

| 작물 | 낮은 순 |
|---|---|
| 사과 | 유효인산 47.6 · K 63.9 · pH 74.5 · Mg 74.8 · 유기물 79.7 · 경사 85.9 · Ca 96.9 · **기온 99.5** |
| 배 | Ca 46.8 · 유효인산 47.6 · K 63.9 · 경사 71.0 · pH 74.5 · Mg 74.8 · 유기물 79.7 · 기온 96.1 |
| 오이 | Ca 46.8 · K 51.3 · 유효인산 59.7 · 경사 71.0 · Mg 74.8 · 기온 86.8 · pH 87.9 · 유기물 91.1 · EC 99.3 |
| 감자 | Ca 38.9 · K 45.1 · 유효인산 56.7 · 경사 71.0 · Mg 74.8 · 자갈 84.3 · 유기물 91.1 · 기온 94.5 · EC 99.3 · **pH 99.6** |
| 상추 | **pH 15.0** · Ca 55.9 · K 57.0 · Mg 63.2 · 경사 71.0 · 유효인산 73.2 · 기온 83.2 · 유기물 91.1 · EC 99.3 |

🔵 **감자 pH 99.6 vs 상추 pH 15.0** — 같은 처방서에서 온 두 pH 밴드인데, 감자는 **노지 행으로
바꾼 뒤** 실측과 거의 완전히 겹치고 상추는 **시설 행 그대로**라 거의 겹치지 않는다.
전국 밭 pH 중앙값은 5.9인데 상추 밴드는 `6.5~7.0`이다. **§1-1의 편향이 처음으로 정량화됐다** —
오이·상추에 노지 화학성 기준이 국가 문헌에 없다는 것은 확인됐지만(§2-1), 상추 pH만큼은
**밴드가 실측 분포 밖에 있다는 사실 자체가 별개의 문제**로 남는다.

### 6-5. 🔴 사과 기온 — 문제가 반대 방향으로 뒤집혔다

| | 종전(arccas 적지 14.5~18.5) | 신(농사로 생육적온 18~28) |
|---|---|---|
| 전국 평균 기온 점수 | **30.9** | **99.4** |
| 변별력 | 있음(0점 32지역) | 🔴 **사실상 없음** — 실측이 전부 밴드 안 |

FinalReport §1-4 ⓐ의 단점 칸이 경고한 *"생육적온을 월평균 기온 적합 구간으로 쓰는 성격 오인"*이
**그대로 일어났다**. 밴드 폭이 10℃라 앵커월 평균(20.63℃)이 넉넉히 들어가고, 결과적으로 사과 기온이
지역을 가르지 못한다. 종전 arccas는 **국가 적지평가의 적지/가능지 2단계가 우리 optimal/allowed에
정확히 대응**하는 구조였다는 점을 덧붙인다(FinalReport의 서술과 달리 Kim 2016 Table 3이 아니었다).

**선택지**: ⓐ 유지(점수 정상화 우선) / ⓑ arccas 복귀(변별력 우선, 점수는 다시 낮아짐) /
ⓒ 두 값을 다른 지표로 분리(생육적온=재배 가능 여부, arccas=적지 등급). 🔵 우리는 **ⓒ**를 권한다 —
두 값이 서로 다른 질문에 답하고 있고, 실제로 이 프로젝트가 5번 겪은 성격 오인의 정확한 해법이다.

---

## 7. §1-7 재안내 — `drainage`는 "단조"가 아니라 **부재**다

사용자 요청("먼저 확인부터")에 대한 답:

- `crop_growth_guide.indicator`에 `drainage` **0건**
- `outcomes/memory/crop_rules/*.json`의 `soil_overrides`/`physical_overrides`에 **0건**
- 입력 데이터(`data/02_soil_physical_modified.csv`)에 **배수등급 컬럼 자체가 없다**
- 리포지토리 전체에서 `drainage`가 나오는 곳은 `chat_service.py`의 한글 동음이의 주석과
  `cucumber.json`·`_shared.json`의 **출처 서술문**뿐이다

→ **U자로 고칠 대상이 없다. 신설 여부의 문제다.**

신설하려면 선행 조건이 둘이다:
1. **입력 확보** — 흙토람 토양 물리성 API에 배수등급이 있는지 확인해야 한다. 있다면
   `region_soil_profile_seed.csv` 경로로 들어올 수 있다.
2. **U자 배점 확정** — 근거는 이미 2건 있다(심교문 2016 · 통합적지 2021: 국가 배점표에서
   **「양호」 20점 > 「매우양호」 15점**). 과배수(사질토 한발 위험)를 잡는 형태다.

🔵 **권고**: §2-7(지표 신설 4종)이 보류 상태이므로 `drainage`도 같은 묶음으로 두고,
**입력 확보 여부 확인만 선행**한다. 배점표는 이미 있으므로 데이터만 오면 바로 만들 수 있다.

---

## 8. §1-17 곡선 형태 제안 (사용자 요청)

**문제**: `LIMING`의 감쇠가 지표별로 달라야 하는데(pH는 7개월 뒤에도 그대로, Ca는 증가분의 52% 소실),
**시계열이 3점·1년뿐이라 지수인지 선형인지 고를 수 없다.**

🔵 **제안: 곡선형을 고르지 말고 「반감기」 1파라미터로 표현한다.**

경남 거창 사과원 실측(패화석 159 kg/10a 1회 살포)에서:

| 지표 | 직전 | 1개월(피크) | 7개월 | 증가분 잔존 |
|---|---|---|---|---|
| pH | 5.6 | 6.8 | 6.8 | **100%** |
| 치환성 Ca | 2.6 | 5.7 | 4.1 | **48%** |

- Ca: 피크(1개월) 이후 6개월 동안 증가분 3.1 → 1.5. 지수 감쇠 가정 시 **반감기 ≈ 5.7개월(174일)**.
  (선형 가정이면 소진까지 11.6개월 — 두 가정의 **7개월 시점 예측이 같으므로** 관측 3점으로는
  구분할 수 없다.)
- pH: 관측 구간에서 감쇠가 없다 → **반감기 = ∞**(또는 `decay_days = NULL`).

**왜 반감기인가**:
1. **관측이 실제로 제약하는 것은 「언제 절반이 되는가」 하나뿐**이다. 지수/선형/계단을 고르는 것은
   관측이 지지하지 않는 정보를 추가하는 것이다(추측 금지).
2. `soil_change_rule.decay_days`가 이미 있다 — **스키마 변경 없이** `(rule, indicator)` 단위로
   쪼개기만 하면 된다.
3. 문헌이 하나 더 오면 파라미터 하나만 갱신된다. 곡선형을 박아 두면 형태 자체를 뒤집어야 한다.

**따라서 §1-17 ⓑ(문자열 명기)에 더해 이것을 권한다**:
`LIMING`을 `(LIMING, ph)` `decay_days = NULL` / `(LIMING, ca)` `decay_days ≈ 174`로 나누고,
`source_ref`에 *"3점·1년 관측이라 곡선형은 미확정 — 반감기 1파라미터로만 표현"*을 명기한다.
현행 논의값 「3년」을 Ca에 그대로 쓰면 **개선 조치의 Ca 효과를 6배 이상 과대평가**한다.

🔴 **자재가 염기 조성을 가른다**: 동량 시용 수확기 **패화석 Ca 4.1/Mg 1.2** vs
**석회고토 Ca 3.4/Mg 2.5**(pH는 둘 다 6.8 동일). 자재 무관 단일 규칙이면 Mg 예측이 틀린다.

---

## 9. §2-5 추천 (사용자 요청) — `soil_change_rule` 7행을 지금 넣을 것인가

🔵 **권고: 지금은 넣지 않는다. 단 「빈 테이블」인 채로 두지도 않는다.**

**넣지 말아야 할 이유 3가지**:
1. **`COMPOST`가 방향을 못 정한다.** 축분퇴비 1.5→2.5톤/10a에서 유기물이 오르는 동시에
   **EC가 3.83→8.61**로 오른다. "개선 조치"로 등록하면 **EC 지표를 스스로 망가뜨리는 추천**이 나간다.
   그런데 그 구간이 바로 표준시비량 구간이다.
2. **`ACIDIFY`·`LIMING`의 계수가 토성 의존**인데 토성이 채점되지 않는다(§5). 저자 자신이
   토성별 완충력 차이를 명시했고, 단일 계수는 사질토에서 과교정을 낸다.
3. **감자 `LIMING`이 역방향**이다(소석회 → pH 6.49 → 상품률 37.0%). 같은 `action_type`이
   작물에 따라 부호가 뒤집히는데, **총수량으로는 안 보인다** — 무너지는 것은 상품률이다.
   지금 채점에 상품률 지표가 없으므로 이 규칙은 **검증할 방법이 없다**.

**대신 지금 할 것 2가지**:
- ⭐ **`GREEN_MANURE`만 먼저 넣는다.** 녹비작물은 유기물↑인데 **EC는 오히려 ↓**(38→52 / 53→62,
  EC 0.4→0.3 / 0.7→0.4)라 방향이 모순되지 않는 유일한 행위다. 투입량·기간이 미기재라
  **방향성만**(`effect_coeff` 부호와 `decay_days = NULL`) 넣고 정량 추천은 하지 않는다.
- **나머지 6행은 「등록하되 비활성」**으로 둔다 — 값 없이 `source_ref`만 채운 행을 만들어
  *"이 조치는 알고 있으나 계수를 줄 근거가 없다"*를 테이블 자체가 말하게 한다. 빈 테이블은
  "아무도 조사하지 않았다"로 읽힌다.

---

## 10. 알려진 한계 · 후속

| # | 한계 |
|---|---|
| 1 | 🔴 **MLCM 총점이 실용 범위를 벗어났다**(§6). 완화안 ⓐ+ⓒ 판단 대기 |
| 2 | 🔴 **사과 기온이 변별력을 잃었다**(§6-5). ⓒ(지표 분리) 판단 대기 |
| 3 | 🔴 **상추 pH 15.0점** — 시설 밴드가 노지 실측 분포 밖이다. §1-1의 남은 조각 |
| 4 | 🔴 **감자 기온 밴드 교체가 점수를 전혀 바꾸지 않았다**(94.08 → 94.08). 앵커월 3~6월 평균이 전 지역 19.3℃ 미만이라 상한을 넓혀도 걸리는 지역이 없다 — 값은 더 정확해졌지만 **효과는 0**이다 |
| 5 | 🔴 흙토람 `elcd`의 1:5 환산 여부 미확인 → `method='unknown'`. EC 단독 판정 금지 조건이 계속 유효 |
| 6 | 🟠 `outcomes/scripts/ml/build_regional_score.py`는 **이 리포지토리에서 그대로 실행되지 않는다** — `ROOT = parents[2]`가 FarmML 레이아웃(`<root>/scripts/ml/`)을 전제해 `outcomes/data/`를 찾는다. 이번 재생성은 FarmML 모양의 임시 디렉터리를 만들어 돌렸다. 기존 결함이며 이번에 고치지 않았다 |
| 7 | 🟠 `pandas`·`scikit-learn`을 `backend/.venv`에 설치했다(`requirements.txt` 미반영) — outcomes 스크립트 실행용이며 백엔드는 쓰지 않는다 |

---

## 11. 커밋

| 커밋 | 내용 |
|---|---|
| `95d3785` | FarmML 단측 밴드 테스트 8줄 회수 — 이관 때 유실된 검증 |
| `9db666b` | `0032` 출처 성격 컬럼 4개 신설 |
| `b692188` | `0033` 감자 노지 pH 행 + 처방서 밴드 3건 |
| `608bf3f` | 재배형 선택 · 성격별 경계점수 · MLCM 총점 |
| `bc5c859` | outcomes 미러 + 산출물 재생성 |
| `c66d81e` | 계약·단위 테스트 + `0032` 단계 매칭 결함 수정 |

**검증**: backend `472 passed` · outcomes `17 passed` ·
마이그레이션 왕복(`upgrade head → downgrade 0031 → upgrade head`)을 **폐기용 DB**에서 통과.
개발 DB(`localhost/foryourfarm`, rev 0018)는 건드리지 않았다.

🔴 **`git pull` 사전 대조 결과**: FarmML `SangUk-for_upload`와 `outcomes/`를 전수 비교해
갈리는 파일은 2개뿐이었다 — `apple.json`(origin/dev가 최신, 가져오지 않음)과
`test_crop_literature_anchor.py`(**FarmML에만 있던 단측 밴드 assertion 8줄**, 회수함).

---

## 관련 문서
- `FinalReport.md` — 이 작업의 입력(결정 대기열 30건)
- `knowledge-base/registry.md` §5 — 판정 근거 전문
- `outcomes/README.md` — 두 구현의 점수가 갈리면 안 되는 계약
- `docs/research-base` 브랜치의 `user-decisions.md` — 결정 이력(이 브랜치에는 없다)
- `docs/research-base` 브랜치의 `wiki/reports/cultivation-type-score-impact.md` —
  §1-1 노지/시설 실측 측정(흙토람 API 893콜·77,003 표본)
