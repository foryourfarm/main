# PRD.md — 제품 요구사항 명세서

> 귀농 사전 시뮬레이션 게임 (가칭: For Your Farm)
> 본 문서는 제품의 기획·기능·규칙을 정의한다. 구현 세부(스키마 등)는 `DB.md`, 개발 규칙은 `CLAUDE.md` 참조.

---

## 1. 제품 개요

### 1.1 배경
서울에서 귀농한 초보 농업인 A씨는 "수박 25~30°C, 물 관리 중요" 같은 전국 표준 지식을 익혔지만, 정착지(전북 고창) 특유의 토양(황토·산도)과 봄철 야간 저온을 고려하지 못해 초기 활착에 실패했다. 초보 귀농인에게 필요한 건 박제된 매뉴얼이 아니라 **"내 땅과 날씨에 맞는 맞춤형 예방 가이드"**다.

### 1.2 제품 정의
재배 희망 지역과 작물을 고르면, 실제 공공데이터(토양·기상) 기반으로 재배 한 시즌을 **턴제(주 단위) 관리 게임**처럼 미리 체험한다. 유저는 시행착오의 결과를 데이터와 AI 멘토링으로 학습하고, 실제 재배 전에 위험 요소를 선제적으로 이해한다.

### 1.3 목표 / 비목표
- **목표**: 공공데이터 기반 환경 적합도 분석 + 눈높이 자연어 가이드를 게임 형식으로 전달.
- **비목표**: 실제 농사 시뮬레이터 수준의 물리 모델, 멀티플레이, 실시간 기상 예보 연동, 성공/실패 승패 판정.

### 1.4 타겟 사용자
귀농/귀촌을 준비 중이거나 갓 시작한 초보 농업인. 농업 전문 용어에 익숙하지 않은 사람.

### 1.5 대상 작물 (5종 고정)
사과, 배, 오이, 감자, 상추. 확장은 향후 계획.
- **작물 유형 구분**: 나무형(사과, 배) / 밭작물형(오이, 감자, 상추) — 재배 주기가 근본적으로 달라 게임 내 취급이 다르다(§15).

---

## 2. 게임 컨셉

- **장르**: 클릭 선택형 턴제 관리 시뮬레이션 (풋볼매니저의 "다음으로" 진행 방식 차용).
- **뷰**: 탑뷰(top-down) 도트 그래픽. 여러 밭(지역)을 한 화면에서 관리.
- **진행 단위**: "주(week)". 유저가 버튼을 누르면 즉시 다음 주로 넘어간다(실제 시간과 무관).
- **핵심 재미**: 내가 고른 땅/작물이 실제 데이터상 얼마나 맞는지, 어느 주차에 어떤 위험이 오는지를 등급·게이지·촌장(NPC) 멘토링으로 확인하는 것.
- **승패 없음**: 실패 판정/RNG 없이 순수 정보 제공. "잘못 골랐다"가 아니라 "이 조건에선 이런 위험이 있으니 이렇게 대비하라"를 알려준다.

---

## 3. 플레이 루프

```
[로그인]
   ↓
[Lobby] 탑뷰에서 관리할 밭(지역) 선택 — 지정된 밭 전부 오픈
   ↓
[지역 상세] 밭 클릭 → 라디얼 UI 펼침
   · 지역 특성 확인 · 작물 궁합 · 이번 주 행동 · 촌장에게 묻기
   ↓
[주간 진행] 행동카드 확인(LLM 추천 포함) → "다음 주" 클릭 → 주차 +1, 계절/시간 변화 반영
   ↓
[결과 리포트] 해당 주 적합도 변화 + 위험신호 + 촌장(LLM) 멘토링
   ↓  (반복: 시즌 끝까지 원하는 만큼 클릭으로 진행)
[재배 기록] 진행한 지역-작물 조합과 등급을 모아보기
```

---

## 4. 페이지별 상세 명세

### 4.1 Login
- 계정 로그인/회원가입. 로그인 필수.
- 비로그인 상태로 게임 페이지 접근 시 `/login`으로 리다이렉트.
- 로그인 성공 시 마지막 세이브가 있으면 이어하기, 없으면 Lobby 신규 시작.

### 4.2 Lobby (Map / 탑뷰)
- 도트 그래픽 탑뷰. 화면에 여러 밭 타일 배치.
- **지역**은 프로젝트에서 지정한 곳 그대로 고정, **밭은 전부 오픈**(잠금/해금 없음).
- **밭 = (지역, 작물) 조합 단위**. 한 지역 안에 여러 작물을 동시에 재배할 수 있으므로, 같은 지역에 밭이 여러 개 있을 수 있다(예: 청송에 사과밭 + 상추밭 동시 존재).
- 타일 표시: 지역 단위로 묶어서 보여주되, 그 안에 심어진 작물이 여러 개면 서브 타일/스택으로 구분 표시(개별 밭마다 작물 스프라이트 + 적합도 등급).
- 현재 주차/계절이 배경·타일 비주얼에 반영.
- 타일(밭) 클릭 → 해당 밭의 상세(라디얼) 오픈.

### 4.3 밭 상세 (Encounter → 라디얼 UI)
- 클릭한 밭 위치에서 원형 메뉴가 펼쳐진다.
- 중앙 메달리온: 지역·작물·적합도 점수/등급 (이 밭 = 이 지역의 이 작물 기준).
- 위성 액션 4종:
  1. **지역 특성** — 토양(pH/EC/유효인산 등)·기후 요약을 게임 스탯 게이지로. 같은 지역의 다른 밭과 토양/기상 실측값은 동일(지역 단위 데이터), 생육 지침만 작물별로 다름.
  2. **작물 궁합** — 이 지역에서 5종 작물의 적합도 비교(또는 이 작물의 지역 비교 — 방향 택1). 아직 안 심은 작물도 "이 지역에 심으면 몇 점"으로 미리 볼 수 있음.
  3. **행동카드** — 해당 주차에 가능한 행동카드를 전부 제시(예: 물주기, 병충해 관리, 수확 등).
     - **주간 날씨 기반 행동 추천 (필수)**: 그 주의 위험신호(§9, 평년 데이터 기반)를 LLM이 해석해 "이런 위험이 있으니 이렇게 하라"는 자연어 코멘트를 반드시 생성한다. 대회 핵심 요구사항(시계열/예측 반영)에 해당 — LLM 실패 시에도 규칙 기반 폴백 문구로 대체되어 코멘트 자체는 항상 나온다(§10, §13).
     - **행동카드 하이라이트 (옵션)**: 위 코멘트와 별개로, 여러 행동카드 중 어떤 카드가 이번 주 상황에 맞는지 짚어주는 건 부가 기능(가능하면 — 로컬 LLM 여건에 따라 생략 가능, 없어도 카드 목록 자체는 항상 노출).
  4. **촌장에게 묻기** — LLM 멘토링(말풍선·타이핑 효과).
- 배경 클릭/Esc로 닫기.

### 4.4 주간 진행 (Catch 대체)
- "다음 주" 버튼 하나로 주차 +1. FM식 즉시 진행.
- **성공/실패 판정 없음** — 유저 행동으로 인한 승패 계산이 없다. 주차가 넘어가면 그 주차의 환경 데이터(평년 기반)로 적합도·위험신호를 다시 산출해 보여줄 뿐.
- 진행 시 계절/시간 변화 연출(§7).
- **밭작물형**은 심은 시점부터 경과 주수에 따라 특정 시점에 수확 행동카드가 새로 노출(§15). **나무형**은 게임 시작부터 이미 성숙 상태라 이 성장추적 자체가 없음.

### 4.5 결과 리포트 (Result)
- 해당 주차 적합도 점수/등급, 지표별 게이지, 위험신호(있으면).
- 촌장(LLM)의 눈높이 자연어 가이드.
- 데이터 근사 한계 고지(예: "작년 동일 시기 기준", "시/군 평균값").

### 4.6 재배 기록 (Pokedex 대체)
- 유저가 진행한 (지역, 작물, 주차) 조합과 등급 이력을 모아보는 화면.
- 시즌 요약: 어느 주차에 어떤 위험이 있었는지 타임라인.

---

## 5. UI 흐름 / 상태 머신

### 5.1 화면 전이
`Login → Lobby ⇄ 지역상세(라디얼) → (주간진행) → Result → Lobby`, 언제든 `재배기록` 열람.

### 5.2 주차 상태 머신
```
IDLE(주 시작 대기)
  → ADVANCING(다음 주 클릭, 데이터 로드/계산)
  → REVIEWING(결과 리포트 표시)
  → IDLE(다음 주 대기)
```
- 시즌 종료 조건: 미리 정한 시즌 길이(작물별 재배 주차) 도달 시 `SEASON_END` → 시즌 요약.
- 어떤 상태에서도 데이터 결측/계산 실패 시 `ERROR_SAFE`로 빠져 안내 문구를 보여주되 진행을 막지 않는다.

---

## 6. 지역 시스템

- **단위**: 시/군. 지정된 지역 고정.
- **좌표 매핑**: 기상 데이터는 격자좌표(nx, ny) 기준이므로 시/군-격자 매핑 테이블 사전 구축(`DB.md`).
- **밭 = (지역, 작물) 슬롯**: 하나의 지역이 여러 밭(작물별 슬롯)을 가질 수 있다 — 지역과 작물은 N:M 관계. 토양·기상 실측값은 지역 단위로 공유되고, 생육 지침(적정범위·가중치)만 작물별로 달라 밭마다 적합도 점수가 따로 계산된다.
- **한계 표기**: 토양은 시/군 평균이라 필지별 편차를 반영하지 못함 → UI에 명시.

---

## 7. 계절 / 시간 변화 시스템

- 주차가 진행될수록 계절이 변한다(봄→여름→가을 등). 이는 데이터(작년 동일 주차 기상)와 비주얼 양쪽에 반영.
- 타일/배경 도트 그래픽이 계절에 따라 전환(색감, 작물 성장 단계).
- 계절 변화는 적합도 계산 입력에도 영향(주차별 기온/강수 등이 달라짐).

---

## 8. 환경 적합도 점수 시스템 ★핵심★

### 8.1 개요
표준 생육 지침과 실제 환경 변수를 비교해 0~100 점수 및 S/A/B/C 등급을 산출한다. 계산은 백엔드 룰 엔진(`DB.md` §계산 로직). 5가지 요구사항 반영:

1. **가중치 설계**: 지표(기온·pH·EC·강수·일조·야간최저기온 등)별로 작물 생육 영향도를 다르게 부여.
2. **범위 밖 정도(Deviation)**: 표준 범위를 살짝 벗어남 vs 크게 벗어남을 차등 감점. → 지표별 적정/허용/위험 3단계 구간 사용.
3. **작물별 민감도**: 5종 작물이 같은 지표에도 다른 요구조건/민감도를 갖도록 시드 데이터로 세팅.
4. **시계열 반영**: 단순 현재값이 아니라 해당 주차 및 인접 주차 추이를 반영(특히 **야간 최저기온**은 주간 평균과 분리한 별도 지표 — A씨 사례 핵심).
5. **예외 처리(Fallback)**: 결측/이상치 시 대체 로직으로 산출이 죽지 않게.

### 8.2 등급 매핑 (초안, 시드로 조정)
- S: 90~100 / A: 75~89 / B: 60~74 / C: 0~59.

### 8.3 게임 스탯 치환 매핑
전문 용어를 직관적 게임 스탯으로 변환(백엔드 지표 확정 후 매핑표 완성):
| 농업 지표 | 게임 스탯(예시) | 표현 |
|---|---|---|
| EC(전기전도도) | 영양분 흡수율 | 게이지 |
| pH(산도) | 땅의 피로도 | 게이지 |
| 유효인산 | (미정) | 게이지 |
| 유기물함량 | (미정) | 게이지 |
| 토성 | (미정) | 아이콘/라벨 |
- **원칙**: 은유가 원래 의미를 왜곡하지 않을 것. 색만으로 구분하지 말고 라벨 병기.

---

## 9. 예보 / 위험신호 처리 ★설계 확정★

- **정식 설계 = 평년(작년 동일 주차) 데이터 기반.** 실시간 기상 예보 API는 사용하지 않는다.
- **이유**: 게임은 유저가 클릭으로 순식간에 여러 주차를 진행할 수 있다(실제 시간과 무관). 기상청 예보는 "오늘로부터 며칠 후"만 가능하므로 이 게임 구조와 근본적으로 맞지 않는다. 따라서 실시간 예보 연동은 v2 임시 계획이 아니라 **폐기**하고, 평년 기반을 정식으로 채택한다.
- **위험신호 산출**: 해당 주차의 작년 실측값(및 인접 주차 추이)에서 작물별 위험 조건(예: 야간 저온 지속, 과습 등)을 감지해 안내.
- **한계 고지(필수)**: 이는 예보가 아니라 "작년 이맘때 통상 이랬다"는 히스토리 기반 근사다. 올해 실제 이상기후는 반영하지 못함을 리포트에 명시.

---

## 10. LLM 멘토링 시스템 (촌장 NPC, 로컬 모델)

- **모델**: 로컬 LLM.
- **역할(스코프 고정)**:
  1. **지역↔작물 추천 (택1)**: 지역 입력 시 5종 중 추천 / 작물 입력 시 지역 추천 — **한 방향만** 구현(메인 디벨로퍼 판단).
  2. **지역 토양 특성 설명**: 선택 지역의 토양을 초보자 눈높이로.
  3. **주간 날씨 기반 행동 추천 (필수)**: 그 주차의 위험신호(§9)를 해석해 대비 행동을 코멘트로 생성. 대회 핵심 요구사항(시계열/예측 반영)에 해당하므로 항상 나와야 하고, LLM 실패 시에도 규칙 기반 폴백 문구로 대체(§13).
  4. **행동카드 하이라이트 (옵션)**: 해당 주차에 노출된 행동카드 중 어떤 게 맞는지 짚어주는 부가 코멘트(§4.3, §15) — 로컬 LLM 여건에 따라 생략 가능.
- **입력**: 백엔드가 만든 정형 분석 결과(점수·위험요소·컨텍스트). LLM은 수치 계산을 하지 않고 자연어로 번역만.
- **UI**: 촌장 NPC 말풍선 + 타이핑 효과.
- **폴백**: LLM 실패 시 규칙 기반 기본 문구. 게임 진행은 멈추지 않는다.

---

## 11. 재배 기록 시스템

- (지역, 작물, 주차, 등급, 위험이벤트) 이력 저장.
- 시즌 타임라인/요약 제공. 수확 이벤트도 함께 타임라인에 표시.

---

## 12. 사운드 / 애니메이션

- **사운드**: 배경 앰비언트(농촌/계절), 클릭·주차진행·라디얼 오픈 효과음. 음소거 옵션.
- **애니메이션**: 라디얼 펼침/닫힘, 주차 진행 시 계절 전환, 게이지 채워짐, 촌장 타이핑. `prefers-reduced-motion` 시 축소.

---

## 13. 예외 처리

- 공공데이터 결측/이상치: 대체값 사용 + "대체됨" 고지, 산출 지속.
- 로컬 LLM 실패: 규칙 기반 폴백 문구.
- 네트워크/백엔드 오류: 안내 후 재시도 가능하게, 세이브 보존.
- 어떤 예외도 게임을 완전히 멈추게 하지 않는다(§상태머신 `ERROR_SAFE`).

---

## 14. 성능 목표

- 주차 진행(다음 주 클릭) 후 결과 표시까지 체감 지연 최소화 — 사전 적재 데이터 + 계산 캐시로 목표 1초 이내.
- 외부 API 동기 호출 0회(유저 경로).
- 도트 렌더 60fps 목표(Canvas, 스프라이트 재사용).

---

## 15. 작물 유형과 재배 주기

- **나무형 (사과, 배)**: 실제로는 결실까지 수년 걸리지만, 그 성장 과정을 시뮬레이션하지 않는다. **게임 시작부터 이미 성숙한 나무 상태**로 취급 — 주차 기반 성장 추적 자체가 없다. 즉시 수확/관리 행동카드 대상.
- **밭작물형 (오이, 감자, 상추)**: 심은 시점(주차)부터 경과 주수에 따라 생육 단계가 진행되고, 특정 시점에 수확 행동카드가 노출된다.
  - 상추: 심은 후 1개월(약 4주) 시점 — 수확("뽑기")
  - 오이: 심은 후 2개월(약 8주) 시점 — 수확
  - 감자: 심은 후 3개월(약 12주) 시점 — 수확
  - (1개월=4주 가정. 정확한 주 수는 확정 필요 — §18)
- 밭(`farm_plot`)마다 심은 시점을 저장하고, 경과 주수 = 세이브의 현재 주차 − 심은 시점으로 계산한다(`DB.md` §3.3). 나무형은 이 계산이 필요 없다.

---

## 16. 경제 시스템 [v2 이후 단계 — MVP 범위 아님]

> **MVP에는 포함하지 않는다.** 아래는 이후 단계에서 추가할 때의 설계 방향만 미리 정리한 것 — 지금 스키마/구현엔 없음(`DB.md` 참고).

- **목적**: 재미 요소로 수지(수입/지출) 정보를 추가하되, **승패 조건이 아니다.** §2의 "성공/실패 판정 없음" 원칙을 그대로 유지 — 최종 수지는 적합도 등급(S/A/B/C)과 별개인 **정보성 지표**로 리포트에 표시할 뿐, 흑자 미달이 게임을 실패로 만들지 않는다.
- **유지비**: 주차가 진행될 때마다 고정 유지비가 자동 차감(−).
- **관광객 방문**: 조건부 수익 이벤트. **랜덤이 아니라** 해당 밭의 적합도 등급·계절 등 게임 상태 조건으로 발생 여부와 금액이 결정된다(결정론 원칙 유지, `CLAUDE.md` §2).
- **재료**: 농사 행동에 쓰이는 재료는 무제한 사용 가능 — 별도 인벤토리/구매 제약 시스템을 만들지 않는다(YAGNI).
- **상점**: [확인 필요] 무엇을 사고파는지, 필요성 자체를 포함해 미정(§18).
- **최종 리포트**: 시즌 종료 시 누적 수지(흑자/적자)를 하나의 정보 지표로 노출.

---

## 17. 향후 확장 계획

- 대상 작물/지역 확장.
- 지역↔작물 추천 반대 방향 추가.
- 게임 스탯 매핑 지표 확대(유효인산·유기물 등 세분화).
- 필지 단위 토양(시군 평균 → 세분화).
- 시즌 다회차/비교 기능.

---

## 18. 미결정 사항

- 흙토람 API 문서/발급 여부.
- 로컬 LLM 구체 모델.
- 지역↔작물 추천 방향 택1.
- 게임 스탯 매핑표(유효인산 등) 확정.
- 밭작물형 정확한 재배 주 수(1개월=4주 가정, 확정 필요).
- (v2) 관광객 방문 조건의 구체 임계값(적합도 등급 몇 이상 등).
- (v2) 유지비·관광객 수익 구체 수치.
- (v2) 상점 시스템 필요 여부 및 내용.
