# PRD.md — 제품 요구사항 명세서

> 귀농인 맞춤형 재배 의사결정 지원 서비스 (가칭: For Your Farm)
> 본 문서는 제품의 기획·기능·규칙을 정의한다. 구현 세부(스키마 등)는 [[DB.md]], 개발 규칙은 [[CLAUDE.md]] 참조.
> **주의:** 초기 "게임형 시뮬레이션" 기획은 폐기되었다. 이 문서가 현재 유효한 유일한 제품 정의다.

---

## 1. 제품 개요

### 1.1 배경
서울에서 귀농한 초보 농업인 A씨는 "수박 25~30°C, 물 관리 중요" 같은 전국 표준 지식을 익혔지만, 정착지 특유의 토양·기후는 매뉴얼과 달라 초기 활착에 실패했다. 초보 귀농인에게 필요한 건 박제된 매뉴얼이 아니라 **"내 땅과 오늘 날씨에 맞는 맞춤형 행동 가이드"**다.

### 1.2 제품 정의
실제로 농사를 짓는(또는 막 시작한) 귀농인이 **자신의 재배 지역과 작물을 등록하면, 매일 실제 기상·토양 데이터를 바탕으로 "오늘/이번 시즌 무엇을 어떻게 해야 하는지"를 자연어로 안내받는 웹 서비스.** 게임이 아니라, 실제 농사 기간 내내 곁에서 함께 가는 실용 도구다.

- **현실 시간 = 서비스 시간.** 오늘은 오늘 날씨로 판단한다. 시간을 압축하거나 건너뛰지 않는다.
- 서비스는 **장기(다가오는 3개월 시즌 가이드)**와 **단기(당일~3일 실시간 대응)** 두 관점을 함께 제공한다.

### 1.3 목표 / 비목표
- **목표**: 공공데이터(기상·토양) 기반으로 (1) 지역×작물의 환경 적합도를 정확히 분석하고, (2) 실시간 예보에 따른 당일 위험·행동을 선제적으로 안내하며, (3) 이를 초보자 눈높이 자연어로 전달한다.
- **비목표**: 실시간 토양 센서 하드웨어, 멀티플레이, 실제 농약·비료 처방의 법적 책임을 지는 정밀 처방(정보 제공 수준까지만).

### 1.4 타겟 사용자
실제 귀농/귀촌을 했거나 준비 중인 초보 농업인. 전문 용어에 익숙하지 않고, "지금 내 땅에서 뭘 해야 하나"를 알고 싶어 한다.

### 1.5 대상 작물 / 지역
- **작물**: 사과, 배, 오이, 감자, 상추 (5종, 확장 여지).
- **지역**: 전국 임의 시/군 지원(사용자가 자기 지역을 직접 등록). 데이터는 요청 시점 기준으로 조회·캐싱한다(§9).

---

## 2. 핵심 차별성 (심사 대응)

같은 문제를 푸는 다른 팀과 구분되는 지점:
1. **정확도** — 적합도/위험도 예측의 정확성이 1순위. 이론치(문헌·표준 지침)와 실제 데이터의 괴리를 좁히는 것 자체가 과제의 핵심이다.
2. **장기/단기 분리** — "이 지역에서 이 작물을 이렇게 키워라"(장기 커리큘럼)와 "오늘 이 날씨엔 이렇게 대응하라"(단기)를 분리해 제공. 예방(장기)과 즉시 대응(단기)을 모두 커버.
3. **LLM 행동추천 품질** — 정형 분석 결과를 초보자가 바로 행동에 옮길 수 있는 자연어로 변환. 로컬 LLM 품질·응답속도가 실제 경쟁력.
4. **UI 가독성** — 전문 수치를 직관적 표현으로, 최대한 읽기 쉽게.

---

## 3. 사용자 플로우

```
[로그인/회원가입]
   ↓
[온보딩] 내 지역(시/군) + 작물 + 파종/정식일 등록 (여러 밭 등록 가능)
   ↓
[대시보드] 등록한 밭별 카드
   ├─ [장기 탭] 이 지역×작물의 3개월 적합도 + 시기별 커리큘럼(예방 가이드)
   └─ [단기 탭] 오늘~3일 실시간 예보 + 위험신호 + 오늘의 추천 행동(LLM)
   ↓
[행동 기록] 내가 한 행위(관수·시비·방제 등) 기록 → 토양 상태 추정에 반영
   ↓
(매일 접속: 예보·추천이 그날 데이터로 갱신, 누적될수록 신뢰도↑)
```

---

## 4. 페이지 / 탭 명세

### 4.1 Login / 회원가입
- 로그인 필수. 유저별 밭·행동 기록·상태를 저장해야 하므로 계정이 필요하다.
- 비로그인 접근 시 로그인으로 리다이렉트.

### 4.2 온보딩 (밭 등록)
- 입력: 지역(**시/군 → 법정동 말단**), 작물(5종 중), 파종/정식 실제 날짜.
- 지역은 전국 시/군 자동완성 선택 후 **해당 시/군의 법정동 말단을 이어서 선택**한다.
  - **말단**이란 리가 있으면 리까지, 없으면 그 윗선(동·읍·면)이다. 전국 18,864개.
  - 말단까지 받는 이유: 흙토람 토양검정이 말단 코드로만 조회되고, 상위 단위 평균은 유저 밭과 무관해진다(§5).
  - 기상·적합도·ML은 여전히 시/군 단위다 — 법정동은 **토양 기준값 확보에만** 쓴다.
  - 시군당 선택지가 중앙값 69개(최대 236개, 상주시)라 드롭다운으로는 못 찾는다 — 검색형 입력으로 받는다.
- 등록 시 시/군의 격자좌표와 **법정동 말단의 토양 기준값**을 확보한다(§9).
- 여러 밭 등록 가능(지역·작물 조합 자유).

### 4.3 대시보드
- 등록한 밭 목록. 각 밭 카드에 현재 적합도 등급, 오늘의 핵심 위험/행동 요약.
- 밭 선택 → 장기/단기 탭 상세.

### 4.4 장기 탭 (시즌 커리큘럼 · 예방)
- 이 지역×작물의 **다가오는 3개월 적합도 분석**(평년 기상 + 토양 기반, §7 알고리즘 재사용).
- 생육 단계/시기별로 "이 시기엔 무엇을 주의하고 무엇을 해야 하는지" 커리큘럼 형태 안내.
- 데이터 한계 명시(평년치·시군 평균 기반, 전망 반영 여부).

**왜 시즌 전체가 아니라 3개월인가** (2026-08-02 변경, 종전 명세는 "시즌 전체 적합도 분석")

평년치를 넘어서는 신호는 **기상청 3개월전망(tercile 확률)뿐**이다. 12개월을 계산하면 그중
9칸은 평년치만으로 낸 값인데, 화면에서는 전망이 반영된 3칸과 **같은 점수·같은 등급으로**
보인다. 근사를 확정값처럼 보이게 하지 않는다는 원칙(`CLAUDE.md` §18-4)에 어긋난다.
범위를 좁히는 것은 정보를 버리는 게 아니라 **신뢰할 수 있는 구간만 남기는 것**이다.

- **창(window)은 달력이 정하고 전망을 그 위에 얹는다.** 오늘이 속한 달부터 3개월
  (`[이번 달, +1, +2]`)이다. 전망은 각 달에 있으면 보정으로 반영되고, 없으면 평년치만 쓴다.
  - **왜 "전망이 커버하는 달"이 아닌가** (2026-08-02 정정, 구현 분석에서 드러남): 3개월전망은
    매월 23일경 **발표월 다음 1~3개월**을 준다. 그래서 전망 창을 그대로 쓰면 매월 23일에
    창이 한 칸 점프해 **지금 농사 중인 이번 달이 화면에서 사라진다**(8/22엔 8·9·10월,
    8/24엔 9·10·11월). 지금 대응해야 하는 달을 빼는 것은 제품 목적에 어긋난다.
  - **이번 달도 전망을 받는다.** 보정 조회가 대상월마다 따로 최신 발표분을 고르므로
    (`load_corrections`), 8월 25일 기준이면 8월은 7/23 발표분에서, 9·10월은 8/23 발표분에서
    온다. 달력 고정으로 바꿔도 전망을 잃지 않는다.
- **연도를 넘어간다.** 11·12월 조회는 내년으로 넘어가므로(11월 → 11·12·1월) 창은 단일 연도로
  가정하지 않는다. 응답은 **칸마다 연도와 전망 발표일**을 갖는다 — 칸마다 사실이 다르기 때문에
  응답 최상위에 하나로 두면 반드시 한쪽이 틀린다.
- **전망이 없거나 낡아도 산출이 죽지 않는다**(§12) — 창이 오늘 기준이라 과거를 보여줄 수 없고,
  전망 결측은 칸별 "전망 미반영"으로 그대로 드러난다. 별도의 낡음 판정 임계값을 두지 않는다.
  실제로 프로덕션에서 전망이 비어 있던 적이 있다.
- **창이 전부 휴면기/제철 아님이어도 3칸을 그대로 보여주고**, 다음 생육기가 언제 시작하는지
  함께 안내한다(최대 12개월 앞까지 탐색). 칸을 임의로 늘려 전망 없는 달을 채우지 않는다.

나머지 9개월치 전망은 만들지 않는다 — 요구가 생기면 그때 붙인다(§2 YAGNI).

### 4.5 단기 탭 (당일 대응)
- 오늘 실황 + 단기예보(최대 3일) 기반.
- **위험신호**: 예보에서 작물 위험 조건(야간 저온, 과습, 고온 등)을 감지.
- **오늘의 추천 행동**: 위험신호 + 현재 토양 추정 상태를 근거로 LLM이 자연어 행동 가이드 생성(§10).
- 매일 예보-실측을 쌓아 **장기적으로** 예보 신뢰도를 캘리브레이션할 근거를 축적(§11). (단, 통계적으로 유의미해지려면 수개월 데이터가 필요 — 짧은 기간에 즉시 정확해진다는 뜻은 아님.)

### 4.6 챗봇 / 상담
- 유저가 자기 상황을 물으면 **(1) 등록 밭 컨텍스트·분석 결과**(직접 주입) + **(2) 큐레이션된 농업 문서 근거**(RAG 검색)를 합쳐 LLM이 답변.
- **RAG 범위**: 내 밭 얘기를 넘어 일반 재배 질문("배 봄철 병충해 대응?")도 받되, **신뢰 문서 코퍼스**에 근거해서만 답한다. 인터넷 오픈 도메인이 아니다.
- **1차 코퍼스 = 농사로 작물별 안내책자**, 그중에서도 **제한 작물 5종(사과·배·오이·감자·상추) 정보만 우선** 적재. 그 외 문서·작물은 이후 확장.
- **정확도 우선 폴백**: 관련 문서를 못 찾으면 지어내지 말고 "해당 정보는 확실치 않으니 전문가 상담 권장"으로 안내(환각 방지 — 정확도가 1순위).
- 스코프: 재배 관련 상담에 한정(자유 잡담 확장 안 함).

### 4.7 기록
- 유저의 행동 로그·위험 이벤트·적합도 변화 타임라인.

---

## 5. 지역·데이터 단위

데이터 성질에 따라 **두 층**으로 나눈다. 무엇을 어느 단위로 쓰는지 혼동하면 정확도가 무너지므로 아래를 기준으로 삼는다.

| 층 | 단위 | 쓰는 곳 | 왜 이 단위인가 |
|---|---|---|---|
| **집계·예측** | **시/군**(`region`, 256개) | 기상 평년치·장기예보·적합도 캐시·ML 학습 | 통계적으로 유의미한 **데이터 총량**이 필요하다. 읍면동으로 쪼개면 표본이 부족해 학습·평년 추정이 불안정해진다. |
| **유저 밭 토양** | **법정동 말단**(10자리 — 리가 있으면 리) | `soil_state` 최초 기준값(흙토람 토양검정) | 시/군 안에서도 토양 편차가 매우 크다(실측 예: 순천시 삼거동 한 곳, 2023년 조사 18개 필지에서 유기물 4~76 g/kg). 읍·면 평균조차 리별 실측과 최대 60점 갈린다(아래). 상위 단위 평균을 쓰면 유저 실제 밭과 무관한 값이 되어 점수의 토양 부분이 무의미해진다. |

- **기상**: 시/군-격자좌표(nx, ny) 매핑으로 조회(전국, `DB.md`).
- **토양**: 흙토람 토양검정 화학성은 **법정동 말단 코드로만 조회된다**. 시/군 코드는 "데이터 없음"이고(2026-07-25 실측), 리를 가진 읍·면도 면 코드로 물으면 데이터가 있어도 301이 온다(부여 장암면 `4476042000` → 301, 점상리 `4476042021` → 100건. 2026-07-31 실측). 따라서 온보딩에서 말단까지 입력받는다(§4.2).
  - **읍·면 평균 vs 리 실측을 실측 비교했다**(각 작물의 경지구분 기준):

    | 면 | 리 수 | 면 평균 점수 | 평균 \|차이\| | 최대 차 | 등급 바뀐 리 |
    |---|---|---|---|---|---|
    | 부여 장암면 (상추) | 6 | 100 S | **34.7점** | **60.0점** | **4/6** |
    | 고창 고수면 (감자) | 14 | 82.1 A | 7.5점 | 43.1점 | 3/14 |
    | 고창 공음면 (오이) | 12 | 100 S | 2.6점 | 11.3점 | 1/12 |

    장암면은 면 평균이 100점 S인데 리 3곳은 40점 C다 — "내 땅"이라 믿고 보는 값이 정반대 신호를 준다. 공음면이 2.6점으로 작은 건 안전해서가 아니라 오이 지침이 pH 하나뿐이라 편차가 점수에 덜 실린 것이다.
  - **토양은 즉각 변하지 않는 근본 성질**이 기준이며, 사용자 행위에 따른 단기 변화는 §8 추정 모델로 보정한다.
  - 같은 법정동에서도 **경지구분(논/밭/시설/과수)에 따라 값이 크게 다르므로** 등록 작물에 맞는 경지구분 표본만 사용한다(사과·배=과수, 감자·상추=밭, 오이=시설).
  - **표본이 없으면 비운다.** 이웃 리를 모아 평균하는 폴백을 뒀다가 제거했다 — 남의 땅 값이 "내 땅 값"으로 표기되기 때문이다(§18-4). 빠진 지표는 채점 커버리지 문구로 드러낸다.
  - 호출량 방어: 조회 결과는 (법정동, 경지구분) 단위로 캐시해 재사용한다(§12). 말단 직접 조회는 이웃 리 5곳을 평균하던 방식보다 **오히려 빠르다**(13.6초 → 3초).
- **한계 표기**: 법정동 표본 평균, 평년치, 이론 추정치임을 UI/리포트에 명시.
  - **EC는 리 단위로도 대표값이 안 선다** — 공음면 용수리 한 곳의 범위(0.47~21.05)가 면 전체 범위(0.14~21.05)를 그대로 담는다. 시설 EC는 개별 하우스 시비 이력이 지배해 행정구역으로 수렴하지 않는다. pH·유기물은 리 단위 이득이 분명하다. **EC 전용 한계 문구가 필요하다**(미착수).

---

## 6. 작물·생육 지침 데이터

- 농사로 등 문헌에서 작물별 표준 생육 지침을 수작업으로 구조화(지표별 적정/허용/위험 구간 + 가중치 + 생육 단계별 요구조건). `DB.md` §crop_growth_guide.
- 야간 최저기온은 주간 평균과 분리한 별도 지표로 유지(A씨 사례 핵심).

---

## 7. 환경 적합도 점수 시스템 ★장기 탭 백본 (기존 재사용)★
표준 생육 지침과 실제 환경 변수를 비교해 0~100 점수·등급을 산출한다. 계산은 백엔드 룰 엔진(결정론, `DB.md` §8). 반영 요구사항:
1. **가중치 설계**: 지표별 작물 생육 영향도 차등.
2. **범위 밖 정도(Deviation)**: 적정/허용/위험 3단계 구간으로 이탈 정도 차등 감점.
3. **작물별 민감도**: 5종 작물이 같은 지표에도 다른 요구조건.
4. **시계열 반영**: 단일 시점이 아니라 생육 단계·인접 기간 추이 반영(야간 최저기온 분리).
5. **예외 처리**: 결측/이상치 시 대체 로직으로 산출이 죽지 않게.

**기상 입력 = 평년치를 장기예보로 보정 (제안, §9)**: 기대 기상값 = 평년치 + (장기예보 tercile 확률 기반 보정치). 작년 실측은 점수에 섞지 않고 백테스팅·유저 병기 전용으로만 쓴다 — 단일 연도 데이터는 계절 스케일에서 노이즈가 커서 평년치·공식 예보와 동급으로 평균하면 오히려 왜곡된다. 상세 공식은 `DB.md` §8.1.

등급 매핑(초안): S 90~100 / A 75~89 / B 60~74 / C 0~59.

---

## 8. 토양 변화 추정 모델 ★단기 탭 신규★

- **목적**: 흙토람은 실측 변경이 있어야 갱신되므로 실시간성이 없다. 사용자 행위(관수·시비·석회 시용 등)가 토양 지표(EC·pH·수분 등)를 **이론적으로 어떻게 변화시키는지**를 문헌 기반 룰로 추정해, 현재 토양 상태를 근사한다.
- **입력**: 기준 토양값(흙토람) + 사용자 행동 로그(행위 종류·양·시점) + 기상(강수 등).
- **원칙**:
  - **이론치임을 명시** — 실측이 아니라 추정이며 실제와 괴리가 있을 수 있음을 항상 표기.
  - 변화 계수·공식은 코드 하드코딩 금지, 문헌 근거와 함께 시드/설정으로 관리(`CLAUDE.md`).
  - 지속성·빈도에 따라 영향력이 달라지는 점을 반영(1회성 vs 반복).
- **[확인 필요]** 어떤 행위·지표를 1차 범위로 삼을지, 참고 문헌·공식은 조사 후 확정(§16).

---

## 9. 실시간 데이터 파이프라인

- **기상(단기 탭)**: 기상청 실황 + 단기예보(최대 3일)를 **실시간 조회**한다. 단, 같은 지역·같은 발표시각 데이터는 캐싱해 재사용하고, 하루 정해진 횟수(예: 6시간마다)만 갱신해 rate limit·성능을 방어한다.
- **토양**: 지역 기준값은 최초 등록 시 조회 후 저장, 이후 흙토람 갱신 주기에 맞춰 드물게 재조회. 단기 변화는 §8 모델로 보정.
- **장기 탭(시즌 커리큘럼·적합도)의 기상 데이터 — 제안된 결합 방식**:
  1. **평년치** — 지역별 월별 다년 평균(기상청 평년값). 기준선.
  2. **장기예보(3개월 전망)** — 기상청 계절전망 API. "평년보다 높음/비슷/낮음" tercile 확률로 다가오는 시즌 경향을 평년치에 보정 적용(단순 평년치 단독보다 정확도↑, §2). 확률이 평년과 다를 바 없으면(33/33/33) 보정 없음.
  3. **작년 실측** — 최근 1년 실제 관측(`weather_snapshot` OBS 백필). **점수 계산엔 넣지 않음** — 단일 연도 데이터라 평년치·공식 예보와 동급으로 섞으면 노이즈로 왜곡됨. 대신 (a) 이 로직의 백테스팅 검증용, (b) 유저 리포트에 "이번 시즌 전망 vs 작년 실제" 병기용으로 사용.
  - 보정 공식 구조는 확정(`DB.md` §8.1), 보정 강도(`δ`)의 실제 수치는 캘리브레이션 필요(§16).
- **결측·이상치 방어**: 모든 외부 데이터는 진입 시 검증, 대체 시 플래그. 출처·수집시각 기록.

> 게임 시절의 "유저 경로 실시간 호출 전면 금지" 원칙은 폐기. 단기 탭은 실시간 호출이 본질이며, 캐싱·호출 주기 제한으로 방어한다(`CLAUDE.md` §12 갱신 참고).

---

## 10. LLM 행동추천 시스템 (로컬 모델)

- **모델**: 로컬 LLM(서버 스펙·모델은 별도 인프라 문서/`docs/llm-integration.md`).
- **역할(스코프 고정)**:
  1. **단기 행동추천 (필수·메인)**: 당일 예보 위험신호 + 토양 추정 상태를 해석해 "오늘 이렇게 하라"를 자연어로.
  2. **장기 커리큘럼 서술**: 3개월 적합도 분석을 시기별 예방 가이드 문장으로.
  3. **토양/지역 특성 설명**: 초보자 눈높이 해설.
  4. **상담 챗봇**: 등록 밭 컨텍스트(직접 주입) + 큐레이션 농업 문서(RAG 검색) 기반 질의응답. RAG는 챗봇에만 적용 — ①~③은 직접 주입만.
- **입력 = 정형 분석 결과**: 점수·위험요소·토양상태·컨텍스트를 백엔드가 정형화해 주입. LLM은 수치 계산을 하지 않고 자연어 변환만.
- **RAG(챗봇 한정)**: 문서 코퍼스를 임베딩(bge-m3)해 PostgreSQL `pgvector`에 저장, 질문과 유사한 조각을 검색해 근거로 주입. 별도 벡터DB 도입 아님(쓰던 Postgres에 벡터 컬럼). 핵심 예측 기능엔 RAG 안 씀.
- **폴백**: LLM 실패 시 규칙 기반 문구로 대체, 서비스는 멈추지 않는다.

---

## 11. 데이터 정확도 / 신뢰성

- 예보-실측 대조 로그를 매일 축적해 예보 신뢰도 캘리브레이션의 근거로 삼는다. 단, 유의미한 신뢰도 지표는 수개월 누적이 필요 — 데모 기간엔 "축적 중"으로 표기하고 즉시 향상을 주장하지 않는다.
- **백테스팅**: 장기 탭 로직(평년치+장기예보 보정)을 작년 실측과 대조해 검증 — "이 로직으로 작년을 예측했다면 얼마나 맞았는가"를 확인하고 보정 단위(`δ`)를 캘리브레이션하는 근거로 삼는다(`DB.md` §8.1). 이게 "이론치와 실제 괴리를 좁힌다"는 제품 핵심 가치의 실증 방법이다.
- 정확도가 제품의 1순위 가치이므로, 예측 결과에는 항상 근거(어떤 지표가 왜 위험인지)를 함께 제시. 장기 탭은 평년치·보정치·최종 기대값을 분해해 노출(`DB.md` §8.1 breakdown).
- 이론 추정(토양변화)과 실측·전망(평년치/장기예보/작년실측)을 UI에서 구분 표기 — 성격이 다른 데이터임을 숨기지 않는다. 작년 실측은 점수가 아니라 "참고 병기" 항목임을 명확히.

---

## 12. 예외 처리

- 공공데이터 결측/이상치: 대체값 + 고지, 산출 지속.
- 외부 API 장애/rate limit: 마지막 캐시 사용 + "최신 아님" 고지, 서비스 지속.
- 로컬 LLM 실패: 규칙 기반 폴백.
- 어떤 예외도 서비스를 완전히 멈추게 하지 않는다.

---

## 13. 성능 목표

- 단기 탭 조회 시 캐시 히트 경로는 체감 1초 이내. 캐시 미스(실 API 호출) 시에도 로딩 상태를 명확히.
- LLM 응답: 로컬 모델로 수 초 내 목표(모델·서버 스펙으로 조정).

---

## 14. 리스크 / 팀 결정 필요 ★착수 전 정렬★

> 아래는 임의로 정하지 않고 팀이 명시적으로 결정/검증해야 하는 항목. 방치하면 뒤탈 나는 지점들.

- **R1. "정확도" 서사 재정의 (프레이밍)**: 작황 정답 레이블이 없어 적합도 점수의 정확도를 실측 대비로 증명할 수 없다. 백테스팅은 *날씨 예측*만 검증할 뿐 *적합도→결과* 매핑은 검증 못 함. 심사에서 "점수 정확도 검증법"을 물으면 방어 불가. → 간판을 "정확도" 단독보다 **"투명성·설명가능성·문헌 근거 + 전문가 휴리스틱 검증(골든 케이스)"**로 재정의 권장. [팀 결정]
- **R2. 외부 API 실사용 검증 = Day-0 게이트**: 흙토람(발급·스키마), 기상청 **3개월 장기예보의 시/군별 tercile 기계판독 제공 여부**, 과거관측 API — 셋 다 미검증인데 제품 전체가 여기 의존. **코드 착수 전 실제 호출로 확인**. 안 되면 스키마(`weather_outlook` 등)·설계 조정 필요. [Day-0 검증]
- **R3. 토양변화 모델 강등 검토**: §8 정량 추정은 공수 크고(문헌 계수 조사) 정확도 낮고 검증 불가 — "정확도 1순위"와 충돌. MVP에선 "기준 토양 표시 + 행위의 정성적 영향 안내"로 낮추고 정량 모델은 후순위로 미는 안 검토. [팀 결정]
- **R4. MVP 커트라인**: 서브시스템이 ~14개(인증/전국시드/흙토람/기상3종+스케줄러/생육지침/룰엔진+백테스트/토양모델/LLM/RAG/프론트6화면)로 2주엔 과다. "데모 필수 슬라이스 vs 있으면 좋은 것"을 명시적으로 나눠야 함. [팀 우선순위]

---

## 15. 향후 확장

- 대상 작물/지역 확장, 토양변화 모델 정교화(더 많은 행위·지표), 실측 센서 연동(선택), 예보 신뢰도 학습 고도화.

---

## 16. 미결정 사항

- 흙토람 API 문서/발급 여부 및 반환 스키마.
- 기상청 실황/단기예보/**장기예보(3개월 전망)**/과거관측 API 스펙·갱신 주기 확정.
- 보정 단위 `δ`의 실제 수치(캘리브레이션 필요). 결합 구조(평년치+장기예보 보정, 작년실측은 백테스팅/병기 전용)는 제안 확정(`DB.md` §8.1, §10).
- 토양 변화 추정 모델: 대상 행위·지표 1차 범위, 참고 문헌·공식.
- 지표별 가중치/구간 실제 수치(생육 지침 제작 결과).
- 로컬 LLM 구체 모델·서버 스펙(개발 Ollama / 서빙 vLLM).
- 생육 단계 구분 기준(파종일로부터 경과일 → 단계 매핑).
- 챗봇 RAG: 1차 코퍼스는 농사로 작물별 안내책자(5종 우선)로 확정 — 청킹/임베딩 파이프라인·확장 범위는 착수 시.
