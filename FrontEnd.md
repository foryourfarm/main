# For Your Farm 프론트엔드 전면 재편 마스터 프롬프트

당신은 “For Your Farm”의 프론트엔드 전면 재편을 책임지는
Lead Frontend Architect 겸 Multi-Agent Orchestrator다.

목표는 보기 좋은 정적 시안을 만드는 것이 아니다.
현재 실제로 연결된 인증·밭 관리·농업 데이터·챗봇 기능을 모두 보존하면서,
데스크톱과 모바일 각각에 맞는 완성도 높은 운영 UI로 재구축하는 것이다.

모든 설명·질문·보고서는 한국어로 작성한다.
코드·타입·식별자는 프로젝트의 기존 규칙을 따른다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 최상위 작업 원칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 구현을 시작하기 전에 반드시 사용자 요구사항 인터뷰를 진행한다.
2. 사용자가 답하지 않은 핵심 제품 결정을 임의로 확정하지 않는다.
3. 사용자에게 물어보기 전에 저장소에서 확인할 수 있는 사실은 직접 조사한다.
4. `comUI.html`은 데스크톱 UI의 배치·시각 기준이며, 화면 배치까지 90% 이상 충실도로 재현한다.
   이 HTML의 구조·컴포넌트 상세와 문제 진단은 `Desgin_Com_index.md`를 함께 근거로 삼는다.
   단, 배치 기준일 뿐 기능 계약은 아니다.
5. `comUI.html`의 하드코딩된 숫자·문구·날씨·점수·기록을 실제 코드에 복사하지 않는다.
6. 기존 API 클라이언트, 타입, 인증 흐름, SSE 챗봇과 예외 상태를 보존한다.
7. 모바일은 데스크톱을 단순히 한 열로 접은 화면이 아니라 별도의 정보구조와 컴포넌트 조합으로 설계한다.
8. 실제 동작하지 않는 버튼·메뉴·카드·필터를 완성된 기능처럼 노출하지 않는다.
   예외: §12-1의 "칸만 만드는 카드"(오늘의 할 일·행동 기록·최근 기록)는
   "준비 중" 상태를 명시한 채 노출한다.
9. 새로운 기능에 백엔드 계약이 필요하면 UI만 가짜로 만들지 말고 `[백엔드 확장 필요]`로 분리하여 사용자 승인을 받는다.
10. 사용자 승인 없이 커밋, 푸시, 배포 또는 기존 사용자 변경사항 삭제를 하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. 프로젝트 루트 및 환경 검증
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

작업 시작 시 다음 순서로 진행한다.

1. `comUI.html`, `frontend/package.json`, `PRD.md`가 함께 존재하는 실제 프로젝트 루트를 탐색한다.
   - 경로를 추측하지 않는다.
   - 현재 환경에서는 `C:\FYM\ForyourFarm`일 가능성이 높다.
   - `C:\ForyourFarm` 경로가 존재한다고 가정하지 않는다.

2. 다음 파일을 우선 읽는다.
   - `AGENTS.md`
   - `RTK.md`가 실제로 존재하면 전체 내용
   - `CLAUDE.md`
   - `PRD.md`
   - `DB.md`
   - `frontend/AGENTS.md`
   - `docs/llm-integration.md`
   - `docs/long-term-tab-api.md`
   - `docs/design/`
   - `comUI.html`
   - `Desgin_Com_index.md` (comUI.html의 상세 진단·개선 보고서)

3. `RTK.md`가 참조되어 있지만 존재하지 않으면 이를 한 줄로 보고하고, 존재하는 규칙으로 계속 진행한다.

4. `git status`를 확인하여 기존 변경사항을 기록한다.
   - 기존 변경사항은 사용자 소유로 간주한다.
   - 관련 없는 파일을 정리하거나 되돌리지 않는다.

5. 이 프로젝트는 Next.js 16이므로 구현 전에
   `frontend/node_modules/next/dist/docs/`의 관련 App Router 문서를 읽는다.
   학습 데이터에 의존하여 과거 Next.js 관례를 적용하지 않는다.

6. 셸에서 `ecc` 또는 `voltagent` 실행 파일을 호출하지 않는다.
   두 시스템은 Claude Code 플러그인과 Task 서브에이전트로 사용한다.

7. 존재하지 않는 다음 항목을 호출하지 않는다.
   - 오래된 `C:\ForyourFarm\scripts\orchestrate.ps1`
   - 설정되지 않은 Aira MCP
   - 준비되지 않은 `ccg-workflow`
   - 설치 여부를 확인하지 않은 임의의 에이전트나 명령

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. ECC 중심 서브에이전트 오케스트레이션
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

전체 작업의 최종 오케스트레이터는 ECC로 고정한다.
VoltAgent는 ECC 아래에서 전문 작업을 수행한다.
여러 에이전트가 서로 다른 최종 결정을 내리게 하지 않는다.

사용 가능한 경우 다음 ECC 기능을 활용한다.

- `/ecc:plan`
- `/ecc:feature-dev`
- `/ecc:react-review`
- `/ecc:quality-gate`
- `team-agent-orchestration`
- `planner`
- `code-explorer`
- `code-architect`
- `a11y-architect`
- `react-reviewer`
- `typescript-reviewer`
- `e2e-runner`
- `performance-optimizer`
- `security-reviewer`
- `code-reviewer`

`/ecc:multi-frontend`, `/ecc:multi-plan`, `/ecc:multi-execute`,
`/ecc:multi-workflow`는 `ccg-workflow` 런타임이 실제로 준비된 경우에만 사용한다.
준비되지 않았다면 설치를 시도하지 말고 기본 Task 서브에이전트 오케스트레이션을 사용한다.

ECC 오케스트레이터는 모든 작업 단위에 다음 정보를 기록한다.

- 작업 ID
- 담당 에이전트
- 목표
- 입력 문서 및 파일
- 허용된 수정 파일
- 수정 금지 파일
- 선행 작업
- 완료 기준
- 검증 명령
- 상태: pending / in_progress / review / completed / blocked
- 결과 근거
- 통합 순서

병렬 작업 규칙:

- 읽기 전용 분석은 적극적으로 병렬화한다.
- 동일 파일을 두 구현 에이전트가 동시에 수정하지 않는다.
- 공통 토큰과 앱 셸을 먼저 확정한 후 페이지 작업을 병렬화한다.
- 교차 파일 수정은 한 명의 통합 담당자만 수행한다.
- 각 에이전트는 자신이 건드린 파일, 판단, 테스트 결과를 handoff에 남긴다.
- 최종 판정과 병합 결정은 ECC 오케스트레이터 한 명이 담당한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. VoltAgent 전문 에이전트 활용
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

설치·활성 상태를 먼저 확인한 후 실제 존재하는 에이전트만 호출한다.

권장 역할 배치:

A. 코드베이스·기능 조사
- `codebase-orchestrator`
- `context-manager`
- ECC `code-explorer`

B. UI 디자인
- `ui-designer`
- `design-bridge`

C. 데스크톱 및 모바일 웹 구현
- `frontend-developer`
- `nextjs-developer`
- `react-specialist`

D. 검증
- `accessibility-tester`
- `ui-ux-tester`
- `performance-engineer`
- `test-automator`

E. 작업 분배 보조
- `multi-agent-coordinator`
- `task-distributor`

`mobile-developer`는 React Native 또는 Flutter 네이티브 앱용으로 간주한다.
이번 요구가 모바일 웹 UI라면 이를 기본 선택하지 말고,
`ui-designer + frontend-developer + nextjs-developer` 조합으로
별도의 모바일 웹 설계 레인을 구성한다.

펫 이미지는 사용자가 추후 제공할 예정이므로 자동 생성하지 않는다.
사용자가 이미지 생성을 별도로 요청한 경우에만 `visual-asset-generator`를 검토한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. 첫 번째 실행 단계: 병렬 읽기 전용 감사
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

코드를 수정하기 전에 다음 조사를 병렬로 수행한다.

Track A — 기능 및 API 감사
- 모든 라우트, 컴포넌트, API 호출, 타입, 인증 흐름을 조사한다.
- 현재 연결된 기능과 정적 데모 기능을 구분한다.
- 화면별 기능 연결 매트릭스를 만든다.

Track B — 데스크톱 UI 감사
- `comUI.html`의 구조, 시각 계층, 토큰, 컴포넌트를 추출한다.
- 실제 앱에 이식 가능한 부분과 하드코딩 데모 부분을 구분한다.
- 데스크톱 화면별 와이어프레임을 작성한다.

Track C — 모바일 웹 설계
- 데스크톱과 독립적인 모바일 정보구조를 제안한다.
- 한손 조작, 야외 시인성, 하단 내비게이션, safe area,
  가상 키보드, 챗봇 열림 상태를 고려한다.
- 360px, 375px, 390px, 430px 기준을 검토한다.

Track D — 접근성 및 콘텐츠
- 30~50대 사용자의 가독성, 농업 초보자의 이해도,
  키보드 조작, 확대, 명도 대비를 점검한다.
- 전문성을 유지하면서 쉬운 한국어로 바꾸는 콘텐츠 규칙을 만든다.

Track E — Next.js 구현 및 QA
- RSC/CSR 경계, 라우팅, intercepting route, 데이터 로딩,
  번들 크기, 테스트 구조를 조사한다.
- 구현 전후 검증 기준을 정의한다.

각 Track은 코드를 수정하지 않고 결과만 반환한다.
ECC 오케스트레이터는 이를 하나의 현황 보고서로 합친다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. 사용자 확정 결정사항 (재질문 금지)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 항목은 사용자가 이미 확정했다. 다시 묻지 않고 그대로 적용한다.

1. `comUI.html` 재현 수준: 화면 배치까지 90% 이상 충실도로 재현.
   상세 근거는 `Desgin_Com_index.md`.

2. 재편 범위: 로그인·회원가입·온보딩·설정을 포함한 전체 화면 UI 재편.

3. 오늘의 할 일(데일리 퀘스트)·행동 기록·최근 기록:
   UI 칸(카드·섹션)만 만들고 기능은 구현하지 않는다. §12-1 참조.

4. 모바일 첫 화면 정보 순서:
   오늘 할 일(데일리 퀘스트) → 위험 → 날씨 → 밭 점수.
   단, 심각(critical) 등급 위험이 있으면 최상단 경고 배너로 순서를 무시하고 노출한다.

5. 모바일 하단 내비게이션: 데스크톱(comUI) 상단 탭 항목을 그대로 하단 탭으로 옮긴다.
   상담 항목은 탭에서 제거한다 — 상담 진입은 펫 플로팅 버튼이 전담한다.

6. 펫: 이미지·이름·성격은 확정되어 있으나 현재는 더미데이터(placeholder)로 처리한다.
   더미 자산 경로·크기 계약만 준비하고, 실제 자산은 추후 교체한다.

7. 챗봇 표시 방식: 데스크톱은 우측 패널, 모바일은 전체 화면.

8. 타이포: 본문 18px, 보조문 16px.

9. 상세 설명 노출: “왜 그런가/근거 보기”로 접는 disclosure 방식.

10. 테마: 다크모드 유지(기존 다크모드 설정 저장 동작 보존).

11. 아이콘: 이모지 아이콘 전량 일관된 SVG 아이콘으로 교체.

12. 메인 컬러: 이삭 골드 `#F59E0B` (확정, §7-1 참조).
    초록·갈색 등 전형적 “농장색”이 아닌, 농장과 연관되면서
    서비스 차별점이 되는 포인트 컬러를 사용한다.

13. 카카오 로그인: 회원가입·로그인에 카카오 OAuth가 추가될 예정이다. `[백엔드 확장 필요]`
    백엔드 준비 전에는 동작하는 척하는 버튼을 노출하지 않는다(§1-8).
    로그인·회원가입 레이아웃은 소셜 로그인 버튼이 들어올 자리를 전제로 설계한다.

14. 단기 탭 날짜 카드: 3개까지만 표시한다.
    API가 더 많은 날짜를 반환하더라도 화면에는 앞의 3개만 노출한다.

15. 챗봇·펫 트랙: 우선순위 보류 — 팀원이 별도로 작업 중.
    이번 재편에서 챗봇 관련 파일(`lib/chat.ts`, `app/chat/`, 챗봇·펫 컴포넌트)을 수정하지 않는다.
    다른 화면 작업 시 챗봇 진입점(펫 플로팅 버튼 등)은 자리·계약만 고려하고 구현하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6-1. 남은 인터뷰 항목 (감사 결과 보고 후 질문)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

첫 번째 감사 결과를 보여준 뒤, 아직 미확정인 다음 항목만 질문한다.
질문은 한국어로 한 번에 정리하고, 각 질문에 권장 기본안을 함께 제시한다.

1. `comUI.html`의 나머지 미구현 예시 기능을 실제로 추가할 것인가?
   (오늘의 할 일·행동 기록·최근 기록은 “칸만”으로 확정됨 — 제외)
   - 7일 날씨
   - 월별 점수 분해
   - 챗봇 출처·문서 인용 패널
   - 품종·나무 연령

2. 신규 기능에 필요한 백엔드 API 및 DB 변경까지 허용되는가?

3. 챗봇의 밭 컨텍스트 결정 방식:
   - 현재 보고 있는 밭 자동 연결
   - 사용자가 직접 밭 선택
   - 대시보드의 대표 밭 자동 선택

4. 대화 기록을 새로고침 후에도 저장할 것인가?
   여러 대화방, 제목, 삭제 기능이 필요한지도 확인한다.

5. 챗봇 답변의 문서명·페이지·데이터 출처를
   클릭 가능한 인용으로 제공해야 하는가?

6. 시스템 테마 자동 연동 여부(다크모드 유지는 확정, 라이트↔다크 자동 전환만 미정).

7. 지원·검수 범위:
   - 320px 소형폰
   - 태블릿
   - 200% 브라우저 확대
   - 야외 고명도 환경
   - 장갑 사용을 고려한 큰 터치 영역

8. 펫 세부(더미데이터 단계에서는 기본안으로 진행, 실자산 교체 시 확인):
   - 표정 또는 상태 이미지 변형
   - 애니메이션 여부
   - 미확인 메시지 배지 표시
   - 로그인·회원가입 화면 노출 여부

답변을 받은 뒤 다음 형식으로 결정을 요약한다.

- 확정된 요구사항
- 권장안을 적용한 항목
- 보류된 항목
- 백엔드 확장이 필요한 항목
- 이번 작업에서 제외되는 항목
- 완료 기준

사용자 확인 전에는 구현을 시작하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. UI-UX Pro Max 필수 활용
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

사용자의 답변이 확정되면 `ui-ux-pro-max`의 `SKILL.md`를 먼저 읽고,
다음 순서로 디자인 시스템을 생성한다.

1. `--design-system` 검색을 반드시 먼저 실행한다.
   검색 키워드에는 다음 개념을 포함한다.
   - agriculture
   - farm management
   - decision support dashboard
   - trustworthy
   - organic
   - outdoor readability
   - age 30–50
   - large typography
   - Korean web app

2. 추가 검색:
   - UX: accessibility, large text, touch target, reduced motion,
     chatbot, mobile navigation, outdoor contrast
   - Stack: nextjs, responsive dashboard, forms, data cards
   - Chart: weather trend, risk, monthly outlook, score breakdown

3. 결과를 `comUI.html`보다 우선하는 절대 규칙으로 사용하지 않는다.
   `comUI.html`의 레이아웃·정보 계층을 기본으로 삼되,
   컬러 팔레트는 §7-1의 확정 메인 컬러로 교체한다.
   UI-UX Pro Max는 접근성·타이포·상호작용·안티패턴 검증에 사용한다.

4. 디자인 토큰을 하나의 Source of Truth로 정리한다.
   페이지 CSS에서 색상·반경·그림자·폰트 크기를 반복 하드코딩하지 않는다.

5. 권장 기본 방향:
   - 장식보다 정보 계층 우선
   - AI 서비스 특유의 보라·분홍 그라데이션 금지
   - 과도한 글래스모피즘·애니메이션 금지

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7-1. 컬러 팔레트 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- 메인(포인트) 컬러: 이삭 골드 `#F59E0B` (확정).
  의미: 잘 익은 곡식·햇살·수확. 다크 배경에서 강한 존재감을 가지는 포인트 컬러.
- 초록·갈색 계열을 메인 컬러로 쓰지 않는다. 전형적 “농장 서비스” 색을 피하는 것이
  의도된 차별화 전략이다.
- 중립 토대(배경·표면·텍스트)는 다크모드 기준으로 설계하고,
  메인 컬러는 CTA·활성 상태·강조·펫 관련 요소에 포인트로 사용한다.
- ★시맨틱 컬러 충돌 규칙 (골드 선택에 따른 필수 조치)★
  이삭 골드는 통상적인 “경고(warning) 앰버”와 같은 계열이다.
  따라서 시맨틱 컬러를 다음과 같이 분리한다.
  - 경고(warning): 앰버·골드 계열 사용 금지. 주황-적색 쪽으로 이동(예: `#E8590C` 계열)
    하거나, 위험(danger) 적색 단일 축으로 통합해 2단계(주의/위험)로 운영한다.
  - 위험(danger): 명확한 적색 계열. 메인 골드와 혼동 불가해야 한다.
  - 성공(success): 녹색 계열 허용 — 메인 컬러가 아닌 시맨틱 상태 표시로만 쓴다.
  - 등급·위험 표시는 어차피 색+라벨+아이콘 병기가 원칙(§10)이므로,
    색 충돌이 의심되는 화면은 라벨 병기로 이중 방어한다.
- 골드는 밝은 색이므로 다크 배경 위 텍스트·아이콘으로는 통과가 쉽지만,
  골드 배경 위 텍스트는 어두운 색(near-black)을 사용해야 AA를 통과한다.
  흰 텍스트를 골드 배경에 올리지 않는다.
- 모든 조합은 다크 배경에서 WCAG AA(4.5:1) 대비를 통과해야 한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. 데스크톱 UI 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

데스크톱은 `comUI.html`의 레이아웃과 정보 계층을 90% 이상 충실도로 재현한다.
구조 상세는 `Desgin_Com_index.md`를 따른다.
단, 컬러는 원본의 흙색·녹색 팔레트 대신 §7-1의 확정 메인 컬러 체계로 교체한다.

반영 대상:

- 최대 너비 약 1240px
- sticky 상단바
- 로고, 사용자 정보, 주요 내비게이션, 테마 전환
- 핵심 행동을 크게 보여주는 히어로
- 비대칭 12열 벤토 그리드
- 밭 카드와 적합도 표시
- 생육 단계
- 단기예보와 위험 경고
- 단기/장기 세그먼트
- 데이터 출처와 한계
- 자연스러운 둥근 모서리와 절제된 그림자
- 다크모드 기반 중립 토대 + §7-1 메인 컬러 포인트 팔레트

그러나 다음은 실제 데이터 계약에 맞게 수정한다.

- 실제 앱의 밭 상세 기본 탭은 단기로 유지한다.
- 실제 API가 최대 3일이면 7일 예보를 하드코딩하지 않는다.
- 실제 API가 제공하지 않는 월별 점수 분해를 가짜로 만들지 않는다.
- 정적 시안의 “1991–2020 평년값” 문구를 복사하지 않는다.
- 실제 장기 데이터 기준과 `limitations`를 그대로 표시한다.
- 대시보드 카드에 존재하지 않는 데이터를 임의 조합하지 않는다.
- 품종·나무 연령이 스키마에 없다면 입력란을 만들지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. 모바일 UI 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

모바일은 데스크톱의 축소판으로 만들지 않는다.
도메인 타입과 API 클라이언트는 공유하되 화면 구조는 독립적으로 설계한다.

확정된 첫 화면 정보 계층 (§6-4):

0. (심각 위험이 있을 때만) 최상단 위험 경고 배너
1. 오늘 할 일(데일리 퀘스트) — §12-1의 “칸만” 카드
2. 위험 키워드 1~3개
3. 오늘 날씨 및 핵심 수치
4. 밭 점수(적합도)와 간단한 이유
5. 현재 밭과 생육 단계
6. “왜 그런가/근거 보기”
7. 장기 전망

하단 내비게이션 (§6-5):

- 데스크톱 상단 탭 항목을 그대로 하단 탭으로 구성한다.
- 상담 탭은 두지 않는다 — 상담 진입은 펫 플로팅 버튼이 전담한다.
- 탭 수가 5개를 초과하면 사용 빈도가 낮은 항목을 “더보기”로 묶는다.

모바일 원칙:

- 한 화면에 하나의 주요 의사결정만 강조한다.
- 한 손 조작이 가능해야 한다.
- 터치 영역은 최소 48×48px, 인접 영역 간 8px 이상 간격을 둔다.
- 하단 내비게이션과 브라우저 safe area를 반영한다.
- 핵심 CTA는 손가락이 닿기 쉬운 위치에 둔다.
- 가로 스크롤을 기본 해결책으로 사용하지 않는다.
- 복잡한 차트에는 요약 문장과 데이터 표 또는 텍스트 대안을 제공한다.
- 키보드가 열렸을 때 챗봇 입력창과 전송 버튼이 가려지지 않아야 한다.
- 펫, 하단 내비게이션, 토스트, 챗봇 입력창이 겹치지 않아야 한다.
- 야외에서도 핵심 텍스트와 위험 상태가 분명해야 한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. 30~50대 사용자용 타이포·콘텐츠
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

사용자를 고령자처럼 단순화하거나 유아적으로 대하지 않는다.
전문성은 유지하되 농업 전문 용어를 즉시 풀어서 설명한다.

타이포 기준 (본문 18px·보조문 16px는 §6-8 확정, 나머지는 권장):

- 모바일 본문: 18px (확정)
- 데스크톱 본문: 18px (확정)
- 보조문·라벨: 16px (확정)
- 버튼: 17~18px
- 모바일 H1: 28~32px
- 데스크톱 H1: 32~40px
- 본문 line-height: 1.55~1.7
- 200% 확대에서도 정보와 기능이 손실되지 않아야 한다.

콘텐츠 작성 순서:

`핵심 키워드 → 지금 할 행동 → 쉬운 설명 → 주요 수치 → 근거/출처 → 한계`

예시:

- 나쁜 예: “토양 EC가 1.2dS/m이며 강수량 예측을 반영한 결과입니다.”
- 좋은 예:
  “염분은 적정해요.”
  “오늘은 물빠짐만 확인하세요.”
  “토양 염분 1.2dS/m · 08:00 갱신”
  “왜 이런 안내가 나왔나요?”

규칙:

- 한 문단에 여러 행동을 섞지 않는다.
- 긴 설명은 2~3줄 요약 후 펼쳐보기를 제공한다.
- 숫자만 보여주지 말고 의미와 행동을 함께 표시한다.
- 등급은 색상, 문자, 설명을 함께 사용한다.
- “최신 아님”, “추정”, “대체 데이터”, “LLM 생성” 상태를 숨기지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. 펫 및 챗봇
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

펫은 우측 하단의 이미지형 플로팅 버튼으로 구현한다.

기본 접근성 규칙:

- 단순 `img`가 아니라 의미 있는 `button` 안에 이미지를 배치한다.
- `aria-label="농사 상담 열기"`를 제공한다.
- 키보드 Enter/Space로 열 수 있어야 한다.
- 명확한 focus-visible 상태를 제공한다.
- 의미 있는 alt 또는 장식 이미지 처리를 구분한다.
- 모바일에서는 `env(safe-area-inset-bottom)`을 반영한다.
- reduced-motion 사용자는 펫 움직임을 비활성화한다.

권장 기본 크기:

- 데스크톱: 약 72~88px
- 모바일: 약 56~72px
- 실제 클릭 영역은 최소 48×48px

챗봇 열림 방식 (§6-7 확정):

- 데스크톱: 우측 패널 (콘텐츠를 밀거나 덮는 방식은 구현 시 결정하되, 열린 상태에서도 대시보드 핵심 정보가 보이도록 한다)
- 모바일: 전체 화면

챗봇은 기존 `/api/v1/chat` SSE 스트리밍을 재사용한다.

반드시 보존할 동작:

- 게스트 상담
- 로그인 사용자 상담
- 특정 `farmId`를 전달한 밭 기준 상담
- 멀티턴 메시지
- 스트리밍 토큰
- 중단·오류·재시도 상태
- 인증 갱신 흐름

동일한 챗봇 로직을 라우트와 플로팅 패널에 중복 구현하지 않는다.
공유 hook 또는 controller로 추출하고 UI 셸만 분리한다.

패널 접근성:

- 열릴 때 입력창 또는 패널 제목으로 포커스 이동
- 닫을 때 펫 버튼으로 포커스 복귀
- Esc 닫기
- 모달형이면 focus trap
- 스트리밍 답변은 적절한 `aria-live` 사용
- 모바일 키보드가 열려도 입력 영역 유지
- 현재 연결된 밭을 명확히 표시하고 변경 가능 여부 제공

펫 자산 처리 (§6-6 확정):

- 펫 이미지·이름·성격은 사용자 측에서 확정되어 있으나, 이번 작업에서는 더미데이터로 처리한다.
- 임의의 최종 캐릭터를 생성하지 않는다. 레이아웃 검증용 임시 placeholder임을 명확히 표시한다.
- 펫 이름·성격 문구는 상수/설정 파일 한 곳에 더미값으로 두어, 실제 값 교체가 한 파일 수정으로 끝나게 한다.
- 최종 자산 경로와 크기 계약만 준비한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. 보존해야 할 실제 기능과 계약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

기존 기능 연결 매트릭스를 먼저 만들고 구현 전후를 비교한다.

보존 대상:

- 회원가입
- 로그인
- 로그아웃
- 새로고침 후 세션 복구
- 401 후 refresh 1회 재시도
- 밭 등록·조회·수정·삭제
- 지역 → 읍면동·리 선택
- 작물 5종 선택
- 대시보드 여러 밭
- 빈 상태
- 단기 예보
- 지속 위험
- 행동 추천
- 날짜 상세 모달
- 기온 차트와 채점 근거
- 장기 3개월 전망
- 월별 등급·점수·생육 단계
- 위험 시기
- 데이터 부족·휴면기·제철 아님
- 설정과 데이터 출처
- 게스트 및 로그인 챗봇
- 밭 컨텍스트 챗봇
- 대시보드 카드 클릭 시 intercepting modal
- 직접 URL 접근 시 전체 상세 페이지
- 다크모드 설정 저장

보존해야 할 데이터 상태:

- `limitations`
- `is_stale`
- `is_imputed`
- `outlook_applied`
- `is_llm`
- null 점수
- null 등급
- 부분 시간대 표본
- 로딩
- 빈 상태
- 네트워크 오류
- 인증 만료
- 외부 데이터 실패

일반 API의 `{ success, data, error }` 응답과
챗봇 SSE 예외 계약을 혼동하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12-1. 칸만 만드는 카드 (기능 미구현, UI 자리만)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

다음 세 카드는 이번 작업에서 UI 자리(카드·섹션)만 만들고 기능은 구현하지 않는다.
기능은 추후 별도 작업으로 추가된다.

- 오늘의 할 일 (데일리 퀘스트)
- 행동 기록
- 최근 기록

규칙:

- 각 카드에 “준비 중” 상태를 명시적으로 표시한다. 완성된 기능처럼 위장하지 않는다.
- 가짜 데이터·가짜 목록을 채워 넣지 않는다. 빈 상태 문구와 아이콘만 둔다.
- 클릭 시 동작하는 척하는 버튼·링크를 넣지 않는다.
- 레이아웃은 향후 실데이터가 들어올 것을 전제로 잡되,
  데이터 스키마를 추측해 타입·API 호출을 미리 만들지 않는다.
- `comUI.html`의 해당 영역 배치를 따르고, 하드코딩된 예시 내용은 복사하지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. 구현 구조와 병렬 작업 DAG
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

권장 구현 순서:

Phase 1 — 기반 작업, 직렬
- 디자인 토큰
- 글로벌 타이포
- 아이콘 규칙
- 반응형 앱 셸
- Header/Navigation
- 공통 Button/Card/Badge/Disclosure/State 컴포넌트
- 모바일·데스크톱 분기 원칙

Phase 2 — 기능군별 병렬 구현
- Track 1: 로그인·회원가입·온보딩·설정
- Track 2: 대시보드·밭 카드·칸만 카드(§12-1)
- Track 3: 단기·장기 상세·차트·모달
- Track 4: 펫·챗봇·밭 컨텍스트 (보류 — §6-15, 팀원 별도 작업 중)
- Track 5: 접근성·콘텐츠·상태 점검(읽기 전용 리뷰)

각 Track은 자신에게 허용된 파일만 수정한다.
공통 파일 변경 요청은 통합 담당자에게 전달한다.

Phase 3 — 통합
- 라우팅
- API 연결
- 데스크톱/모바일 앱 셸
- 디자인 토큰 중복 제거
- 충돌 해결
- 회귀 테스트

Phase 4 — ECC 리뷰
- `/ecc:react-review`
- TypeScript 리뷰
- 접근성 리뷰
- 성능 리뷰
- 보안 리뷰
- `/ecc:quality-gate`

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. 기술 제약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Next.js 16.2.11 App Router
- React 19.2.4
- TypeScript strict
- 기존 CSS Modules 유지
- 기존 `lib/auth.ts`, `lib/farm.ts`, `lib/chat.ts`와 타입을 우선 재사용
- 필요하지 않은 상태관리·UI·차트 라이브러리를 추가하지 않는다.
- 새 의존성이 필요하면 이유, 번들 영향, 대안부터 보고한다.
- 초기 데이터 로딩에 불필요한 `useEffect` 체인을 만들지 않는다.
- Server/Client Component 경계를 최소화한다.
- 펫 이미지에는 `next/image` 사용을 검토한다.
- 폰트는 가능하면 `next/font` 또는 로컬 파일로 관리한다.
- 이모지 아이콘은 사용하지 않는다. 전량 일관된 SVG 아이콘으로 교체한다 (§6-11 확정).
  아이콘 세트는 하나의 라이브러리 또는 하나의 스타일로 통일한다.
- hover에만 의존하는 정보를 만들지 않는다.
- hover 시 scale로 레이아웃이 움직이지 않게 한다.
- `prefers-reduced-motion`을 존중한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. 검증 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최소 검증:

- `npm ci`
- `npx tsc --noEmit`
- `npm run build`

Playwright가 이미 있으면 사용한다.
없으면 사용자 승인 없이 무거운 테스트 의존성을 추가하지 않는다.
대신 사용 가능한 브라우저 도구로 실제 화면을 검증한다.

필수 화면 폭:

- 320px
- 375px
- 390px
- 430px
- 768px
- 1024px
- 1440px

필수 시나리오:

1. 회원가입 → 로그인 → 대시보드
2. 첫 밭 등록
3. 여러 밭 표시
4. 밭 상세 모달 열기·닫기
5. 상세 URL 직접 진입
6. 단기/장기 전환
7. 날짜 상세와 차트
8. 데이터 부족·오류·stale·imputed 상태
9. 밭 수정·삭제
10. 펫으로 챗봇 열기
11. 밭 컨텍스트 전환
12. SSE 전송·스트리밍·중단·오류·재시도
13. 키보드만으로 전체 흐름
14. 200% 확대
15. 다크모드
16. reduced-motion
17. 모바일 키보드가 열린 채팅 화면
18. 펫과 하단 내비게이션 충돌 여부

접근성 완료 기준:

- WCAG AA 이상
- 일반 텍스트 대비 4.5:1 이상
- 색상만으로 상태 구분하지 않음
- 모든 입력에 label
- 모든 기능 키보드 접근 가능
- focus-visible 명확
- 차트에 텍스트 대안
- 페이지 제목과 heading 순서 정상
- 오류 메시지가 해당 입력과 연결됨
- 모바일 터치 영역 최소 48×48px

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
16. 최종 산출물
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

최종 보고에는 다음을 포함한다.

1. 사용자 요구사항 결정표
2. 사용한 ECC·VoltAgent 에이전트와 담당 범위
3. 데스크톱 IA
4. 모바일 IA
5. 디자인 토큰과 타이포 체계
6. 기능/API 연결 매트릭스
7. 변경 파일 목록
8. 기존 기능 보존 여부
9. 새로 추가된 기능
10. 제외·보류된 기능
11. 실행한 테스트와 결과
12. 화면 크기별 시각 검증 결과
13. 접근성 결과
14. 남은 위험과 후속 작업

완료 판정:

- 정적 데모가 아니라 실제 API와 연결되어 있다.
- 기존 기능이 끊기지 않았다.
- 동작하지 않는 가짜 UI가 없다.
- 데스크톱은 `comUI.html`의 배치를 90% 이상 재현하되, 컬러는 §7-1 확정 팔레트를 따른다.
- 모바일은 독립적인 사용 흐름을 가진다.
- 30~50대가 핵심 행동을 빠르게 이해할 수 있다.
- 펫을 통해 실제 챗봇이 열린다.
- 로딩·빈 상태·오류·추정·최신 아님 상태가 모두 표현된다.
- 타입 검사와 프로덕션 빌드가 통과한다.
- ECC 품질 게이트의 blocking 항목이 모두 해결되었다.