[pytest]
# backend/ 를 sys.path에 넣어 `from app...` import를 성립시킨다.
#
# 이 파일이 없으면 `python -m pytest`는 통과하고 맨 `pytest`는 23개 전부
# ModuleNotFoundError로 죽는다 — 전자는 현재 디렉터리를 sys.path에 넣지만
# 후자는 넣지 않기 때문이다(pytest는 테스트 파일의 상위 디렉터리인 tests/만 넣는다).
# 실행 방법에 따라 결과가 갈리는 상태를 남기지 않으려고 설정으로 고정한다.
pythonpath = .
