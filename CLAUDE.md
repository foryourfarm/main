# CLAUDE.md — 프로젝트 개발 헌법

> 이 문서는 프로젝트의 모든 개발 활동(사람 + AI 에이전트)이 따라야 하는 최상위 규칙이다.
> 다른 문서와 충돌 시 이 문서가 우선한다. 예외는 반드시 PR 설명에 근거를 남긴다.

---

## 1. 프로젝트 철학

**"박제된 매뉴얼이 아니라, 내 땅과 오늘 날씨에 맞는 맞춤형 행동 가이드."**

초보 귀농인은 전국 표준 생육 지침을 배우지만, 실제 정착지의 토양·기후는 매뉴얼과 다르다.
우리는 실제로 농사를 짓는 귀농인에게, 실제 공공데이터 기반으로 **장기(시즌 커리큘럼)와 단기(당일 실시간 대응)** 행동 가이드를 매일 제공한다. (게임형 시뮬레이션 기획은 폐기됨 — `PRD.md` 참고.)

핵심 가치:
1. **정확성 우선** — 데이터의 정확성과 해석의 신뢰성이 먼저다. 이론치와 실제의 괴리를 좁히는 것이 이 제품의 핵심 경쟁력이다.
2. **눈높이 번역** — EC, 유효인산 같은 전문 용어를 초보자가 직관적으로 이해할 언어로 바꾼다. 단, 번역이 원래 의미를 왜곡해선 안 된다.
3. **선제적 안내** — 문제가 터진 뒤 알려주는 게 아니라, 위험 신호를 미리 감지해 안내한다.
4. **정직한 한계 표기** — 시/군 평균, 평년치, 이론 추정치 등 데이터의 한계를 숨기지 않고 UI/리포트에 명시한다.

---

## 2. 개발 원칙

- **YAGNI**: 지금 필요하지 않은 기능/추상화는 만들지 않는다. 5종 작물 고정이라는 제약을 최대한 활용해 단순하게 간다.
- **한계는 코드보다 문서로**: 데이터 근사(평년치, 시군 평균, 이론 추정치 등)를 쓸 때는 코드 주석과 유저 노출 텍스트 양쪽에 명시한다.
- **결정론 우선**: 적합도·토양변화·위험판정 계산은 같은 입력이면 항상 같은 출력이어야 한다. 랜덤/승패 판정 없음(순수 정보 제공형).
- **경계에서 방어**: 공공 API 응답은 항상 결측·이상치가 있다고 가정하고 파이프라인 진입 지점에서 검증한다.
- **작은 PR**: 한 PR은 한 가지 일만. 리뷰 가능한 크기를 유지한다.

---

## 3. AI(SubAgent) 협업 규칙

이 프로젝트는 사람 + Claude 에이전트가 함께 개발한다.

1. **작업 전 컨텍스트 로드**: 에이전트는 작업 시작 전 `CLAUDE.md`, `PRD.md`, `DB.md`를 읽고 관련 섹션을 근거로 삼는다.
2. **결정되지 않은 것은 결정하지 않는다**: 미결정 사항(아래 §18)을 만나면 임의로 정하지 말고 `[확인 필요]` 주석을 남기고 사람에게 확인받는다.
3. **명세 우선**: 코드가 명세와 다르면 명세를 고치는 PR과 코드 PR을 분리한다. 코드가 슬쩍 명세를 앞서가지 않는다.
4. **추측 금지**: 공공 API 응답 스키마, 농업 수치 기준 등은 추측하지 않고 실제 문서/데이터로 확인한다.
5. **한 에이전트 = 한 관심사**: FE/BE/데이터 파이프라인 작업은 분리해서 맡긴다. 교차 변경은 명시적으로 조율한다.
6. **검증 가능한 산출물**: 비자명한 로직(점수 계산, 파서, 결측치 처리)에는 최소 두번의 실행 가능한 테스트를 남긴다.
7. **FE 인계 문서 필수**: 모든 백엔드 작업(엔드포인트·스키마·인증·연동 계약)은 완료 시 `docs/`에 FE 개발자용 보고서를 남긴다. FE는 이 문서만 보고 연동할 수 있어야 한다 — 엔드포인트 계약(메서드·경로·요청/응답·상태코드·쿠키), FE가 해야 할 것, 에러 처리를 포함한다. 예: `docs/auth-security.md`.

---

## 4. 코딩 컨벤션

**공통**
- 언어: 코드/식별자 영어, 주석/문서 한국어 허용.
- 파일 인코딩 UTF-8, 개행 LF.
- 매직 넘버 금지 → 상수/설정으로. 특히 농업 기준값은 반드시 생육 지침 시드 데이터로 분리(코드에 하드코딩 금지).

**네이밍**
- 변수/함수: `snake_case` (Python), `camelCase` (TS).
- 클래스/타입/컴포넌트: `PascalCase`. 모듈/파일: `snake_case` (Python).
- 상수: `UPPER_SNAKE_CASE`.
- DB 컬럼/테이블: `snake_case`.
- 불리언: `is`/`has`/`should` 접두어.

**Python**
- 타입힌트 필수(`mypy`/`pyright` 기준으로 통과). `Any` 남발 금지.
- 포매터/린터: `ruff`(포맷+린트) 권장. 라인 길이·import 정렬 통일.
- 런타임 경계(공공 API/LLM 응답)는 `pydantic`으로 파싱·검증.

**주석**
- "무엇"이 아니라 "왜"를 적는다.
- 의도적으로 근사·단순화한 지점은 근거와 한계를 명시한다.

---

## 5. 폴더 구조

```
foryourfarm/
├── CLAUDE.md            # 본 문서
├── PRD.md               # 제품 요구사항
├── DB.md                # DB 설계
├── README.md
├── docs/
│   └── seed/            # 생육 지침 시드 (작물별 JSON/YAML)
├── frontend/            # Next.js (App Router)
│   ├── app/
│   ├── components/
│   ├── lib/
│   ├── hooks/
│   ├── types/
│   └── public/assets/   # 아이콘, 이미지
└── backend/             # FastAPI (Python)
    ├── requirements.txt
    ├── alembic.ini
    ├── alembic/         # 마이그레이션 (versions/)
    └── app/
        ├── main.py      # FastAPI 앱 + 예외 핸들러
        ├── core/        # 설정(config), 보안
        ├── db/          # SQLAlchemy 세션
        ├── models/      # SQLAlchemy 모델 (도메인 엔티티)
        ├── schemas/     # pydantic DTO (요청/응답)
        ├── api/         # 라우터 (엔드포인트)
        ├── services/    # 유스케이스, 룰 엔진(적합도·토양변화)
        └── infra/       # 공공 API 클라이언트, LLM 클라이언트, 스케줄러
```

---

## 6. API 작성 규칙 (FastAPI REST)

- **URL**: 명사 복수형, `kebab-case`. 예: `/api/v1/farms/{farm_id}/short-term`.
- **버저닝**: `/api/v1/` 접두어 고정. 라우터는 `app/api/`에 도메인별로.
- **메서드**: 조회 GET, 생성 POST, 수정 PUT/PATCH, 삭제 DELETE.
- **응답 포맷**: 성공/실패 공통 래퍼(`app/schemas/common.py`의 `ApiResponse`) 사용.
  ```json
  { "success": true, "data": { ... }, "error": null }
  { "success": false, "data": null, "error": { "code": "SOIL_DATA_MISSING", "message": "..." } }
  ```
- **에러 코드**: 문자열 상수/Enum으로 관리. 공공데이터 결측/이상치·외부 API 장애는 전용 코드(`WEATHER_DATA_MISSING`, `SOIL_OUT_OF_RANGE`, `UPSTREAM_UNAVAILABLE` 등).
- **DTO(스키마) ≠ 모델**: SQLAlchemy 모델을 그대로 응답하지 않는다. 항상 pydantic 스키마로 변환.
- **검증**: 입력은 pydantic 스키마로. 지역/작물 식별자는 화이트리스트로 검증.
- **스트리밍 예외**: 챗봇 등 SSE/스트리밍 응답은 `ApiResponse` JSON 래퍼를 쓰지 않는다(토큰을 흘려보내는 특성상). 스트림 중 오류는 이벤트로 전달하고, 스트림 시작 전 오류만 일반 래퍼로.
- **소유권**: 특정 유저 리소스(`/farms/...`)는 요청 유저 소유인지 서비스 계층에서 검증(§11).
- **신선도 표기**: 캐시로 응답한 실시간 데이터는 `data`에 조회시각/신선도(예: `fetched_at`, `is_stale`)를 실어 프론트가 "최신 아님"을 표시할 수 있게 한다(§12).

---

## 7. TypeScript 규칙

- `strict: true` 필수. `any` 금지(불가피하면 `unknown` + 좁히기, 주석 근거).
- API 응답은 반드시 타입 정의 후 사용. 런타임 경계에서 `zod` 등으로 파싱 검증(로컬 LLM/공공 API 응답).
- 타입은 `types/`에 도메인별로. FE-BE 공유 개념(적합도 등급 등)은 단일 정의를 참조.
- `enum` 대신 `as const` + 유니온 선호.
- 옵셔널 체이닝/널 병합으로 결측 데이터 방어. 화면 컴포넌트는 데이터 없음 상태를 항상 처리.

---

## 8. React 규칙

- 함수형 컴포넌트 + Hooks만.
- 컴포넌트는 한 가지 책임. 200줄 넘으면 분리 검토.
- 비즈니스 로직(점수 해석 등)은 컴포넌트가 아니라 `lib/`/`hooks/`로.
- 서버 상태는 데이터 페칭 계층(예: React Query)으로, 로컬 UI 상태는 `useState`/`useReducer`로 분리.
- 차트/그래프 등 데이터 시각화는 검증된 라이브러리 활용. 가독성 우선(초보자 대상).
- 접근성 기본: 키보드 포커스 가시화, 모달 Esc 닫기, 색만으로 등급 구분 금지(라벨 병기).
- `prefers-reduced-motion` 존중.

---

## 9. Next.js App Router 규칙

- `app/` 라우팅. 페이지: `login`, `onboarding(밭 등록)`, `dashboard`, `farm/[farmId]`(장기/단기 탭), `chat(상담)`, `history(기록)`.
- **RSC/CSR 경계**: 정적/데이터 페칭은 서버 컴포넌트, 인터랙션(탭 전환, 챗봇 등)은 `'use client'` 컴포넌트.
- 인증 게이팅은 middleware에서. 비로그인은 `/login`으로.
- 공공데이터/적합도 계산은 Next에서 하지 않는다 — 전부 FastAPI 백엔드 호출. Next는 BFF/렌더링 역할만.
- 환경변수: 공개값만 `NEXT_PUBLIC_` 접두어. 비밀값은 서버 전용.

---

## 10. SQLAlchemy / Alembic 규칙

- 모델은 `app/models/`에(SQLAlchemy 2.0 `Mapped`/`mapped_column`). 스키마 자체는 Alembic이 관리하고 모델은 매핑만 — 런타임 `create_all` 사용 금지.
- 마스터 데이터(생육 지침, 지역-격자 매핑, 토양변화 계수)는 마이그레이션/시드로 적재. 런타임에 수정하지 않는다.
- 스키마 변경은 Alembic 마이그레이션으로만 버전 관리(수동 ALTER 금지). baseline은 `alembic/versions/0001_init_schema.py`.
- 트랜잭션 경계는 서비스 계층. 세션은 요청 단위 의존성(`get_db`)으로 주입, 라우터에서 직접 커밋 로직을 흩뿌리지 않는다.
- N+1은 `selectinload`/`joinedload`로 대응.

---

## 11. 인증 / 인가 규칙

- 인증: 로그인 필요. 세션/토큰 방식은 구현 시 확정하되 비밀은 서버에만.
- **DB 직접 노출 금지**: 클라이언트가 DB에 직접 붙지 않는다. 모든 접근은 FastAPI를 경유하며 인가는 서비스/라우터 계층에서 처리(행 수준 보안은 애플리케이션이 책임).
- 유저는 본인 소유 데이터(밭, 행동 기록)만 접근 가능하도록 소유권 검증(요청 유저 == user_farm.user_id).
- 비밀번호는 해시(bcrypt/argon2 등). 평문 저장·로깅 절대 금지.

---

## 12. 공공데이터 API 연동 규칙 ★핵심 리스크★

- **실시간 조회 + 캐싱**이 기본(전국 임의 지역·매일 예보 대응 위해). 실시간 호출은 허용하되 무분별한 호출은 막는다.
  - 기상(단기 탭): 기상청 실황/단기예보를 실시간 조회. 단 같은 (지역, kind, 발표시각)은 `weather_snapshot`에 캐시해 재사용하고, 갱신 주기(예: 6시간)를 제한해 rate limit·성능을 방어한다.
  - 토양: 흙토람 등 지역 기준값은 유저 등록/드문 갱신 시에만 조회해 저장. 단기 변화는 실측이 아니라 문헌 기반 추정 모델로 보정한다(`PRD.md` §8).
  - 장기 탭: 시즌 적합도·커리큘럼은 **평년치 + 장기예보(3개월 전망) + 작년 실측** 세 신호를 함께 조회·캐시해 산출(단순 평년치 단독 아님, `PRD.md` §9, `DB.md` §8.1).
- **결측·이상치 방어(필수)**: 모든 외부 데이터는 진입 시 검증한다.
  - 범위 밖 값(예: pH 14 초과, 음수 강수량) → 이상치 처리.
  - 결측 → 대체 로직(인접 기간 보간/평년치) 적용 후 "대체됨" 플래그 기록.
  - 어떤 경우에도 산출이 예외로 죽지 않게 한다.
- **외부 API 장애 방어**: 실패 시 마지막 캐시 사용 + "최신 아님" 고지. 유저 요청이 API 장애로 죽지 않게 한다.
- **rate limit / 재시도**: 지수 백오프 재시도. API 키는 서버 환경변수로만.
- **출처·수집시각 기록**: 적재/조회 데이터는 출처와 시각을 함께 저장(재현성·디버깅).

---

## 13. LLM 프롬프트 관리 규칙 (로컬 모델)

- **모델**: 로컬 LLM 사용(외부 API 아님). 모델 교체 가능하도록 추론 호출을 인터페이스로 추상화.
- **프롬프트 버전 관리**: 프롬프트는 코드에 흩뿌리지 않고 템플릿 파일/상수로 관리하며 버전을 남긴다.
- **입력 = 구조화된 분석 결과**: 적합도 점수 + 위험 요소 + 지역/작물 컨텍스트를 정형 데이터로 만들어 프롬프트에 주입. LLM에게 수치 계산을 맡기지 않는다(계산은 백엔드 룰 엔진, LLM은 자연어 변환만).
- **응답 검증/폴백**: LLM 응답 파싱 실패·비정상 출력 시 규칙 기반 기본 문구로 폴백. LLM 실패가 서비스를 막지 않게 한다.
- **RAG는 챗봇에만**: 핵심 예측 기능(적합도/추천/설명)은 정형 데이터 **직접 주입**이지 RAG 아님. 상담 챗봇만 큐레이션 문서 코퍼스를 `pgvector`로 검색해 근거 주입(별도 벡터DB 도입 아님). 관련 문서 없으면 지어내지 말고 "확실치 않음/전문가 상담" 폴백(정확도 우선). 상세 `PRD.md` §10, `DB.md` §3.15.
- **역할 범위**: (1) 단기 행동추천(필수·메인 — 당일 예보 위험신호+토양상태 해석), (2) 장기 커리큘럼 서술, (3) 토양/지역 특성 설명, (4) 재배 상담 챗봇(RAG). 그 외 자유 대화로 확장하지 않는다(스코프 고정). 상세는 `PRD.md` §10.

---

## 14. Git 전략 / Commit 규칙

**브랜치**
- `main`: **배포 전용**. 실전 테스트 없이 여기로 직접 작업/머지하지 않는다.
- `dev`: **통합 및 실전 테스트 브랜치**. 모든 기능 개발·테스트는 여기로 모인다 — PR/머지는 항상 `dev`를 대상으로 연다.
- `feature/<scope>-<요약>`, `fix/<요약>`, `docs/<요약>` → 전부 `dev`에서 분기, `dev`로 머지.
- `dev`에서 충분히 검증된 뒤에만 `dev → main` 승격(배포 담당자가 별도로 진행, 일반 기능 PR의 대상이 아님).
- 직접 `main` 푸시 금지. PR + 리뷰 1인 이상 승인 후 `dev`에 머지.

**커밋 (Conventional Commits)**
```
<type>(<scope>): <제목>

<본문: 왜 이렇게 했는지>
```
- type: `feat` `fix` `docs` `refactor` `test` `chore` `perf`.
- 제목은 명령형, 50자 이내. 한 커밋 = 한 논리 변경.

---

## 15. QA 체크리스트 (PR 전 셀프체크)

- [ ] 5종 작물 각각에서 화면/계산이 정상 동작하는가.
- [ ] 공공데이터 결측/이상치·외부 API 장애에도 산출이 죽지 않는가(캐시 폴백).
- [ ] 데이터 근사(평년치·시군평균·이론 추정치) 한계가 UI에 표기되는가.
- [ ] 장기/단기 탭이 각각 올바른 데이터원(평년 vs 실시간)으로 동작하는가.
- [ ] 로그인/비로그인 접근 제어 및 밭 소유권 검증이 동작하는가.
- [ ] 색만으로 등급을 구분하지 않는가(라벨 병기).
- [ ] 로컬 LLM 응답 실패 시 폴백 문구가 나오는가.

---

## 16. 코드 리뷰 체크리스트

- [ ] 명세(PRD/DB)와 일치하는가. 임의 결정이 섞이지 않았는가.
- [ ] 농업 기준값이 코드에 하드코딩되지 않고 시드 데이터를 참조하는가.
- [ ] 계산 로직이 결정론적이며 테스트가 있는가.
- [ ] SQLAlchemy 모델이 라우터 밖으로 새지 않는가(pydantic 스키마 변환).
- [ ] 외부 데이터 신뢰 경계에서 검증하는가.
- [ ] 불필요한 추상화/미사용 코드가 없는가(YAGNI).

---

## 17. 보안 / 성능 규칙

**보안**
- 비밀(API 키, DB 비밀번호, 모델 경로)은 환경변수/시크릿 매니저. 레포·로그·클라이언트 노출 금지.
- 개인정보/민감값을 URL 쿼리에 넣지 않는다.
- 입력은 신뢰 경계에서 검증(지역·작물 화이트리스트).
- 의존성 취약점 정기 점검.

**성능**
- 실시간 외부 호출은 캐시 우선 — 캐시 히트 시 외부 API·무거운 계산을 타지 않게.
- 적합도(장기)는 (지역, 작물, 생육단계) 단위 캐시(입력 동일 → 출력 동일). 기상(단기)은 (지역, kind, 발표시각) 단위 캐시.
- DB 조회는 인덱스 활용, N+1 제거.

---

## 18. 절대 금지사항

1. ❌ 캐시 없이 외부 API를 무분별 호출(같은 지역·발표시각 중복 호출, 갱신 주기 무시). 실시간 조회는 허용하되 캐싱·주기 제한 필수.
2. ❌ 농업 기준값/가중치/토양변화 계수를 코드에 하드코딩(시드·설정으로만).
3. ❌ LLM에게 점수·수치 계산 위임(자연어 변환만 허용).
4. ❌ 데이터 근사·이론 추정치 한계를 숨기고 확정 실측값처럼 표기.
5. ❌ 결측/이상치·외부 API 장애로 산출이 예외로 죽게 방치.
6. ❌ SQLAlchemy 모델 직접 노출, 비밀번호 평문 저장/로깅.
7. ❌ 명세에 없는 기능을 코드가 임의로 앞서 구현(승패/RNG로 농사 결과를 만들어내는 게임형 시뮬레이션 등 — 본 프로젝트에 없음).
   - 예외: `PRD.md` §14.5가 정의한 리텐션 장치(펫·레벨·데일리 퀘스트). **제품 판단(적합도·추천·위험)에 개입하지 않고**, RNG 없이 퀘스트 로그에서 파생 계산하는 범위에서만 허용한다.
8. ❌ `main` 직접 푸시, 리뷰 없는 머지.

---

## 19. 미결정 사항 (작업 전 확인)

- 흙토람/기상청 공식 API 문서·발급·반환 스키마.
- 토양 변화 추정 모델: 대상 행위·지표, 참고 문헌·공식(조사 필요).
- 로컬 LLM 구체 모델·서버 스펙·구동 방식.
- 생육 단계 구분 기준(파종일 경과일 → 단계 매핑).
- 기상 캐시 갱신 주기.
- 상세 미결정 목록은 `PRD.md` §15 / `DB.md` §10 참조.
