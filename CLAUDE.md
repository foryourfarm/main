# CLAUDE.md — 프로젝트 개발 헌법

> 이 문서는 프로젝트의 모든 개발 활동(사람 + AI 에이전트)이 따라야 하는 최상위 규칙이다.
> 다른 문서와 충돌 시 이 문서가 우선한다. 예외는 반드시 PR 설명에 근거를 남긴다.

---

## 1. 프로젝트 철학

**"박제된 매뉴얼이 아니라, 내 땅과 날씨에 맞는 맞춤형 예방 가이드."**

초보 귀농인은 전국 표준 생육 지침을 배우지만, 실제 정착지의 토양·기후는 매뉴얼과 다르다.
우리는 유저가 재배를 시작하기 전에, 실제 공공데이터 기반으로 시행착오를 **게임처럼 미리 경험**하게 한다.

핵심 가치:
1. **정확성 우선** — 게임적 재미보다 데이터의 정확성과 해석의 신뢰성이 먼저다. 틀린 농업 정보는 재미없는 UI보다 나쁘다.
2. **눈높이 번역** — EC, 유효인산 같은 전문 용어를 초보자가 직관적으로 이해할 게임 언어로 바꾼다. 단, 번역이 원래 의미를 왜곡해선 안 된다.
3. **선제적 안내** — 문제가 터진 뒤 알려주는 게 아니라, 위험 신호를 미리 감지해 안내한다.
4. **정직한 한계 표기** — 시/군 단위 평균, 작년 데이터 기반 등 데이터의 한계를 숨기지 않고 UI/리포트에 명시한다.

---

## 2. 개발 원칙

- **YAGNI**: 지금 필요하지 않은 기능/추상화는 만들지 않는다. 5종 작물 고정이라는 제약을 최대한 활용해 단순하게 간다.
- **한계는 코드보다 문서로**: 데이터 근사(평년치, 시군 평균 등)를 쓸 때는 코드 주석과 유저 노출 텍스트 양쪽에 명시한다.
- **결정론 우선**: 적합도 점수 계산은 같은 입력이면 항상 같은 출력이어야 한다. 랜덤/실패 판정 없음(순수 정보 제공형). (v2 이후 경제 시스템을 추가할 때도 이 원칙 유지 — 랜덤 이벤트나 승패 조건을 넣지 않는다. `PRD.md` §16 참고, MVP엔 없음.)
- **경계에서 방어**: 공공 API 응답은 항상 결측·이상치가 있다고 가정하고 파이프라인 진입 지점에서 검증한다.
- **작은 PR**: 한 PR은 한 가지 일만. 리뷰 가능한 크기를 유지한다.

---

## 3. AI(SubAgent) 협업 규칙

이 프로젝트는 사람 + Claude 에이전트가 함께 개발한다.

1. **작업 전 컨텍스트 로드**: 에이전트는 작업 시작 전 `CLAUDE.md`, `PRD.md`, `DB.md`를 읽고 관련 섹션을 근거로 삼는다.
2. **결정되지 않은 것은 결정하지 않는다**: 미결정 사항(아래 §18)을 만나면 임의로 정하지 말고 `[확인 필요]` 주석을 남기고 사람에게 확인받는다.
3. **명세 우선**: 코드가 명세와 다르면 명세를 고치는 PR과 코드 PR을 분리한다. 코드가 슬쩍 명세를 앞서가지 않는다.
4. **추측 금지**: 공공 API 응답 스키마, 농업 수치 기준 등은 추측하지 않고 실제 문서/데이터로 확인한다.
5. **한 에이전트 = 한 관심사**: FE/BE/데이터 파이프라인 작업은 분리해서 맡긴다. 교차 변경은 명시적으로 조율한다.
6. **검증 가능한 산출물**: 비자명한 로직(점수 계산, 파서, 결측치 처리)에는 최소 두번의 실행 가능한 테스트를 남긴다.

---

## 4. 코딩 컨벤션

**공통**
- 언어: 코드/식별자 영어, 주석/문서 한국어 허용.
- 파일 인코딩 UTF-8, 개행 LF.
- 매직 넘버 금지 → 상수/설정으로. 특히 농업 기준값은 반드시 생육 지침 시드 데이터로 분리(코드에 하드코딩 금지).

**네이밍**
- 변수/함수: `camelCase` (TS), `camelCase` (Java).
- 타입/클래스/컴포넌트: `PascalCase`.
- 상수: `UPPER_SNAKE_CASE`.
- DB 컬럼/테이블: `snake_case`.
- 불리언: `is`/`has`/`should` 접두어.

**주석**
- "무엇"이 아니라 "왜"를 적는다.
- 의도적으로 근사·단순화한 지점은 근거와 한계를 명시한다.

---

## 5. 폴더 구조

```
foryourfarm/
├── CLAUDE.md            # 본 문서
├── PRD.md               # 제품 요구사항
├── DB.md                # DB 설계
├── README.md
├── docs/
│   └── seed/            # 생육 지침 시드 (작물별 JSON/YAML)
├── frontend/            # Next.js (App Router)
│   ├── app/
│   ├── components/
│   ├── lib/
│   ├── hooks/
│   ├── types/
│   └── public/assets/   # 도트 스프라이트, 사운드
└── backend/             # Spring Boot
    ├── src/main/java/.../
    │   ├── domain/      # Entity, 도메인 로직
    │   ├── application/ # Service (유스케이스)
    │   ├── api/         # Controller, DTO
    │   ├── infra/       # 공공 API 클라이언트, 배치, JPA Repository
    │   └── config/
    └── src/main/resources/
```

---

## 6. API 작성 규칙 (Spring Boot REST)

- **URL**: 명사 복수형, `snake_case` 대신 `kebab-case`. 예: `/api/v1/regions/{regionId}/suitability`.
- **버저닝**: `/api/v1/` 접두어 고정.
- **메서드**: 조회 GET, 생성 POST, 전체수정 PUT, 부분수정 PATCH, 삭제 DELETE. 본 프로젝트는 조회 중심.
- **응답 포맷**: 성공/실패 공통 래퍼 사용.
  ```json
  { "success": true, "data": { ... }, "error": null }
  { "success": false, "data": null, "error": { "code": "SOIL_DATA_MISSING", "message": "..." } }
  ```
- **에러 코드**: 열거형으로 관리. 특히 공공데이터 결측/이상치 상황은 전용 코드를 둔다(`WEATHER_DATA_MISSING`, `SOIL_OUT_OF_RANGE` 등).
- **DTO ≠ Entity**: Entity를 컨트롤러 밖으로 노출하지 않는다. 항상 DTO로 변환.
- **검증**: 입력은 `@Valid` + Bean Validation. 지역/작물 식별자는 화이트리스트(5종, 지정 지역)로 검증.

---

## 7. TypeScript 규칙

- `strict: true` 필수. `any` 금지(불가피하면 `unknown` + 좁히기, 주석 근거).
- API 응답은 반드시 타입 정의 후 사용. 런타임 경계에서 `zod` 등으로 파싱 검증(로컬 LLM/공공 API 응답).
- 타입은 `types/`에 도메인별로. FE-BE 공유 개념(적합도 등급 등)은 단일 정의를 참조.
- `enum` 대신 `as const` + 유니온 선호.
- 옵셔널 체이닝/널 병합으로 결측 데이터 방어. 화면 컴포넌트는 데이터 없음 상태를 항상 처리.

---

## 8. React 규칙

- 함수형 컴포넌트 + Hooks만.
- 컴포넌트는 한 가지 책임. 200줄 넘으면 분리 검토.
- 비즈니스 로직(점수 해석 등)은 컴포넌트가 아니라 `lib/`/`hooks/`로.
- 서버 상태는 데이터 페칭 계층(예: React Query)으로, 로컬 UI 상태는 `useState`/`useReducer`로 분리.
- 도트 그래픽/애니메이션 등 무거운 렌더는 Canvas로. SVG 긴 path 수작업 금지.
- 접근성 기본: 키보드 포커스 가시화, 라디얼 메뉴 Esc 닫기, 색만으로 등급 구분 금지(라벨 병기).
- `prefers-reduced-motion` 존중.

---

## 9. Next.js App Router 규칙

- `app/` 라우팅. 페이지: `login`, `lobby`, `region/[regionId]`, `week`, `result`, `pokedex(재배기록)`.
- **RSC/CSR 경계**: 정적/데이터 페칭은 서버 컴포넌트, 인터랙션(라디얼 UI, 게임 진행)은 `'use client'` 컴포넌트.
- 인증 게이팅은 middleware에서. 비로그인은 `/login`으로.
- 공공데이터/적합도 계산은 Next에서 하지 않는다 — 전부 Spring Boot 백엔드 호출. Next는 BFF/렌더링 역할만.
- 환경변수: 공개값만 `NEXT_PUBLIC_` 접두어. 비밀값은 서버 전용.

---

## 10. JPA / Entity 규칙

- Entity는 `domain/`에. Setter 남발 금지, 의미 있는 도메인 메서드로 상태 변경.
- 연관관계는 지연 로딩(`LAZY`) 기본. N+1은 fetch join/`@EntityGraph`로 대응.
- 마스터 데이터(생육 지침, 지역-격자 매핑, 작년 기상 CSV, 토양)는 마이그레이션/시드로 적재. 런타임에 수정하지 않는다.
- 스키마 변경은 마이그레이션 스크립트로 버전 관리(수동 ALTER 금지).
- 트랜잭션 경계는 Service 계층. Repository는 트랜잭션을 열지 않는다.

---

## 11. Spring Security 인증/인가 규칙

- 인증: 로그인 필요. 세션/토큰 방식은 구현 시 확정하되 비밀은 서버에만.
- **DB 직접 노출 금지**: 클라이언트가 DB에 직접 붙지 않는다. 모든 접근은 Spring Boot를 경유하며 인가는 Service/Controller 계층에서 처리(행 수준 보안은 애플리케이션이 책임).
- 유저는 본인 소유 데이터(세이브, 재배 기록)만 접근 가능하도록 소유권 검증.
- 비밀번호는 해시(BCrypt 등). 평문 저장·로깅 절대 금지.

---

## 12. 공공데이터 API 연동 규칙 ★핵심 리스크★

- **실시간 호출 아님, 배치 사전 적재**가 기본. 유저 요청 경로에서 외부 API를 동기 호출하지 않는다.
  - 토양(흙토람 등): 대상 시/군 데이터를 미리 수집해 DB 적재.
  - 기상: **작년 1년치 실측 데이터를 CSV로 사전 적재**. 게임의 "주간 위험신호"는 실시간 예보가 아니라 **평년(작년 동일 주차) 데이터**로 산출한다(이게 정식 설계 — §PRD 예보 처리 참고).
- **결측·이상치 방어(필수)**: 모든 외부 데이터는 파이프라인 진입 시 검증한다.
  - 범위 밖 값(예: pH 14 초과, 음수 강수량) → 이상치 처리.
  - 결측 → 대체 로직(인접 기간 보간/평년치) 적용 후 "대체됨" 플래그 기록.
  - 어떤 경우에도 점수 산출이 예외로 죽지 않게 한다.
- **rate limit / 재시도**: 배치는 지수 백오프 재시도. API 키는 서버 환경변수로만.
- **출처·수집시각 기록**: 적재 데이터는 출처와 수집 시각을 함께 저장(재현성·디버깅).

---

## 13. LLM 프롬프트 관리 규칙 (로컬 모델)

- **모델**: 로컬 LLM 사용(외부 API 아님). 모델 교체 가능하도록 추론 호출을 인터페이스로 추상화.
- **프롬프트 버전 관리**: 프롬프트는 코드에 흩뿌리지 않고 템플릿 파일/상수로 관리하며 버전을 남긴다.
- **입력 = 구조화된 분석 결과**: 적합도 점수 + 위험 요소 + 지역/작물 컨텍스트를 정형 데이터로 만들어 프롬프트에 주입. LLM에게 수치 계산을 맡기지 않는다(계산은 백엔드 룰 엔진, LLM은 자연어 변환만).
- **응답 검증/폴백**: LLM 응답 파싱 실패·비정상 출력 시 규칙 기반 기본 문구로 폴백. LLM 실패가 게임 진행을 막지 않게 한다.
- **역할 범위**: (1) 지역↔작물 추천 중 택1, (2) 토양 특성 설명, (3) 주간 날씨 기반 행동 추천(필수 — 위험신호 해석, 대회 핵심 요구사항), (4) 행동카드 하이라이트(옵션). 그 외 자유 대화로 확장하지 않는다(스코프 고정).

---

## 14. Git 전략 / Commit 규칙

**브랜치**
- `main`: **배포 전용**. 실전 테스트 없이 여기로 직접 작업/머지하지 않는다.
- `dev`: **통합 및 실전 테스트 브랜치**. 모든 기능 개발·테스트는 여기로 모인다 — PR/머지는 항상 `dev`를 대상으로 연다.
- `feature/<scope>-<요약>`, `fix/<요약>`, `docs/<요약>` → 전부 `dev`에서 분기, `dev`로 머지.
- `dev`에서 충분히 검증된 뒤에만 `dev → main` 승격(배포 담당자가 별도로 진행, 일반 기능 PR의 대상이 아님).
- 직접 `main` 푸시 금지. PR + 리뷰 1인 이상 승인 후 `dev`에 머지.

**커밋 (Conventional Commits)**
```
<type>(<scope>): <제목>

<본문: 왜 이렇게 했는지>
```
- type: `feat` `fix` `docs` `refactor` `test` `chore` `perf`.
- 제목은 명령형, 50자 이내. 한 커밋 = 한 논리 변경.

---

## 15. QA 체크리스트 (PR 전 셀프체크)

- [ ] 5종 작물 각각에서 화면/계산이 정상 동작하는가.
- [ ] 공공데이터 결측/이상치를 넣어도 점수 산출이 죽지 않는가.
- [ ] 데이터 근사(평년치·시군평균) 한계가 UI에 표기되는가.
- [ ] 주차 진행(다음 주 클릭) 시 계절/시간 변화가 반영되는가.
- [ ] 로그인/비로그인 접근 제어가 동작하는가.
- [ ] 색만으로 등급을 구분하지 않는가(라벨 병기).
- [ ] 로컬 LLM 응답 실패 시 폴백 문구가 나오는가.

---

## 16. 코드 리뷰 체크리스트

- [ ] 명세(PRD/DB)와 일치하는가. 임의 결정이 섞이지 않았는가.
- [ ] 농업 기준값이 코드에 하드코딩되지 않고 시드 데이터를 참조하는가.
- [ ] 계산 로직이 결정론적이며 테스트가 있는가.
- [ ] Entity가 컨트롤러 밖으로 새지 않는가(DTO 변환).
- [ ] 외부 데이터 신뢰 경계에서 검증하는가.
- [ ] 불필요한 추상화/미사용 코드가 없는가(YAGNI).

---

## 17. 보안 / 성능 규칙

**보안**
- 비밀(API 키, DB 비밀번호, 모델 경로)은 환경변수/시크릿 매니저. 레포·로그·클라이언트 노출 금지.
- 개인정보/민감값을 URL 쿼리에 넣지 않는다.
- 입력은 신뢰 경계에서 검증(지역·작물 화이트리스트).
- 의존성 취약점 정기 점검.

**성능**
- 유저 요청 경로에 외부 API/무거운 계산 없음 → 사전 적재 + 캐시.
- 적합도 계산 결과는 (지역, 작물, 주차) 단위로 캐시 가능(입력 동일 → 출력 동일).
- 도트 렌더는 Canvas, 스프라이트 재사용. 리렌더 최소화.
- DB 조회는 인덱스 활용, N+1 제거.

---

## 18. 절대 금지사항

1. ❌ 유저 요청 경로에서 공공 API 동기 호출.
2. ❌ 농업 기준값/가중치를 코드에 하드코딩.
3. ❌ LLM에게 점수·수치 계산 위임(자연어 변환만 허용).
4. ❌ 데이터 근사·한계를 숨기고 확정값처럼 표기.
5. ❌ 결측/이상치로 점수 산출이 예외로 죽게 방치.
6. ❌ Entity 직접 노출, 비밀번호 평문 저장/로깅.
7. ❌ 명세에 없는 기능을 코드가 임의로 앞서 구현(성공/실패 RNG, 지역 해금, 이동 시스템 등 — 본 프로젝트에 없음).
8. ❌ `main` 직접 푸시, 리뷰 없는 머지.

---

## 19. 미결정 사항 (작업 전 확인)

- 흙토람 공식 API 문서/발급 절차 존재 여부(미발급 상태).
- 로컬 LLM 구체 모델·구동 방식.
- 지역↔작물 추천 방향(지역→작물 / 작물→지역 중 택1, 메인 디벨로퍼 판단).
