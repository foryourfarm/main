# For Your Farm — UI 진단 & 개선 보고서

> 대상: 2026-08-04 배포본 (대시보드 / 밭 상세 모달 / 상담 화면)
> 작성 관점: 해커톤·공모전 심사자 시선 + 실사용자(초보 귀농인) 시선
> 함께 제출: `시안A_따뜻한흙.html`, `시안B_흙의단면.html`, `mascot-tetbat.svg`, `mascot.js`

---

## 1. 무엇이 문제인가

### 1-1. 치명적: "빈 화면"이 곧 "빈 제품"으로 읽힌다

현재 대시보드는 카드 2장 + `데이터 부족` 배지 + `—` 대시가 전부다. 화면 면적의 약 70%가 비어 있다.
심사자는 **화면의 밀도를 제품의 완성도로 해석**한다. 실제로는 백엔드에 룰 엔진, 3종 기상 API, 흙토람 연동, RAG까지 들어 있는데,
화면에는 그 어떤 흔적도 없다. **가장 큰 손실은 기술이 아니라 기술이 안 보이는 것이다.**

- `데이터 부족` 배지가 2개 중 2개 → "이 서비스는 아직 아무것도 못 한다"로 읽힘
- 적합도 자리에 `—`, 기간 자리에 `전기간` → 정보가 아니라 결함처럼 보임
- 근거 배너("이 수치는 추정치입니다 · 근거 3건 보기")가 접혀 있어 **가장 강한 차별점이 클릭 뒤에 숨음**

### 1-2. 정체성 부재 — "농사 서비스"라는 신호가 색 하나뿐

진한 초록 헤더 외에 농업·흙·계절을 느낄 요소가 없다. 지금 색만 바꾸면 그대로 재고관리 대시보드가 된다.
`For Your Farm` 워드마크는 영문 세리프인데 본문은 한글 고딕이라 **정체성이 이어지지 않는다.**

### 1-3. 타이포그래피가 위계를 만들지 못한다

- 제목/본문/수치가 전부 같은 계열 고딕 한 종 + 굵기만 다름 → 눈이 어디를 먼저 볼지 모름
- `78.5`, `65`, `67.8`이 셋 다 같은 크기·같은 무게. **8월이 A인데 9월이 B라는 사실이 시각적으로 전달되지 않는다**
- `A`, `B` 등급이 작은 회색 글자 → 등급 체계를 설계했는데 화면에서는 부가정보로 보임

### 1-4. 마크다운 렌더링 실패 (상담 화면 · 실제 버그)

상담 답변에 `**씨앗 심기**`, `1. **육묘 관리**` 가 **별표째로 출력**되고 있다.
LLM 응답을 마크다운 파서 없이 `text` 그대로 뿌린 상태다. 심사 데모에서 이 화면이 뜨면
"LLM 붙였는데 마무리를 못 했다"는 인상을 준다. **가장 먼저, 가장 싸게 고칠 수 있는 문제.**

### 1-5. 정보 구조 — 장기/단기 분리라는 핵심 설계가 토글 하나에 갇힘

PRD의 2번 차별점이 "장기/단기 분리"인데, UI에서는 작은 알약 토글 두 개다.
게다가 밭 상세가 **모달**이라 스크롤 여유가 없어 3개월 카드 3개 + 주의 목록만 겨우 들어간다.
점수 분해(평년값 + 전망 보정 → 기대값), 지표별 가중치 같은 **설명가능성 자산이 들어갈 자리가 없다.**

### 1-6. "주의가 필요한 시기"가 경고로 안 읽힌다

`유기물 — 권장 범위를 크게 벗어남` 이 세 줄 반복. 문제는 세 가지다.

1. **숫자가 없다** — 얼마나 벗어났는지 모르니 행동으로 이어지지 않음
2. **행동이 없다** — 위험만 알려주고 "그래서 뭘 하라"가 없음 (PRD 10장의 LLM 행동추천이 이 화면에 없음)
3. **반복이 심하다** — 8/9/10월 세 줄이 거의 동일 문장. "계산이 대충인가?" 하는 의심을 부름

### 1-7. 챗봇 캐릭터가 아이덴티티가 아니다

`🧑 🌱` 이모지 두 개를 나란히 놓은 것. OS/브라우저마다 다르게 렌더링되고, 확대하면 깨지고,
표정 변화가 불가능하다. **"당신의 밭에서 함께 일하는 이웃"이라는 좋은 카피가 시각적으로 뒷받침되지 않는다.**

### 1-8. 그 외 실무 이슈

| 항목 | 문제 | 영향 |
|---|---|---|
| 다크모드 토글(🌙) | 눌러도 톤이 바뀌지 않는 것으로 보임 | 미완성 인상 |
| 상담 화면 헤더 | 상단 네비만 있고 어떤 밭 컨텍스트인지 표시 없음 | RAG 근거 주입이 안 보임 |
| 밭 카드 클릭 영역 | 카드 전체인지 배지인지 불명확 | 첫 사용자 이탈 |
| 폰트 크기 | 본문 13~14px 추정 | 귀농 주 사용층(40~60대) 가독성 부족 |
| 로딩/빈 상태 | 없음 | API 지연 시 화면이 죽은 것처럼 보임 |

---

## 3. 추가하면 좋을 시각적 요소

### 3-1. 지금 있는 데이터로 바로 만들 수 있는 것 (우선순위 높음)

| 요소 | 설명 | 왜 효과적인가 |
|---|---|---|
| **점수 분해 바 / 워터폴** | 100점 → 낮기온 −8.4 → 강수 −6.1 → 유기물 −3.9 → 78.5 | 심사 최대 어필. "설명가능성"을 한 장면으로 증명. 시안 A·B 모두 포함 |
| **권장구간 + 현재값 핀** | 유기물 12 ▮ (권장 25~35 구간을 초록 띠로) | "크게 벗어남" 문장보다 100배 빠르게 이해됨. 시안 B의 뿌리층 참고 |
| **생육 단계 곡선 스테퍼** | 발아→개화→결실비대→착색→수확, 현재 단계 확대 | 직선 프로그레스바보다 유기적이고, "시즌 내내 함께 간다"는 정체성과 맞음 |
| **낮/야간 온도 이중 곡선** | 낮 최고(주황 실선) / 야간 최저(파랑 점선) / 평년(회색) | PRD의 "야간 최저 분리" 설계가 눈에 보이게 됨. A씨 사례를 그림으로 반박 |
| **미니 단면 썸네일** | 밭 카드 안에 하늘/지표/뿌리 3층 띠 + 뿌리 그림 | 데이터 없는 밭은 뿌리층이 해칭으로 비어 보임 → 결함이 아니라 **의도된 상태**로 읽힘 |
| **주간 날씨 스트립** | 7일 알약, 오늘 강조 | 빈 화면을 채우면서 "매일 접속"이라는 사용 패턴을 암시 |
| **오늘의 할 일 체크리스트** | 체크 시 토양 추정에 반영된다는 안내 문구 포함 | 행동 기록 기능(PRD §4.7)을 화면에 존재하게 만듦 |
| **근거 칩 항상 노출** | 기상청 / 흙토람 / 농사로 / 평년값 배지를 접지 않고 노출 | 접어 두면 없는 기능. 펴 두면 최대 무기 |

### 3-2. 조금 더 만들면 좋은 것

- **예보 vs 실측 스파크라인** — "신뢰도 축적 11일째" 같은 카운터와 함께. 지금은 자랑할 수 없는 지표지만, **쌓이는 중이라는 사실 자체를 시각화**하면 정직함이 강점이 됨
- **읍면동 미니맵** — 시/군 폴리곤 안에 읍면동 점 하나. "기상은 시/군, 토양은 읍면동" 이라는 설계 결정을 한 장에 설명 (PRD §5의 가장 어려운 부분)
- **24시간 위험 히트맵** — 시간대별 과습/고온 위험을 24칸 띠로. 단기 탭의 밀도를 크게 올림
- **작물 5종 손그림 아이콘 세트** — 이모지(🍎🥬) 대신 같은 선 굵기의 벡터 아이콘. 브랜드 일관성이 여기서 갈림
- **빈 상태(Empty state) 일러스트** — "토양 표본이 아직 없어요" + 텃밭이 worry 표정 + 다음 행동 버튼. 지금의 `데이터 부족` 배지를 대체
- **로딩 = 씨앗 발아 애니메이션** — 캐시 미스 시 수 초 대기를 캐릭터로 견딤
- **문서 근거 스니펫 뷰어** — 답변 하단 칩을 누르면 실제 문서 문단이 하이라이트되어 열림. RAG를 "말"이 아니라 "증거"로

### 3-3. 접근성 — 이 서비스에선 기능이다

주 사용층이 40~60대 귀농인이라는 점에서 다음은 **디자인 취향이 아니라 요구사항**입니다.

- 본문 최소 **15~16px**, 행간 1.65 이상 (현재 13~14px 추정)
- 등급을 **색 + 문자 + 위치** 3중으로 표기 (색맹 대응: A/B를 색만으로 구분하지 않기)
- 터치 타깃 최소 44px — 현재 알약 토글이 작음
- 다크모드는 "검정"이 아니라 **밤의 흙색**(#241C16 계열)으로. 눈부심 감소 + 정체성 유지 (시안 A에 구현)


---

## 4. 시안 요약

|       | **시안 A · 따뜻한 흙**              |
| ----- | ----------------------------- |
| 성격    | 현재 골격 유지 · 안전한 개선             |
| 구조    | 상단 탭 + 12칼럼 비대칭 벤토 그리드        |
| 제목 폰트 | Cafe24 Ssurround (둥근 굵은 고딕)   |
| 숫자 폰트 | Baloo 2 (통통한 라운드)             |
| 강점    | 구현 부담 적음 · 정보 밀도 높음 · 다크모드 포함 |
| 리스크   | "잘 만든 대시보드"에서 그칠 수 있음         |
| 추천    | 1차 제출 안전판                     |

---

## 5. 폰트 다운로드 (눈누)

두 HTML 모두 **로컬 → CDN → 대체 폰트** 순으로 자동 폴백합니다.
로컬 폰트를 쓰려면 HTML과 같은 폴더에 `fonts/` 를 만들고 아래 파일을 넣으세요.

| 폰트 | 용도 | 눈누 | 파일명 |
|---|---|---|---|
| Cafe24 Ssurround | 시안 A 제목 | https://noonnu.cc/font_page/121 | `fonts/Cafe24Ssurround.woff2` |
| 마루 부리 | 시안 B 제목 | https://noonnu.cc 검색 "마루 부리" | `fonts/MaruBuri-SemiBold.woff2`, `-Bold.woff2` |
| Pretendard | 본문 (양쪽) | https://noonnu.cc/font_page/335 | CDN 자동 로드 (설치 불필요) |
| 온글잎 만두몽키 / 박다현체 | 마스코트 대사 | https://noonnu.cc 검색 "온글잎" | `fonts/Ownglyph_mandumonkey.woff2` |

폰트를 넣지 않아도 Google Fonts의 Jua / 고운바탕 / Gaegu 로 대체되어 레이아웃은 그대로 유지됩니다.
